import {
  Checkbox,
  Input,
  NumericTextBox,
  TextBox
} from "./chunk-TEESGLV4.js";
import {
  filterBy,
  isCompositeFilterDescriptor
} from "./chunk-CO5IBX6Q.js";
import {
  DatePicker
} from "./chunk-4XZS3TFL.js";
import "./chunk-JUNIATII.js";
import {
  DropDownList
} from "./chunk-BWCWHVIM.js";
import "./chunk-JEFUAUFC.js";
import "./chunk-X4OET5D2.js";
import {
  Reveal
} from "./chunk-2SAW7IS7.js";
import "./chunk-EL4VMBJ6.js";
import "./chunk-Y67KMEH3.js";
import "./chunk-4R6ABWJA.js";
import {
  Button,
  ButtonGroup,
  Toolbar,
  ToolbarItem
} from "./chunk-FUIIQ4Y4.js";
import {
  cancelIcon,
  caretAltDownIcon,
  caretAltLeftIcon,
  caretAltRightIcon,
  caretAltToLeftIcon,
  caretAltToRightIcon,
  filterAddExpressionIcon,
  filterAddGroupIcon,
  filterClearIcon,
  filterIcon,
  groupIcon,
  minusIcon,
  moreVerticalIcon,
  plusIcon,
  searchIcon,
  sortAscSmallIcon,
  sortDescSmallIcon,
  ungroupIcon,
  xCircleIcon,
  xIcon
} from "./chunk-RFS2H3W2.js";
import {
  provideIntlService,
  provideLocalizationService,
  registerForIntl,
  registerForLocalization,
  useInternationalization,
  useLocalization
} from "./chunk-NQBGXZOD.js";
import {
  Popup
} from "./chunk-XVBPUVJV.js";
import {
  require_react_dom
} from "./chunk-3UUEUCWE.js";
import {
  Draggable,
  IconWrap,
  Keys,
  canUseDOM,
  classNames,
  clone,
  dispatchEvent,
  extendDataItem,
  getScrollbarWidth,
  getter,
  guid,
  hasRelativeStackingContext,
  kendoThemeMaps,
  mapTree,
  memoizeOne,
  setScrollbarWidth,
  toIconName,
  validatePackage2 as validatePackage
} from "./chunk-KTMNFILY.js";
import {
  require_prop_types
} from "./chunk-O6GWARG4.js";
import {
  require_react
} from "./chunk-ABDRZE5M.js";
import {
  __toESM
} from "./chunk-LFBQMW2U.js";

// node_modules/@progress/kendo-react-grid/dist/es/Grid.js
var React61 = __toESM(require_react());
var PropTypes11 = __toESM(require_prop_types());

// node_modules/@progress/kendo-react-data-tools/dist/es/pager/Pager.js
var React4 = __toESM(require_react());
var PropTypes = __toESM(require_prop_types());

// node_modules/@progress/kendo-react-data-tools/dist/es/pager/PagerNumericButtons.js
var React = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/messages/index.js
var _a;
var pagerInfo = "pager.info";
var pagerFirstPage = "pager.firstPage";
var pagerPreviousPage = "pager.previousPage";
var pagerNextPage = "pager.nextPage";
var pagerLastPage = "pager.lastPage";
var pagerItemPerPage = "pager.itemsPerPage";
var pagerPageSelection = "pager.pageSelection";
var pagerPage = "pager.page";
var pagerOf = "pager.of";
var pagerTotalPages = "pager.totalPages";
var pagerPageNumberLabel = "pager.pageLabel";
var pagerAriaKeyshortcuts = "pager.ariaKeyshortcuts";
var pagerAriaLabel = "pager.ariaLabel";
var pagerMobileSelect = "pager.mobileSelect";
var filterEqOperator = "filter.eqOperator";
var filterNotEqOperator = "filter.notEqOperator";
var filterIsNullOperator = "filter.isNullOperator";
var filterIsNotNullOperator = "filter.isNotNullOperator";
var filterIsEmptyOperator = "filter.isEmptyOperator";
var filterIsNotEmptyOperator = "filter.isNotEmptyOperator";
var filterStartsWithOperator = "filter.startsWithOperator";
var filterContainsOperator = "filter.containsOperator";
var filterNotContainsOperator = "filter.notContainsOperator";
var filterEndsWithOperator = "filter.endsWithOperator";
var filterGteOperator = "filter.gteOperator";
var filterGtOperator = "filter.gtOperator";
var filterLteOperator = "filter.lteOperator";
var filterLtOperator = "filter.ltOperator";
var filterIsTrue = "filter.isTrue";
var filterIsFalse = "filter.isFalse";
var filterAfterOrEqualOperator = "filter.afterOrEqualOperator";
var filterAfterOperator = "filter.afterOperator";
var filterBeforeOperator = "filter.beforeOperator";
var filterBeforeOrEqualOperator = "filter.beforeOrEqualOperator";
var filterAndLogic = "filter.andLogic";
var filterOrLogic = "filter.orLogic";
var filterAddExpression = "filter.addExpression";
var filterAddGroup = "filter.addGroup";
var filterClose = "filter.close";
var filterGroupAriaLabel = "filter.groupAriaLabel";
var filterExpressionAriaLabel = "filter.expressionAriaLabel";
var filterExpressionDropdownAriaLabel = "filter.expressionDropdownAriaLabel";
var filterExpressionOperatorDropdownAriaLabel = "filter.expressionOperatorDropdownAriaLabel";
var filterEnumFilterDropdownAriaLabel = "filter.enumFilterDropdownAriaLabel";
var filterNumericFilterAriaLabel = "filter.numericFilterAriaLabel";
var filterTextFilterAriaLabel = "filter.textFilterAriaLabel";
var columnMenuFilterClearButton = "columnMenu.filterClearButton";
var columnMenuFilterSubmitButton = "columnMenu.filterSubmitButton";
var columnMenuFilterTitle = "columnMenu.filterTitle";
var columnMenuSortAscending = "columnMenu.sortAscending";
var columnMenuSortDescending = "columnMenu.sortDescending";
var columnMenuFilterEqOperator = "columnMenu.filterEqOperator";
var columnMenuFilterNotEqOperator = "columnMenu.filterNotEqOperator";
var columnMenuFilterIsNullOperator = "columnMenu.filterIsNullOperator";
var columnMenuFilterIsNotNullOperator = "columnMenu.filterIsNotNullOperator";
var columnMenuFilterIsEmptyOperator = "columnMenu.filterIsEmptyOperator";
var columnMenuFilterIsNotEmptyOperator = "columnMenu.filterIsNotEmptyOperator";
var columnMenuFilterStartsWithOperator = "columnMenu.filterStartsWithOperator";
var columnMenuFilterContainsOperator = "columnMenu.filterContainsOperator";
var columnMenuFilterNotContainsOperator = "columnMenu.filterNotContainsOperator";
var columnMenuFilterEndsWithOperator = "columnMenu.filterEndsWithOperator";
var columnMenuFilterGteOperator = "columnMenu.filterGteOperator";
var columnMenuFilterGtOperator = "columnMenu.filterGtOperator";
var columnMenuFilterLteOperator = "columnMenu.filterLteOperator";
var columnMenuFilterLtOperator = "columnMenu.filterLtOperator";
var columnMenuFilterIsTrue = "columnMenu.filterIsTrue";
var columnMenuFilterAfterOrEqualOperator = "columnMenu.filterAfterOrEqualOperator";
var columnMenuFilterAfterOperator = "columnMenu.filterAfterOperator";
var columnMenuFilterBeforeOperator = "columnMenu.filterBeforeOperator";
var columnMenuFilterBeforeOrEqualOperator = "columnMenu.filterBeforeOrEqualOperator";
var columnMenuFilterAndLogic = "columnMenu.filterAndLogic";
var columnMenuFilterOrLogic = "columnMenu.filterOrLogic";
var sortAriaLabel = "sort.ariaLabel";
var filterAriaLabel = "filter.ariaLabel";
var messages = (_a = {}, _a[pagerItemPerPage] = "items per page", _a[pagerPageSelection] = "Select page", _a[pagerInfo] = "{0} - {1} of {2} items", _a[pagerFirstPage] = "Go to the first page", _a[pagerPreviousPage] = "Go to the previous page", _a[pagerNextPage] = "Go to the next page", _a[pagerLastPage] = "Go to the last page", _a[pagerPage] = "Page", _a[pagerOf] = "Of", _a[pagerTotalPages] = "{0}", _a[pagerPageNumberLabel] = "Page", _a[pagerAriaKeyshortcuts] = "Enter ArrowRight ArrowLeft", _a[pagerAriaLabel] = "Pager", _a[pagerMobileSelect] = "Select", _a[sortAriaLabel] = "Sortable", _a[filterAriaLabel] = "Filter", _a[filterEqOperator] = "Is equal to", _a[filterNotEqOperator] = "Is not equal to", _a[filterIsNullOperator] = "Is null", _a[filterIsNotNullOperator] = "Is not null", _a[filterIsEmptyOperator] = "Is empty", _a[filterIsNotEmptyOperator] = "Is not empty", _a[filterStartsWithOperator] = "Starts with", _a[filterContainsOperator] = "Contains", _a[filterNotContainsOperator] = "Does not contain", _a[filterEndsWithOperator] = "Ends with", _a[filterGteOperator] = "Is greater than or equal to", _a[filterGtOperator] = "Is greater than", _a[filterLteOperator] = "Is less than or equal to", _a[filterLtOperator] = "Is less than", _a[filterIsTrue] = "Is true", _a[filterIsFalse] = "Is false", _a[filterAfterOrEqualOperator] = "Is after or equal to", _a[filterAfterOperator] = "Is after", _a[filterBeforeOperator] = "Is before", _a[filterBeforeOrEqualOperator] = "Is before or equal to", _a[filterAndLogic] = "And", _a[filterOrLogic] = "Or", _a[filterAddExpression] = "Add Expression", _a[filterAddGroup] = "Add Group", _a[filterClose] = "Close", _a[filterGroupAriaLabel] = "Filter toolbar", _a[filterExpressionAriaLabel] = "Filter expression row", _a[filterExpressionDropdownAriaLabel] = "Filter dropdown", _a[filterExpressionOperatorDropdownAriaLabel] = "Filter operator dropdown", _a[filterEnumFilterDropdownAriaLabel] = "Select True/False", _a[filterNumericFilterAriaLabel] = "Enter number", _a[filterTextFilterAriaLabel] = "Enter number", _a[columnMenuFilterClearButton] = "Clear", _a[columnMenuFilterSubmitButton] = "Filter", _a[columnMenuFilterTitle] = "Filter", _a[columnMenuSortAscending] = "Sort Ascending", _a[columnMenuSortDescending] = "Sort Descending", _a[columnMenuFilterEqOperator] = "Is equal to", _a[columnMenuFilterNotEqOperator] = "Is not equal to", _a[columnMenuFilterIsNullOperator] = "Is null", _a[columnMenuFilterIsNotNullOperator] = "Is not null", _a[columnMenuFilterIsEmptyOperator] = "Is empty", _a[columnMenuFilterIsNotEmptyOperator] = "Is not empty", _a[columnMenuFilterStartsWithOperator] = "Starts with", _a[columnMenuFilterContainsOperator] = "Contains", _a[columnMenuFilterNotContainsOperator] = "Does not contain", _a[columnMenuFilterEndsWithOperator] = "Ends with", _a[columnMenuFilterGteOperator] = "Is greater than or equal to", _a[columnMenuFilterGtOperator] = "Is greater than", _a[columnMenuFilterLteOperator] = "Is less than or equal to", _a[columnMenuFilterLtOperator] = "Is less than", _a[columnMenuFilterIsTrue] = "Is true", _a[columnMenuFilterAfterOrEqualOperator] = "Is after or equal to", _a[columnMenuFilterAfterOperator] = "Is after", _a[columnMenuFilterBeforeOperator] = "Is before", _a[columnMenuFilterBeforeOrEqualOperator] = "Is before or equal to", _a[columnMenuFilterAndLogic] = "And", _a[columnMenuFilterOrLogic] = "Or", _a);

// node_modules/@progress/kendo-react-data-tools/dist/es/pager/PagerNumericButtons.js
var __extends = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var PagerNumericButtons = (
  /** @class */
  function(_super) {
    __extends(PagerNumericButtons2, _super);
    function PagerNumericButtons2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.handlePageChange = function(e, page) {
        e.preventDefault();
        _this.props.pageChange(page, e);
      };
      _this.handleSelectPageChange = function(e) {
        e.preventDefault();
        _this.props.pageChange(e.target.value, e);
      };
      return _this;
    }
    PagerNumericButtons2.prototype.render = function() {
      var _a4;
      var _this = this;
      var localizationService = provideLocalizationService(this);
      var mobileSelectMessage = this.props.messagesMap ? this.props.messagesMap(pagerMobileSelect) : { messageKey: pagerMobileSelect, defaultMessage: messages[pagerMobileSelect] };
      var pageNumberLabelMessage = this.props.messagesMap ? this.props.messagesMap(pagerPageNumberLabel) : { messageKey: pagerPageNumberLabel, defaultMessage: messages[pagerPageNumberLabel] };
      var buttons = [];
      for (var idx = this.start; idx <= this.end; idx++) {
        buttons.push(idx);
      }
      var numerics = buttons.map(function(page) {
        return React.createElement(Button, { className: classNames({ "k-selected": _this.props.currentPage === page }), key: page, fillMode: "flat", themeColor: "primary", size: _this.props.size, rounded: null, role: "button", "aria-label": localizationService.toLanguageString(pageNumberLabelMessage.messageKey, pageNumberLabelMessage.defaultMessage) + " " + page, "aria-current": _this.props.currentPage === page ? true : void 0, onClick: function(e) {
          return _this.handlePageChange(e, page);
        } }, page);
      });
      var options = buttons.map(function(page) {
        return React.createElement("option", { key: page }, page);
      });
      return React.createElement(
        React.Fragment,
        null,
        React.createElement("select", { className: classNames("k-picker k-dropdown-list k-dropdown k-rounded-md", (_a4 = {}, _a4["k-picker-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _a4)), "aria-label": localizationService.toLanguageString(mobileSelectMessage.messageKey, mobileSelectMessage.defaultMessage), value: this.props.currentPage, onChange: function(e) {
          return _this.handleSelectPageChange(e);
        } }, options),
        React.createElement("div", { className: "k-pager-numbers" }, numerics)
      );
    };
    Object.defineProperty(PagerNumericButtons2.prototype, "end", {
      get: function() {
        return Math.min(this.start + this.props.buttonCount - 1, this.props.totalPages);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(PagerNumericButtons2.prototype, "start", {
      get: function() {
        var page = this.props.currentPage;
        var buttonCount = this.props.buttonCount;
        if (page > buttonCount) {
          var reminder = page % buttonCount;
          return reminder === 0 ? page - buttonCount + 1 : page - reminder + 1;
        }
        return 1;
      },
      enumerable: false,
      configurable: true
    });
    return PagerNumericButtons2;
  }(React.Component)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/pager/PagerInput.js
var React2 = __toESM(require_react());
var __extends2 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var PagerInput = (
  /** @class */
  function(_super) {
    __extends2(PagerInput2, _super);
    function PagerInput2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this._text = void 0;
      _this.onChange = function(e) {
        var text = _this._text = e.target.value.replace(/\D/g, "");
        _this.forceUpdate();
        if (text) {
          _this.props.pageChange(parseInt(text, 10), e);
        }
      };
      _this.value = function() {
        var value = _this._text === void 0 ? _this.props.currentPage.toString() : _this._text;
        _this._text = void 0;
        return value;
      };
      return _this;
    }
    PagerInput2.prototype.render = function() {
      var _a4;
      var _this = this;
      var intlService = provideIntlService(this);
      var localizationService = provideLocalizationService(this);
      var pageMessage = this.props.messagesMap ? this.props.messagesMap(pagerPage) : { messageKey: pagerPage, defaultMessage: messages[pagerPage] };
      var ofMessage = this.props.messagesMap ? this.props.messagesMap(pagerOf) : { messageKey: pagerOf, defaultMessage: messages[pagerOf] };
      var totalPagesMessage = this.props.messagesMap ? this.props.messagesMap(pagerTotalPages) : { messageKey: pagerTotalPages, defaultMessage: messages[pagerTotalPages] };
      var mobileSelectMessage = this.props.messagesMap ? this.props.messagesMap(pagerMobileSelect) : { messageKey: pagerMobileSelect, defaultMessage: messages[pagerMobileSelect] };
      var buttons = [];
      for (var idx = this.start; idx <= this.end; idx++) {
        buttons.push(idx);
      }
      var options = buttons.map(function(page) {
        return React2.createElement("option", { key: page }, page);
      });
      return React2.createElement(
        React2.Fragment,
        null,
        React2.createElement("select", { className: classNames("k-picker k-dropdown-list k-dropdown k-rounded-md", (_a4 = {}, _a4["k-picker-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _a4)), "aria-label": localizationService.toLanguageString(mobileSelectMessage.messageKey, mobileSelectMessage.defaultMessage), value: this.props.currentPage, onChange: this.onChange }, options),
        React2.createElement(
          "span",
          { className: "k-pager-input k-label" },
          localizationService.toLanguageString(pageMessage.messageKey, pageMessage.defaultMessage),
          React2.createElement(
            "span",
            { className: "k-textbox k-input k-input-md k-rounded-md k-input-solid" },
            React2.createElement("input", { className: "k-input-inner", value: this.value(), onBlur: function() {
              return _this.forceUpdate();
            }, onChange: this.onChange })
          ),
          "".concat(localizationService.toLanguageString(ofMessage.messageKey, ofMessage.defaultMessage), " ").concat(intlService.format(localizationService.toLanguageString(totalPagesMessage.messageKey, totalPagesMessage.defaultMessage), [
            this.props.totalPages
          ]))
        )
      );
    };
    Object.defineProperty(PagerInput2.prototype, "end", {
      get: function() {
        return Math.min(this.start + this.props.buttonCount - 1, this.props.totalPages);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(PagerInput2.prototype, "start", {
      get: function() {
        var page = this.props.currentPage;
        var buttonCount = this.props.buttonCount;
        if (page > buttonCount) {
          var reminder = page % buttonCount;
          return reminder === 0 ? page - buttonCount + 1 : page - reminder + 1;
        }
        return 1;
      },
      enumerable: false,
      configurable: true
    });
    return PagerInput2;
  }(React2.Component)
);
registerForLocalization(PagerInput);

// node_modules/@progress/kendo-react-data-tools/dist/es/pager/PagerPageSizes.js
var React3 = __toESM(require_react());
var __extends3 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var PagerPageSizes = (
  /** @class */
  function(_super) {
    __extends3(PagerPageSizes2, _super);
    function PagerPageSizes2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.pageSizeChange = function(e) {
        if (_this.props.pageChange) {
          _this.props.pageChange({ skip: 0, take: parseInt(e.target.value, 10) }, e);
        }
      };
      return _this;
    }
    PagerPageSizes2.prototype.render = function() {
      var _a4 = this.props, value = _a4.value, pageSizes = _a4.pageSizes, pageSize = _a4.pageSize, messagesMap = _a4.messagesMap;
      var sizes = pageSizes.slice();
      if (value === void 0 && sizes.filter(function(s) {
        return s === pageSize;
      }).length === 0) {
        sizes.unshift(pageSize);
      }
      var itemPerPageMessage = messagesMap ? messagesMap(pagerItemPerPage) : { messageKey: pagerItemPerPage, defaultMessage: messages[pagerItemPerPage] };
      var pageSelectionMessage = messagesMap ? messagesMap(pagerPageSelection) : { messageKey: pagerPageSelection, defaultMessage: messages[pagerPageSelection] };
      return React3.createElement(
        "span",
        { className: "k-pager-sizes" },
        React3.createElement(DropDownList, { value: value !== void 0 ? value : pageSize, data: sizes, onChange: this.pageSizeChange, ariaLabel: provideLocalizationService(this).toLanguageString(pageSelectionMessage.messageKey, pageSelectionMessage.defaultMessage), size: this.props.size }),
        provideLocalizationService(this).toLanguageString(itemPerPageMessage.messageKey, itemPerPageMessage.defaultMessage)
      );
    };
    return PagerPageSizes2;
  }(React3.Component)
);
registerForLocalization(PagerPageSizes);

// node_modules/@progress/kendo-react-data-tools/dist/es/package-metadata.js
var packageMetadata = {
  name: "@progress/kendo-react-data-tools",
  productName: "KendoReact",
  productCodes: ["KENDOUIREACT", "KENDOUICOMPLETE"],
  publishDate: 1680172062,
  version: "",
  licensingDocsUrl: "https://www.telerik.com/kendo-react-ui/my-license/?utm_medium=product&utm_source=kendoreact&utm_campaign=kendo-ui-react-purchase-license-keys-warning"
};

// node_modules/@progress/kendo-react-data-tools/dist/es/pager/Pager.js
var __extends4 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign = function() {
  __assign = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign.apply(this, arguments);
};
var RESPONSIVE_BREAKPOINT_MEDIUM = 600;
var RESPONSIVE_BREAKPOINT_LARGE = 768;
var Pager = (
  /** @class */
  function(_super) {
    __extends4(Pager2, _super);
    function Pager2(props) {
      var _this = _super.call(this, props) || this;
      _this._element = null;
      _this.onPageChange = function(eventData, targetEvent) {
        var event = __assign({ target: _this, syntheticEvent: targetEvent.syntheticEvent, nativeEvent: targetEvent.nativeEvent, targetEvent }, eventData);
        if (_this.props.onPageChange) {
          _this.props.onPageChange.call(void 0, event);
        }
      };
      _this.changePage = function(page, event) {
        if (_this.props.onPageChange && page > 0 && page <= _this.totalPages) {
          _this.props.onPageChange({
            target: _this,
            skip: (page - 1) * _this.props.take,
            take: _this.props.take,
            syntheticEvent: event,
            nativeEvent: event.nativeEvent,
            targetEvent: event
          });
        }
      };
      _this.onWindowResize = function() {
        var element = _this.element;
        if (!element || !_this.props.responsive) {
          return;
        }
        var width = element.offsetWidth;
        if (width < RESPONSIVE_BREAKPOINT_MEDIUM) {
          element.classList.remove("k-pager-md");
          element.classList.add("k-pager-sm");
        } else if (width >= RESPONSIVE_BREAKPOINT_MEDIUM && width < RESPONSIVE_BREAKPOINT_LARGE) {
          element.classList.add("k-pager-md");
          element.classList.remove("k-pager-sm");
        } else {
          element.classList.remove("k-pager-md");
          element.classList.remove("k-pager-sm");
        }
      };
      validatePackage(packageMetadata);
      return _this;
    }
    Object.defineProperty(Pager2.prototype, "element", {
      /**
       * Returns the HTML element of the Pager component.
       */
      get: function() {
        return this._element;
      },
      enumerable: false,
      configurable: true
    });
    Pager2.prototype.componentDidMount = function() {
      window.addEventListener("resize", this.onWindowResize);
      this.onWindowResize();
    };
    Pager2.prototype.componentWillUnmount = function() {
      window.removeEventListener("resize", this.onWindowResize);
    };
    Pager2.prototype.render = function() {
      var _a4;
      var _this = this;
      var _b2 = this.props, skip = _b2.skip, take = _b2.take, total = _b2.total, pageSizes = _b2.pageSizes, size = _b2.size, messagesMap = _b2.messagesMap, pageSizeValue = _b2.pageSizeValue;
      var localizationService = provideLocalizationService(this);
      var intlService = provideIntlService(this);
      var currentPage = Math.floor(skip / take) + 1;
      var changer = this.props.type === "numeric" ? React4.createElement(PagerNumericButtons, { buttonCount: this.props.buttonCount || 0, totalPages: this.totalPages, currentPage, pageChange: this.changePage, messagesMap: this.props.messagesMap, size: this.props.size }) : React4.createElement(PagerInput, { buttonCount: this.props.buttonCount || 0, currentPage, totalPages: this.totalPages, pageChange: this.changePage, messagesMap, size: this.props.size });
      var firstPageMessage = messagesMap ? messagesMap(pagerFirstPage) : { messageKey: pagerFirstPage, defaultMessage: messages[pagerFirstPage] };
      var previousPageMessage = messagesMap ? messagesMap(pagerPreviousPage) : { messageKey: pagerPreviousPage, defaultMessage: messages[pagerPreviousPage] };
      var nextPageMessage = messagesMap ? messagesMap(pagerNextPage) : { messageKey: pagerNextPage, defaultMessage: messages[pagerNextPage] };
      var lastPageMessage = messagesMap ? messagesMap(pagerLastPage) : { messageKey: pagerLastPage, defaultMessage: messages[pagerLastPage] };
      var infoMessage = messagesMap ? messagesMap(pagerInfo) : { messageKey: pagerInfo, defaultMessage: messages[pagerInfo] };
      var ariaKeyshortcutsMessage = messagesMap ? messagesMap(pagerAriaKeyshortcuts) : { messageKey: pagerAriaKeyshortcuts, defaultMessage: messages[pagerAriaKeyshortcuts] };
      var ariaLabelMessage = messagesMap ? messagesMap(pagerAriaLabel) : { messageKey: pagerAriaLabel, defaultMessage: messages[pagerAriaLabel] };
      var first, prev, next, last;
      if (this.props.previousNext) {
        first = this.renderButton(1, "k-pager-nav k-pager-first" + (currentPage === 1 ? " k-disabled" : ""), localizationService.toLanguageString(firstPageMessage.messageKey, firstPageMessage.defaultMessage), "caret-alt-to-left", caretAltToLeftIcon, currentPage === 1 ? true : void 0);
        prev = this.renderButton(currentPage - 1, "k-pager-nav" + (currentPage === 1 ? " k-disabled" : ""), localizationService.toLanguageString(previousPageMessage.messageKey, previousPageMessage.defaultMessage), "caret-alt-left", caretAltLeftIcon, currentPage === 1 ? true : void 0);
        next = this.renderButton(currentPage + 1, "k-pager-nav" + (currentPage >= this.totalPages ? " k-disabled" : ""), localizationService.toLanguageString(nextPageMessage.messageKey, nextPageMessage.defaultMessage), "caret-alt-right", caretAltRightIcon, currentPage >= this.totalPages ? true : void 0);
        last = this.renderButton(this.totalPages, "k-pager-nav k-pager-last" + (currentPage >= this.totalPages ? " k-disabled" : ""), localizationService.toLanguageString(lastPageMessage.messageKey, lastPageMessage.defaultMessage), "caret-alt-to-right", caretAltToRightIcon, currentPage >= this.totalPages ? true : void 0);
      }
      var pagerPageSizes = pageSizes && React4.createElement(PagerPageSizes, { pageChange: this.onPageChange, pageSize: take, pageSizes, value: pageSizeValue, messagesMap, size: this.props.size });
      var info = this.props.info && React4.createElement("div", { className: "k-pager-info" }, intlService.format(localizationService.toLanguageString(infoMessage.messageKey, infoMessage.defaultMessage), [
        Math.min(skip + 1, total),
        Math.min(skip + take, total),
        total
      ]));
      return React4.createElement(
        "div",
        { className: classNames("k-pager", (_a4 = {}, _a4["k-pager-".concat(kendoThemeMaps.sizeMap[size] || size)] = size, _a4), this.props.className), style: this.props.style, role: "application", "aria-roledescription": "pager", "aria-keyshortcuts": localizationService.toLanguageString(ariaKeyshortcutsMessage.messageKey, ariaKeyshortcutsMessage.defaultMessage), "aria-label": localizationService.toLanguageString(ariaLabelMessage.messageKey, ariaLabelMessage.defaultMessage), ref: function(element) {
          return _this._element = element;
        } },
        React4.createElement(
          "div",
          { className: classNames("k-pager-numbers-wrap") },
          first,
          prev,
          changer,
          next,
          last
        ),
        pagerPageSizes,
        info
      );
    };
    Pager2.prototype.renderButton = function(page, className, title, icon, svgIcon, isDisabled) {
      var _this = this;
      return React4.createElement(Button, { fillMode: "flat", themeColor: "base", size: this.props.size, rounded: null, className, icon, svgIcon, title, role: "button", "aria-disabled": isDisabled, onClick: function(e) {
        e.preventDefault();
        _this.changePage(page, e);
      } });
    };
    Object.defineProperty(Pager2.prototype, "totalPages", {
      get: function() {
        return Math.ceil((this.props.total || 0) / this.props.take);
      },
      enumerable: false,
      configurable: true
    });
    Pager2.propTypes = {
      className: PropTypes.string,
      style: PropTypes.object,
      total: PropTypes.number.isRequired,
      skip: PropTypes.number.isRequired,
      take: PropTypes.number.isRequired,
      buttonCount: PropTypes.number,
      info: PropTypes.bool,
      type: PropTypes.oneOf(["numeric", "input"]),
      pageSizes: PropTypes.arrayOf(PropTypes.oneOfType([
        PropTypes.string,
        PropTypes.number
      ])),
      previousNext: PropTypes.bool,
      onPageChange: PropTypes.func,
      messagesMap: PropTypes.func,
      size: PropTypes.oneOf([null, "small", "medium", "large"])
    };
    Pager2.defaultProps = {
      buttonCount: 10,
      info: true,
      type: "numeric",
      size: "medium"
    };
    return Pager2;
  }(React4.Component)
);
registerForIntl(Pager);
registerForLocalization(Pager);

// node_modules/@progress/kendo-react-data-tools/dist/es/filteringCells/FilterComponent.js
var React5 = __toESM(require_react());
var __extends5 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign2 = function() {
  __assign2 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign2.apply(this, arguments);
};
var __spreadArray = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var createFilterComponent = function(settings5) {
  var Cell = (
    /** @class */
    function(_super) {
      __extends5(FilterComponent, _super);
      function FilterComponent() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        _this.filterChange = function(event) {
          if (_this.props.onFilterChange) {
            _this.props.onFilterChange(event);
          }
        };
        _this.inputChange = function(e) {
          var value = e.target.value;
          var _a4 = _this.props, _b2 = _a4.filter, filter = _b2 === void 0 ? [] : _b2, _c2 = _a4.field, field = _c2 === void 0 ? "" : _c2;
          var existing = _this.findCellFilter();
          var filterResult = existing ? filter.map(function(f) {
            return f.field === field ? __assign2(__assign2({}, existing), { value }) : f;
          }) : __spreadArray(__spreadArray([], filter, true), [__assign2(__assign2({}, settings5.emptyFilter), { value, field })], false);
          _this.filterChange({
            filter: filterResult,
            field,
            syntheticEvent: e.syntheticEvent
          });
        };
        _this.operatorChange = function(e) {
          var operator = e.target.value.operator;
          var _a4 = _this.props, _b2 = _a4.filter, filter = _b2 === void 0 ? [] : _b2, _c2 = _a4.field, field = _c2 === void 0 ? "" : _c2;
          var existing = _this.findCellFilter();
          var filterResult = existing ? filter.map(function(f) {
            return f.field === field ? __assign2(__assign2({}, existing), { operator }) : f;
          }) : __spreadArray(__spreadArray([], filter, true), [__assign2(__assign2({}, settings5.emptyFilter), { field, operator })], false);
          _this.filterChange({
            filter: filterResult,
            field,
            syntheticEvent: e.syntheticEvent
          });
        };
        _this.clearButtonClick = function(e) {
          var _a4 = _this.props, _b2 = _a4.filter, filter = _b2 === void 0 ? [] : _b2, _c2 = _a4.field, field = _c2 === void 0 ? "" : _c2;
          _this.filterChange({
            filter: filter.filter(function(f) {
              return f.field !== field;
            }),
            field,
            syntheticEvent: e
          });
        };
        _this.findCellFilter = function() {
          var _a4 = _this.props, _b2 = _a4.filter, filter = _b2 === void 0 ? [] : _b2, field = _a4.field;
          return filter.find(function(f) {
            return f.field === field;
          });
        };
        return _this;
      }
      FilterComponent.prototype.render = function() {
        var _a4;
        var _b2 = this.props, _c2 = _b2.clearButtonTitle, clearButtonTitle = _c2 === void 0 ? "" : _c2, _d = _b2.operators, operators2 = _d === void 0 ? [] : _d;
        var filter = this.findCellFilter() || __assign2(__assign2({}, settings5.emptyFilter), { operator: "" });
        var operator = filter.operator, value = filter.value;
        var operatorComponentProps = {
          ariaLabel: "filter",
          value: operators2.find(function(item) {
            return item.operator === operator;
          }) || null,
          onChange: this.operatorChange,
          className: "k-dropdown-operator",
          iconClassName: "k-i-filter",
          svgIcon: filterIcon,
          data: operators2,
          textField: "text",
          popupSettings: { width: "" },
          components: this.props.components
        };
        return React5.createElement(
          "div",
          { className: "k-filtercell" },
          React5.createElement(
            "span",
            null,
            settings5.inputComponent({
              value,
              onChange: this.inputChange,
              components: this.props.components,
              ariaLabel: this.props.ariaLabel,
              min: this.props.min,
              max: this.props.max
            }),
            React5.createElement(
              "div",
              { className: "k-filtercell-operator" },
              settings5.operatorComponent(operatorComponentProps, this.props),
              " ",
              React5.createElement(Button, { className: classNames((_a4 = {}, _a4["k-clear-button-visible"] = !(value === null || value === "") || operator, _a4)), title: clearButtonTitle, type: "button", onClick: this.clearButtonClick, icon: "filter-clear", svgIcon: filterClearIcon, disabled: !(!(value === null || value === "") || operator) })
            )
          )
        );
      };
      return FilterComponent;
    }(React5.Component)
  );
  return Cell;
};

// node_modules/@progress/kendo-react-data-tools/dist/es/filteringCells/BooleanFilter.js
var React6 = __toESM(require_react());
var __extends6 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign3 = function() {
  __assign3 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign3.apply(this, arguments);
};
var __spreadArray2 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var booleanOperator = "eq";
var settings = {
  emptyFilter: { operator: booleanOperator, value: "" },
  inputComponent: function(_props) {
    return null;
  },
  operatorComponent: function(props, cellProps) {
    var _a4 = cellProps.filter, filter = _a4 === void 0 ? [] : _a4, field = cellProps.field;
    var current = filter.find(function(f) {
      return f.field === field;
    });
    return React6.createElement(DropDownList, __assign3({}, props, { value: props.data.find(function(x) {
      return x.operator === (current ? current.value : "");
    }), onChange: function(e) {
      if (!cellProps.onFilterChange) {
        return;
      }
      var value = e.target.value.operator;
      if (value === "") {
        if (current) {
          cellProps.onFilterChange.call(void 0, {
            filter: filter.filter(function(f) {
              return f.field !== current.field;
            }),
            field,
            syntheticEvent: e.syntheticEvent
          });
        }
        return;
      }
      var filterResult = current ? filter.map(function(f) {
        return f.field === field ? __assign3(__assign3({}, current), { value }) : f;
      }) : __spreadArray2(__spreadArray2([], filter, true), [{ value, field, operator: booleanOperator }], false);
      cellProps.onFilterChange.call(void 0, {
        filter: filterResult,
        field,
        syntheticEvent: e.syntheticEvent
      });
    } }));
  }
};
var BooleanFilterCell = (
  /** @class */
  function(_super) {
    __extends6(BooleanFilterCell2, _super);
    function BooleanFilterCell2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    return BooleanFilterCell2;
  }(createFilterComponent(settings))
);

// node_modules/@progress/kendo-react-data-tools/dist/es/filteringCells/DateFilter.js
var React7 = __toESM(require_react());
var __extends7 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign4 = function() {
  __assign4 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign4.apply(this, arguments);
};
var settings2 = {
  inputComponent: function(props) {
    return React7.createElement(DatePicker, __assign4({}, props));
  },
  operatorComponent: function(props) {
    return React7.createElement(DropDownList, __assign4({}, props));
  },
  emptyFilter: { operator: "eq", value: null }
};
var DateFilterCell = (
  /** @class */
  function(_super) {
    __extends7(DateFilterCell2, _super);
    function DateFilterCell2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    return DateFilterCell2;
  }(createFilterComponent(settings2))
);

// node_modules/@progress/kendo-react-data-tools/dist/es/filteringCells/NumericFilter.js
var React8 = __toESM(require_react());
var __extends8 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign5 = function() {
  __assign5 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign5.apply(this, arguments);
};
var settings3 = {
  inputComponent: function(props) {
    return React8.createElement(NumericTextBox, __assign5({}, props));
  },
  operatorComponent: function(props) {
    return React8.createElement(DropDownList, __assign5({}, props));
  },
  emptyFilter: { operator: "eq", value: null }
};
var NumericFilterCell = (
  /** @class */
  function(_super) {
    __extends8(NumericFilterCell2, _super);
    function NumericFilterCell2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    return NumericFilterCell2;
  }(createFilterComponent(settings3))
);

// node_modules/@progress/kendo-react-data-tools/dist/es/filteringCells/TextFilter.js
var React9 = __toESM(require_react());
var __extends9 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign6 = function() {
  __assign6 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign6.apply(this, arguments);
};
var settings4 = {
  inputComponent: function(props) {
    return React9.createElement(Input, __assign6({}, props));
  },
  operatorComponent: function(props) {
    return React9.createElement(DropDownList, __assign6({}, props));
  },
  emptyFilter: { operator: "contains", value: "" }
};
var TextFilterCell = (
  /** @class */
  function(_super) {
    __extends9(TextFilterCell2, _super);
    function TextFilterCell2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    return TextFilterCell2;
  }(createFilterComponent(settings4))
);

// node_modules/@progress/kendo-react-data-tools/dist/es/header/FilterRow.js
var React13 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/header/HeaderThElement.js
var React12 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/navigation/hooks.js
var React11 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/navigation/constants.js
var __spreadArray3 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var _a2;
var _b;
var _c;
var KEYBOARD_NAV_DATA_LEVEL = "data-keyboardnavlevel";
var KEYBOARD_NAV_DATA_SCOPE = "data-keyboardnavscope";
var KEYBOARD_NAV_DATA_HEADER = "data-keyboardnavheader";
var KEYBOARD_NAV_DATA_BODY = "data-keyboardnavbody";
var KEYBOARD_NAV_DATA_ID = "data-keyboardnavid";
var KEYBOARD_NAV_FILTER_COL_SUFFIX = "_filter";
var FOCUSABLE_ELEMENTS_BASE = [
  "input:not([disabled]):not([type=hidden])",
  "select:not([disabled])",
  "textarea:not([disabled])",
  "button:not([disabled])",
  "a[href]",
  "area[href]",
  "summary",
  "iframe",
  "object",
  "embed",
  "audio[controls]",
  "video[controls]",
  "[contenteditable]"
];
var FOCUSABLE_ELEMENTS = __spreadArray3(__spreadArray3([], FOCUSABLE_ELEMENTS_BASE, true), ["[tabindex]"], false);
var TABBABLE_ELEMENTS = __spreadArray3(__spreadArray3([], FOCUSABLE_ELEMENTS_BASE, true), ["[tabindex]"], false).map(function(selector) {
  return selector + ':not([tabindex="-1"])';
});
var tableKeyboardNavigationScopeAttributes = (_a2 = {}, _a2[KEYBOARD_NAV_DATA_SCOPE] = true, _a2);
var tableKeyboardNavigationHeaderAttributes = (_b = {}, _b[KEYBOARD_NAV_DATA_HEADER] = true, _b);
var tableKeyboardNavigationBodyAttributes = (_c = {}, _c[KEYBOARD_NAV_DATA_BODY] = true, _c);

// node_modules/@progress/kendo-react-data-tools/dist/es/navigation/TableKeyboardNavigationContext.js
var React10 = __toESM(require_react());
var TableKeyboardNavigationContext = React10.createContext(void 0);
TableKeyboardNavigationContext.displayName = "KendoReactTableKeyboardNavigationContext";

// node_modules/@progress/kendo-react-data-tools/dist/es/navigation/hooks.js
var useTableKeyboardNavigation = function(elementId, navigatable) {
  var _a4;
  var navigationContext = React11.useContext(TableKeyboardNavigationContext);
  if (!navigationContext || !elementId || navigatable === false) {
    return {};
  }
  return _a4 = {
    tabIndex: navigationContext && navigationContext.activeId && navigationContext.activeId === elementId ? 0 : -1
  }, _a4[KEYBOARD_NAV_DATA_LEVEL] = navigationContext.level, _a4[KEYBOARD_NAV_DATA_ID] = elementId, _a4;
};

// node_modules/@progress/kendo-react-data-tools/dist/es/header/HeaderThElement.js
var __assign7 = function() {
  __assign7 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign7.apply(this, arguments);
};
var HeaderThElement = function(props) {
  var ariaSort = props.ariaSort, colSpan = props.colSpan, rowSpan = props.rowSpan, className = props.className, style = props.style, columnId = props.columnId, onKeyDown2 = props.onKeyDown, navigatable = props.navigatable, ariaColumnIndex = props.ariaColumnIndex, ariaLabel = props.ariaLabel, role = props.role, ariaSelected = props.ariaSelected;
  var navigationAttributes = useTableKeyboardNavigation(columnId, navigatable);
  return React12.createElement("th", __assign7({
    "aria-sort": ariaSort,
    "aria-label": ariaLabel,
    "aria-colindex": ariaColumnIndex,
    "aria-selected": ariaSelected,
    // TODO: Enable this with React 18. Currently throws warnings
    // aria-description={ariaDescription}
    colSpan,
    rowSpan,
    className,
    style,
    onKeyDown: onKeyDown2,
    role
  }, navigationAttributes), props.children);
};
HeaderThElement.displayName = "KendoReactHeaderThElement";

// node_modules/@progress/kendo-react-data-tools/dist/es/navigation/utils.js
var generateNavigatableId = function(navigationId, idPrefix, type) {
  if (type === void 0) {
    type = "cell";
  }
  return "".concat(idPrefix, "_").concat(navigationId, "_").concat(type);
};
var getNavigatableLevel = function(element) {
  if (!element) {
    return;
  }
  return parseInt(element.getAttribute(KEYBOARD_NAV_DATA_LEVEL) || "", 10);
};
var getNavigatableId = function(element) {
  if (!element) {
    return;
  }
  var dataId = element.getAttribute(KEYBOARD_NAV_DATA_ID);
  return dataId ? dataId : void 0;
};
var isNavigatable = function(element) {
  if (!element) {
    return false;
  }
  var dataId = element.getAttribute(KEYBOARD_NAV_DATA_ID);
  return Boolean(dataId);
};
var getNavigatableElement = function(scope, options) {
  if (options === void 0) {
    options = { level: 0 };
  }
  return scope.querySelector("[".concat(KEYBOARD_NAV_DATA_LEVEL, "='").concat(options.level, "']"));
};
var getClosestNavigatableElement = function(target) {
  return target.parentElement && target.parentElement.closest("[".concat(KEYBOARD_NAV_DATA_LEVEL, "]"));
};
var getActiveElement = function(scope, activeId) {
  return scope.querySelector("[".concat(KEYBOARD_NAV_DATA_ID, "='").concat(activeId, "']"));
};
var getClosestScope = function(target) {
  return target.parentElement && target.parentElement.closest("[".concat(KEYBOARD_NAV_DATA_SCOPE, "]"));
};
var getHeaderElement = function(scope) {
  return scope.querySelector("[".concat(KEYBOARD_NAV_DATA_HEADER, "]"));
};
var getBodyElement = function(scope) {
  return scope.querySelector("[".concat(KEYBOARD_NAV_DATA_BODY, "]"));
};
var getFocusableElements = function(scope, options) {
  if (options === void 0) {
    options = { focusable: false };
  }
  var selectors = options.focusable ? FOCUSABLE_ELEMENTS : TABBABLE_ELEMENTS;
  return Array.from(scope.querySelectorAll(selectors.join(",")));
};
var getNavigatableElements = function(scope, options) {
  if (options === void 0) {
    options = { level: 0 };
  }
  if (!scope) {
    return [];
  }
  var selector = FOCUSABLE_ELEMENTS.map(function(el) {
    return el + "[".concat(KEYBOARD_NAV_DATA_LEVEL, "='").concat(options.level, "']");
  }).join(",");
  return Array.from(scope.querySelectorAll(selector));
};
var filterNavigatableElements = function(options) {
  if (options === void 0) {
    options = { level: 0 };
  }
  var selector = FOCUSABLE_ELEMENTS.map(function(sel) {
    return sel + "[".concat(KEYBOARD_NAV_DATA_LEVEL, "='").concat(options.level, "']");
  }).join(",");
  return function(element) {
    return element.matches(selector);
  };
};
var focusElement = function(options) {
  var elementForFocus = options.elementForFocus, event = options.event, contextStateRef = options.contextStateRef, prevElement = options.prevElement;
  if (contextStateRef.current && elementForFocus && elementForFocus.focus) {
    event.preventDefault();
    elementForFocus.focus();
    if (isNavigatable(elementForFocus)) {
      elementForFocus.setAttribute("tabIndex", "0");
      contextStateRef.current.activeId = getNavigatableId(elementForFocus);
    }
    if (prevElement && isNavigatable(prevElement)) {
      prevElement.setAttribute("tabIndex", "-1");
    }
  }
};
var getIdPrefix = function(navigationStateRef) {
  return navigationStateRef.current ? navigationStateRef.current.idPrefix : "";
};
var findNextIdByRowIndex = function(initialRowIndex, cellIndex, elementId, matrix, isReverse) {
  if (!elementId) {
    return [];
  }
  var currentRowIndex = initialRowIndex + (isReverse ? -1 : 1);
  while (currentRowIndex >= 0 && currentRowIndex < matrix.length) {
    var currentId = matrix[currentRowIndex][cellIndex];
    if (currentId !== elementId) {
      return [currentId, [currentRowIndex, cellIndex]];
    }
    currentRowIndex = currentRowIndex + (isReverse ? -1 : 1);
  }
  return [];
};
var findNextIdByCellIndex = function(rowIndex, initialCellIndex, elementId, matrix, isReverse) {
  if (!elementId) {
    return [];
  }
  var currentCellIndex = initialCellIndex + (isReverse ? -1 : 1);
  while (currentCellIndex >= 0 && currentCellIndex < matrix[rowIndex].length) {
    var currentId = matrix[rowIndex][currentCellIndex];
    if (currentId !== elementId) {
      return [currentId, [rowIndex, currentCellIndex]];
    }
    currentCellIndex = currentCellIndex + (isReverse ? -1 : 1);
  }
  return [];
};
var findId = function(navigationMatrix, cellId) {
  if (!cellId) {
    return;
  }
  for (var rowIndex = 0; rowIndex < navigationMatrix.length; rowIndex++) {
    for (var cellIndex = 0; cellIndex < navigationMatrix[rowIndex].length; cellIndex++) {
      if (navigationMatrix[rowIndex][cellIndex] === cellId) {
        return [rowIndex, cellIndex];
      }
    }
  }
};
var getNextNavigationIndex = function(navigationStateRef) {
  return navigationStateRef.current ? navigationStateRef.current.navigationMatrix.length : 0;
};
var getFilterColumnId = function(columnId) {
  return columnId ? "".concat(columnId).concat(KEYBOARD_NAV_FILTER_COL_SUFFIX) : "";
};
var tableKeyboardNavigationTools = {
  generateNavigatableId,
  getNavigatableId,
  getNavigatableLevel,
  getNavigatableElement,
  getClosestNavigatableElement,
  getActiveElement,
  getClosestScope,
  getHeaderElement,
  getBodyElement,
  getFocusableElements,
  getNavigatableElements,
  filterNavigatableElements,
  focusElement,
  getIdPrefix,
  isNavigatable,
  findNextIdByRowIndex,
  findNextIdByCellIndex,
  findId,
  getNextNavigationIndex,
  getFilterColumnId
};

// node_modules/@progress/kendo-react-data-tools/dist/es/header/Header.js
var React14 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/header/utils/index.js
function nextColumn(columns, current) {
  var currentDepth = columns[current].depth;
  var next = null;
  for (var index = current + 1; index < columns.length; index++) {
    if (columns[index].depth === currentDepth) {
      next = columns[index];
      break;
    }
  }
  return next;
}
var getColumnWidth = function(column) {
  var width = column.width ? parseFloat(column.width.toString()) : 0;
  if (!width && column.children && column.children.length) {
    width = column.children.reduce(function(currentSum, child) {
      return getColumnWidth(child) + currentSum;
    }, 0);
  }
  return width;
};
function mapColumns(columns) {
  var columnsMap = [[]];
  var maxDepth = 0;
  for (var q = columns.length - 1; q >= 0; q--) {
    maxDepth = Math.max(maxDepth, columns[q].depth);
    columns[q].colSpan = columns[q].colSpan || 1;
    if (columns[q].parentIndex !== -1) {
      columns[columns[q].parentIndex].colSpan = (columns[columns[q].parentIndex].colSpan || 0) + columns[q].colSpan;
    }
  }
  var colIndexByLevel = [];
  var rowSpan = 1;
  columns.forEach(function(column2, i2) {
    columnsMap[column2.depth] = columnsMap[column2.depth] || [];
    var needKFirst = false;
    if (columnsMap[column2.depth].length === 0) {
      if (rowSpan <= 1) {
        rowSpan = 1 + (column2.children.length > 0 ? 0 : maxDepth - column2.depth);
      } else {
        rowSpan--;
        needKFirst = true;
      }
    }
    column2.rowSpan = 1 + (column2.children.length > 0 ? 0 : maxDepth - column2.depth);
    column2.kFirst = needKFirst;
    column2.index = columnsMap[column2.depth].length;
    columnsMap[column2.depth].push(i2);
    column2.ariaColumnIndex = colIndexByLevel[column2.depth] ? colIndexByLevel[column2.depth] + 1 : 1;
    for (var ci = column2.depth; ci < column2.depth + column2.rowSpan; ci++) {
      colIndexByLevel[ci] = (colIndexByLevel[ci] || 0) + column2.colSpan;
    }
  });
  var stickyLeftWidth = new Array(columnsMap.length).fill(0);
  var width = 0;
  columns.forEach(function(column2) {
    if (column2.locked) {
      column2.left = stickyLeftWidth[column2.depth];
      width = getColumnWidth(column2);
      if (column2.children.length === 0) {
        for (var i2 = column2.depth; i2 < stickyLeftWidth.length; i2++) {
          stickyLeftWidth[i2] += width;
        }
      } else {
        stickyLeftWidth[column2.depth] += width;
      }
    }
  });
  var stickyRightWidth = new Array(columnsMap.length).fill(0);
  for (var i = columns.length - 1; i >= 0; i--) {
    var column = columns[i];
    if (column.locked) {
      column.right = stickyRightWidth[column.depth];
      width = getColumnWidth(column);
      if (column.children.length === 0) {
        for (var j = column.depth; j < stickyRightWidth.length; j++) {
          stickyRightWidth[j] += width;
        }
      } else {
        stickyRightWidth[column.depth] += width;
      }
      var next = nextColumn(columns, i);
      column.rightBorder = !(next && next.locked);
    }
  }
  return columnsMap;
}
function isRtl(element) {
  return Boolean(element && getComputedStyle(element).direction === "rtl");
}
function getIndex(event, parent) {
  if (!parent || !event || !event.originalEvent) {
    return -1;
  }
  var coord = { x: event.clientX, y: event.originalEvent.clientY };
  var target = parent.ownerDocument ? parent.ownerDocument.elementFromPoint(coord.x, coord.y) : null;
  while (target && target.parentElement !== parent) {
    target = target.parentElement;
  }
  if (!target && parent.matches("tr")) {
    var predicate = function(c) {
      var pos = c.getBoundingClientRect();
      return coord.x > pos.left && coord.x < pos.left + pos.width && coord.y > pos.top && coord.y < pos.top + pos.height;
    };
    target = Array.from(parent.children).find(predicate) || null;
  }
  var children = parent.children;
  for (var i = 0; i < children.length; i++) {
    if (children[i] === target) {
      return i;
    }
  }
  return -1;
}

// node_modules/@progress/kendo-react-data-tools/dist/es/header/Header.js
var __extends10 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign8 = function() {
  __assign8 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign8.apply(this, arguments);
};
var Header = (
  /** @class */
  function(_super) {
    __extends10(Header3, _super);
    function Header3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.element = null;
      return _this;
    }
    Header3.prototype.componentDidMount = function() {
      var columnResize = this.props.columnResize;
      if (columnResize && columnResize.resizable) {
        columnResize.setIsRtl(isRtl(this.element));
      }
    };
    Header3.prototype.render = function() {
      var _this = this;
      return React14.createElement(
        "thead",
        __assign8({ className: "k-table-thead k-grid-header", role: "presentation", ref: function(el) {
          return _this.element = el;
        } }, tableKeyboardNavigationHeaderAttributes),
        this.props.headerRow,
        this.props.filterRow
      );
    };
    return Header3;
  }(React14.Component)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/header/HeaderCell.js
var React15 = __toESM(require_react());
var __assign9 = function() {
  __assign9 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign9.apply(this, arguments);
};
function HeaderCell(props) {
  var clickProps = props.onClick ? {
    onClick: props.onClick
  } : {};
  var defaultRendering = React15.createElement(
    "span",
    __assign9({ className: "k-link" }, clickProps),
    React15.createElement("span", { className: "k-column-title" }, props.title || props.field || " "),
    props.children
  );
  return React15.createElement(React15.Fragment, null, props.render ? props.render.call(void 0, defaultRendering, props) : defaultRendering);
}

// node_modules/@progress/kendo-react-data-tools/dist/es/header/HeaderRow.js
var React18 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/header/SortSettings.js
var __spreadArray4 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var normalize = function() {
  var settings5 = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    settings5[_i] = arguments[_i];
  }
  return Object.assign.apply(Object, __spreadArray4([{ allowUnsort: true, mode: "single" }], settings5, false));
};

// node_modules/@progress/kendo-react-data-tools/dist/es/drag/ColumnResizer.js
var React16 = __toESM(require_react());
var __extends11 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var ColumnResizer = (
  /** @class */
  function(_super) {
    __extends11(ColumnResizer3, _super);
    function ColumnResizer3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.draggable = null;
      _this.onDrag = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element) {
          _this.props.resize(data.event, element, false);
        }
      };
      _this.onRelease = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element) {
          _this.props.resize(data.event, element, true);
        }
      };
      return _this;
    }
    ColumnResizer3.prototype.render = function() {
      var _this = this;
      return React16.createElement(
        Draggable,
        { onDrag: this.onDrag, onRelease: this.onRelease, ref: function(component) {
          _this.draggable = component;
        } },
        React16.createElement("span", { className: "k-column-resizer", draggable: false })
      );
    };
    return ColumnResizer3;
  }(React16.PureComponent)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/drag/ColumnDraggable.js
var React17 = __toESM(require_react());
var __extends12 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var ColumnDraggable = (
  /** @class */
  function(_super) {
    __extends12(ColumnDraggable3, _super);
    function ColumnDraggable3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.draggable = null;
      _this.onPress = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.pressHandler) {
          _this.props.pressHandler(data.event, element);
        }
      };
      _this.onDrag = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.dragHandler) {
          _this.props.dragHandler(data.event, element);
        }
      };
      _this.onRelease = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.releaseHandler) {
          _this.props.releaseHandler(data.event);
        }
      };
      return _this;
    }
    ColumnDraggable3.prototype.render = function() {
      var _this = this;
      return React17.createElement(
        Draggable,
        { onPress: this.onPress, onDrag: this.onDrag, onRelease: this.onRelease, ref: function(component) {
          _this.draggable = component;
        } },
        React17.createElement("tr", { className: "k-table-row", "aria-rowindex": this.props.ariaRowIndex, role: "row" }, this.props.children)
      );
    };
    return ColumnDraggable3;
  }(React17.PureComponent)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/header/HeaderRow.js
var __extends13 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign10 = function() {
  __assign10 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign10.apply(this, arguments);
};
var sortSeqMap = {
  true: { "asc": "desc", "desc": "", "": "asc" },
  false: { "asc": "desc", "desc": "asc", "": "asc" }
};
var ariaSortMap = {
  "none": "none",
  "asc": "ascending",
  "desc": "descending"
};
var HeaderRow = (
  /** @class */
  function(_super) {
    __extends13(HeaderRow3, _super);
    function HeaderRow3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.cellClick = function(e, column) {
        e.preventDefault();
        if (!_this.props.sortChange) {
          return;
        }
        var _a4 = normalize(_this.props.sortable || false, column.sortable || false), allowUnsort = _a4.allowUnsort, mode = _a4.mode;
        var oldDescriptor = (_this.props.sort || []).filter(function(d) {
          return d.field === column.field;
        })[0];
        var dir = sortSeqMap[allowUnsort][oldDescriptor && oldDescriptor.dir || ""];
        var newDescriptor = mode === "single" ? [] : (_this.props.sort || []).filter(function(d) {
          return d.field !== column.field;
        });
        if (dir !== "" && column.field) {
          newDescriptor.push({ field: column.field, dir });
        }
        _this.props.sortChange(e, newDescriptor, column.field);
      };
      _this.cellKeyDown = function(event, column) {
        if (event.isDefaultPrevented()) {
          return;
        }
        if (event.keyCode === Keys.enter) {
          _this.cellClick(event, column);
        }
      };
      _this.cells = function(rowIndexes) {
        return rowIndexes.map(function(columnIndex, index) {
          var column = _this.props.columns[columnIndex];
          var columnResize = _this.props.columnResize;
          var resizable = columnResize && columnResize.resizable && column.resizable;
          var sortable = _this.props.sortable && column.sortable;
          var sortIndex2 = _this.props.sort ? _this.props.sort.findIndex(function(s) {
            return s.field === column.field;
          }) : -1;
          var sortDir = sortIndex2 >= 0 ? _this.props.sort[sortIndex2].dir || "none" : "none";
          var headerCellProps = {
            field: column.field,
            onClick: sortable && function(e) {
              return _this.cellClick(e, column);
            } || void 0,
            selectionChange: _this.props.selectionChange,
            title: column.title,
            selectionValue: column.headerSelectionValue,
            render: _this.props.cellRender,
            children: _this.sortIcon(sortIndex2)
          };
          var ColumnMenu = column.columnMenu || _this.props.columnMenu;
          var className = classNames("k-table-th", {
            "k-first": column.kFirst,
            "k-filterable": Boolean(ColumnMenu),
            "k-header": true,
            "k-grid-header-sticky": column.locked,
            "k-sorted": _this.props.sort && _this.props.sort.some(function(descriptor) {
              return descriptor.field === column.field;
            })
          }, column.headerClassName);
          var style = column.locked !== void 0 ? {
            left: column.left,
            right: column.right,
            borderRightWidth: column.rightBorder ? "1px" : ""
          } : {};
          var sortLabel = provideLocalizationService(_this).toLanguageString(sortAriaLabel, messages[sortAriaLabel]);
          var ariaAttrs = column.isAccessible ? {
            ariaSort: sortable ? ariaSortMap[sortDir] : void 0,
            role: "columnheader",
            ariaColumnIndex: column.ariaColumnIndex,
            ariaSelected: false,
            ariaDescription: sortable ? sortLabel : ""
          } : {
            role: "presentation"
          };
          var columnMenuProps = {
            sort: _this.props.sort,
            onSortChange: _this.props.sortChange,
            filter: _this.props.columnMenuFilter,
            onFilterChange: _this.props.columnMenuFilterChange,
            field: column.field
          };
          return React18.createElement(
            HeaderThElement,
            __assign10({ key: index, colSpan: column.colSpan, rowSpan: column.rowSpan, className, style, columnId: column.id, navigatable: column.navigatable, onKeyDown: sortable && function(e) {
              return _this.cellKeyDown(e, column);
            } || void 0 }, ariaAttrs),
            column.headerCell && React18.createElement(column.headerCell, __assign10({}, headerCellProps)),
            !column.headerCell && React18.createElement(
              "span",
              { className: "k-cell-inner" },
              React18.createElement(HeaderCell, __assign10({}, headerCellProps)),
              ColumnMenu && React18.createElement(ColumnMenu, __assign10({}, columnMenuProps))
            ),
            columnResize && resizable && React18.createElement(ColumnResizer, { key: "ColumnResizer", resize: function(e, element, end) {
              return columnResize.dragHandler(e, column, element, end);
            } })
          );
        });
      };
      return _this;
    }
    HeaderRow3.prototype.sortIcon = function(sortIndex2) {
      if (!this.props.sort || sortIndex2 < 0) {
        return null;
      }
      var dir = this.props.sort[sortIndex2].dir;
      if (!dir) {
        return null;
      }
      return [
        React18.createElement(IconWrap, { key: 1, name: "sort-".concat(dir, "-small"), icon: dir === "asc" ? sortAscSmallIcon : sortDescSmallIcon }),
        this.props.sort.length > 1 && React18.createElement("span", { key: 2, className: "k-sort-order" }, sortIndex2 + 1)
      ];
    };
    HeaderRow3.prototype.render = function() {
      var _this = this;
      return this.props.columnsMap.map(function(rowIndexes, i) {
        return _this.props.pressHandler && React18.createElement(ColumnDraggable, { key: i, pressHandler: _this.props.pressHandler, dragHandler: _this.props.dragHandler, releaseHandler: _this.props.releaseHandler, ariaRowIndex: i + 1 }, _this.cells(rowIndexes)) || React18.createElement("tr", { className: "k-table-row", "aria-rowindex": i + 1, role: "row" }, _this.cells(rowIndexes));
      });
    };
    return HeaderRow3;
  }(React18.Component)
);
registerForLocalization(HeaderRow);

// node_modules/@progress/kendo-react-data-tools/dist/es/header/HeaderSelectionCell.js
var React19 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/virtualization/columns.js
var __spreadArray5 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
function tableColumnsVirtualization(args) {
  var enabled = args.enabled, _a4 = args.columns, columns = _a4 === void 0 ? [] : _a4, tableViewPortWidth = args.tableViewPortWidth, scrollLeft = args.scrollLeft;
  var colSpans = columns.map(function() {
    return 1;
  });
  var hiddenColumns = columns.map(function() {
    return false;
  });
  if (!enabled) {
    return { colSpans, hiddenColumns };
  }
  var widths = columns.map(function(c) {
    return parseFloat((c.width || "").toString()) || 10;
  });
  var widthOfHiddenColumns = 0;
  for (var i = 0; i < columns.length; i++) {
    if (columns[i].locked) {
      continue;
    }
    var considerNext = i < widths.length - 1 ? widths[i + 1] : 0;
    if (widthOfHiddenColumns + widths[i] + considerNext < scrollLeft) {
      hiddenColumns[i] = true;
      widthOfHiddenColumns += widths[i];
    } else {
      break;
    }
  }
  var totalWidth = widths.reduce(function(acc, current) {
    return acc + current;
  }, 0);
  widthOfHiddenColumns = 0;
  for (var i = columns.length - 1; i >= 0; i--) {
    if (columns[i].locked) {
      continue;
    }
    if (widthOfHiddenColumns + 2 * widths[i] < totalWidth - tableViewPortWidth - scrollLeft) {
      hiddenColumns[i] = true;
      widthOfHiddenColumns += widths[i];
    } else {
      break;
    }
  }
  var hiddenCols = __spreadArray5([], hiddenColumns, true);
  var hidden = function(value) {
    return value;
  };
  var lastVisible = hiddenCols.lastIndexOf(false);
  var anyHidden = hiddenCols.some(hidden);
  var allHidden = hiddenCols.every(hidden);
  var hiddenSeqLength;
  var updateIndex;
  while (anyHidden && hiddenCols.length && (lastVisible !== -1 || allHidden)) {
    if (lastVisible < hiddenCols.length - 1) {
      hiddenSeqLength = allHidden ? hiddenCols.length : hiddenCols.length - lastVisible - 1;
      updateIndex = hiddenCols.length - hiddenSeqLength;
      if (updateIndex === 0) {
        updateIndex = hiddenSeqLength - 1;
      }
      hiddenColumns[updateIndex] = false;
      colSpans[updateIndex] = hiddenSeqLength;
      hiddenCols.splice(lastVisible + 1, hiddenSeqLength);
    }
    while (hiddenCols.length && !hiddenCols[hiddenCols.length - 1]) {
      hiddenCols.pop();
    }
    lastVisible = hiddenCols.lastIndexOf(false);
    anyHidden = hiddenCols.some(hidden);
    allHidden = hiddenCols.every(hidden);
  }
  return { colSpans, hiddenColumns };
}

// node_modules/@progress/kendo-react-data-tools/dist/es/drag/ColumnResize.js
var ColumnResize = (
  /** @class */
  function() {
    function ColumnResize3(triggerResize) {
      var _this = this;
      this.columns = [];
      this.resizable = false;
      this.isRtl = false;
      this.setIsRtl = function(isRtl2) {
        _this.isRtl = isRtl2;
      };
      this.onResize = triggerResize;
      this.dragHandler = this.dragHandler.bind(this);
    }
    ColumnResize3.prototype.dragHandler = function(event, column, dragCue, end) {
      var e = event.originalEvent;
      if (!end) {
        e.preventDefault();
        e.stopPropagation();
        e.stopImmediatePropagation();
      }
      var tdElement = dragCue.parentElement;
      if (!tdElement || !tdElement.parentElement) {
        return;
      }
      var oldWidth = tdElement.clientWidth;
      var newWidth;
      if (this.isRtl) {
        newWidth = dragCue.getBoundingClientRect().right - dragCue.offsetWidth / 2 - event.clientX;
        newWidth += oldWidth;
      } else {
        newWidth = oldWidth + event.clientX - dragCue.getBoundingClientRect().left - dragCue.offsetWidth / 2;
      }
      if (!end && Math.abs(newWidth - oldWidth) < 1) {
        return;
      }
      this.setWidths(column, Math.floor(newWidth) / oldWidth);
      var index = this.columns.filter(function(c) {
        return !c.children.length;
      }).indexOf(column);
      this.onResize(index, oldWidth, newWidth, e, end);
    };
    ColumnResize3.prototype.setWidths = function(column, coef) {
      var indexInOriginal = this.columns.indexOf(column);
      var toAdjust = [];
      var more = column.children.length;
      for (var i = indexInOriginal + 1; more > 0 && i < this.columns.length; i++, more--) {
        var cc = this.columns[i];
        if (!cc.children.length) {
          toAdjust.push(cc);
        } else {
          more += cc.children.length;
        }
      }
      if (toAdjust.length === 0) {
        toAdjust.push(column);
      }
      toAdjust.forEach(function(colToAdjust) {
        var targetWidth = colToAdjust.width ? parseFloat(colToAdjust.width.toString()) * coef : 0;
        var min = colToAdjust.minResizableWidth === void 0 ? 10 : colToAdjust.minResizableWidth;
        if (targetWidth < min) {
          targetWidth = min;
        }
        colToAdjust.width = Math.floor(targetWidth);
      });
      this.updateColElements(toAdjust);
    };
    ColumnResize3.prototype.updateColElements = function(affectedColumns) {
      var columns = this.columns.filter(function(c) {
        return !c.children.length;
      });
      var difference = 1e-10;
      for (var i = 0; i < affectedColumns.length; i++) {
        var colIndex = columns.indexOf(affectedColumns[i]);
        var currentColumnFloatWidth = parseFloat((affectedColumns[i].width || 0).toString());
        difference += currentColumnFloatWidth - Math.floor(currentColumnFloatWidth);
        var currentWidth = Math.floor(currentColumnFloatWidth) + Math.floor(difference);
        difference -= Math.floor(difference);
        this.setColWidth(this.colGroupMain && this.colGroupMain.children[colIndex], currentWidth);
        this.setColWidth(this.colGroupHeader && this.colGroupHeader.children[colIndex], currentWidth);
        this.setColWidth(this.colGroupFooter && this.colGroupFooter.children[colIndex], currentWidth);
      }
    };
    ColumnResize3.prototype.setColWidth = function(col, width) {
      if (col) {
        col.style.width = width + "px";
      }
    };
    return ColumnResize3;
  }()
);

// node_modules/@progress/kendo-react-data-tools/dist/es/drag/CommonDragLogic.js
var CommonDragLogic = (
  /** @class */
  function() {
    function CommonDragLogic3(columnReorder, groupReorder, columnToGroup) {
      var _this = this;
      this.columns = [];
      this.reorderable = false;
      this.groupable = false;
      this.startColumn = -1;
      this.startGroup = -1;
      this.currentColumn = -1;
      this.currentGroup = -1;
      this.dropElementClue = null;
      this.dragElementClue = null;
      this.groupPanelDivElement = null;
      this.refGroupPanelDiv = function(e) {
        _this.groupPanelDivElement = e;
      };
      this.refDropElementClue = function(e) {
        _this.dropElementClue = e;
      };
      this.refDragElementClue = function(e) {
        _this.dragElementClue = e;
      };
      this.pressHandler = function(event, element) {
        if (!event.isTouch) {
          event.originalEvent.preventDefault();
        }
        var startColumn = _this.getColumnIndex(event, element);
        _this.startGroup = _this.getGroupIndex(event);
        if (startColumn >= 0) {
          var col = _this.columns[startColumn];
          if (col.reorderable && _this.reorderable || col.groupable && _this.groupable) {
            _this.startColumn = startColumn;
          }
        }
      };
      this.dragHandler = function(event, element) {
        if (!event.isTouch) {
          event.originalEvent.preventDefault();
        }
        event.originalEvent.stopPropagation();
        if (_this.startColumn === -1 && _this.startGroup === -1) {
          return;
        }
        _this.currentColumn = _this.getColumnIndex(event, element);
        _this.currentGroup = _this.getGroupIndex(event);
        if (_this.groupPanelDivElement && _this.startGroup >= 0) {
          _this.currentGroup = Math.min(_this.currentGroup, _this.groupPanelDivElement.children.length - 2);
        }
        var invalidIndex = !_this.isValid();
        if (invalidIndex) {
          _this.currentColumn = -1;
          _this.currentGroup = -1;
        }
        var targetElement = _this.currentColumn >= 0 ? element.children[_this.columns[_this.currentColumn].index] : _this.groupPanelDivElement && _this.groupPanelDivElement.children[_this.currentGroup];
        _this.updateDragElementClue(event, element, targetElement, invalidIndex);
        _this.updateDropElementClue(event, element, targetElement, invalidIndex);
      };
      this.releaseHandler = function(event) {
        var prevColumnIndex = _this.startColumn;
        var nextColumnIndex = _this.currentColumn;
        var prevGroupIndex = _this.startGroup;
        var nextGroupIndex = _this.currentGroup;
        if (_this.dropElementClue) {
          _this.dropElementClue.setState({ visible: false });
        }
        if (_this.dragElementClue) {
          _this.dragElementClue.setState({ visible: false });
        }
        var isValid = _this.isValid();
        _this.startColumn = _this.startGroup = _this.currentColumn = _this.currentGroup = -1;
        if (!isValid) {
          return;
        }
        if (prevColumnIndex >= 0 && nextColumnIndex >= 0) {
          _this.columnReorder(prevColumnIndex, nextColumnIndex, event.originalEvent);
        } else if (prevGroupIndex >= 0 && nextGroupIndex >= 0) {
          _this.groupReorder(prevGroupIndex, nextGroupIndex, event.originalEvent);
        } else if (prevColumnIndex >= 0 && nextGroupIndex >= 0) {
          _this.columnToGroup(prevColumnIndex, nextGroupIndex, event.originalEvent);
        }
      };
      this.columnReorder = columnReorder;
      this.groupReorder = groupReorder;
      this.columnToGroup = columnToGroup;
    }
    CommonDragLogic3.prototype.getColumnIndex = function(event, parent) {
      if (!parent || parent.parentElement === this.groupPanelDivElement) {
        return -1;
      }
      var index = getIndex(event, parent);
      if (index === -1) {
        return -1;
      }
      var _loop_1 = function(i2) {
        if (parent.parentNode.children[i2] === parent) {
          return { value: this_1.columns.findIndex(function(c) {
            return c.index === index && c.depth === i2;
          }) };
        }
      };
      var this_1 = this;
      for (var i = 0; i < parent.parentNode.children.length; i++) {
        var state_1 = _loop_1(i);
        if (typeof state_1 === "object")
          return state_1.value;
      }
      return -1;
    };
    CommonDragLogic3.prototype.getGroupIndex = function(event) {
      return getIndex(event, this.groupPanelDivElement);
    };
    CommonDragLogic3.prototype.isValid = function() {
      if (this.startGroup >= 0) {
        return this.currentGroup >= 0 && this.currentGroup !== this.startGroup;
      }
      if (this.startColumn === -1) {
        return false;
      }
      if (this.currentGroup >= 0) {
        return this.columns[this.startColumn].groupable === true && this.groupable === true;
      }
      return this.reorderable === true && this.currentColumn >= 0 && this.currentColumn !== this.startColumn && this.columns[this.startColumn].reorderable === true && this.columns[this.currentColumn].parentIndex === this.columns[this.startColumn].parentIndex;
    };
    CommonDragLogic3.prototype.updateDragElementClue = function(event, element, targetElement, invalidIndex) {
      if (!this.dragElementClue) {
        return;
      }
      var innerText = this.startColumn >= 0 ? element.children[this.columns[this.startColumn].index].innerText : element.innerText;
      this.dragElementClue.setState({
        visible: true,
        top: event.pageY + 10,
        left: event.pageX,
        innerText,
        status: invalidIndex || !targetElement ? "k-i-cancel" : "k-i-plus"
      });
    };
    CommonDragLogic3.prototype.updateDropElementClue = function(event, element, targetElement, invalidIndex) {
      if (!this.dropElementClue) {
        return;
      }
      if (invalidIndex || !targetElement) {
        this.dropElementClue.setState({ visible: false });
        return;
      }
      var rect = targetElement.getBoundingClientRect();
      var left = rect.left + event.pageX - event.clientX - 6;
      if (this.currentColumn > this.startColumn || this.currentGroup > this.startGroup && this.startGroup !== -1) {
        left += rect.width;
      }
      var top = rect.top + event.pageY - event.clientY;
      this.dropElementClue.setState({
        visible: true,
        top,
        left,
        height: this.currentColumn >= 0 ? element.clientHeight : rect.height
      });
    };
    return CommonDragLogic3;
  }()
);

// node_modules/@progress/kendo-react-data-tools/dist/es/drag/DragClue.js
var React20 = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());
var __extends14 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var DragClue = (
  /** @class */
  function(_super) {
    __extends14(DragClue2, _super);
    function DragClue2(props) {
      var _this = _super.call(this, props) || this;
      _this.elementRef = React20.createRef();
      _this.state = {
        visible: false,
        top: 0,
        left: 0,
        innerText: "",
        status: "k-i-cancel"
      };
      _this.hiddenElementRef = React20.createRef();
      return _this;
    }
    Object.defineProperty(DragClue2.prototype, "element", {
      get: function() {
        return this.elementRef.current;
      },
      enumerable: false,
      configurable: true
    });
    DragClue2.prototype.render = function() {
      var hiddenElement = this.hiddenElementRef.current;
      var status = this.state.status;
      var dragClue = this.state.visible && canUseDOM && (0, import_react_dom.createPortal)(React20.createElement(
        "div",
        { ref: this.elementRef, className: "k-header k-drag-clue", style: {
          display: "block",
          position: "absolute",
          zIndex: 2e4,
          padding: "8px 12px",
          top: this.state.top + "px",
          left: this.state.left + "px"
        } },
        React20.createElement(IconWrap, { className: "k-drag-status k-icon-with-modifier", name: status, icon: status === "k-i-cancel" ? cancelIcon : plusIcon }),
        this.state.innerText
      ), hiddenElement && hiddenElement.ownerDocument ? hiddenElement.ownerDocument.body : document.body);
      return React20.createElement(
        React20.Fragment,
        null,
        dragClue,
        React20.createElement("div", { ref: this.hiddenElementRef, style: { display: "none" } })
      );
    };
    return DragClue2;
  }(React20.PureComponent)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/drag/DropClue.js
var React21 = __toESM(require_react());
var import_react_dom2 = __toESM(require_react_dom());
var __extends15 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var DropClue = (
  /** @class */
  function(_super) {
    __extends15(DropClue2, _super);
    function DropClue2(props) {
      var _this = _super.call(this, props) || this;
      _this.state = {
        height: 0,
        visible: false,
        left: 0,
        top: 0
      };
      _this.hiddenElementRef = React21.createRef();
      return _this;
    }
    DropClue2.prototype.render = function() {
      var hiddenElement = this.hiddenElementRef.current;
      var dropClue = this.state.visible && canUseDOM && (0, import_react_dom2.createPortal)(React21.createElement("div", { className: "k-grouping-dropclue", style: {
        zIndex: 1e4,
        display: "block",
        top: this.state.top + "px",
        left: this.state.left + "px",
        height: this.state.height + "px"
      } }), hiddenElement && hiddenElement.ownerDocument ? hiddenElement.ownerDocument.body : document.body);
      return React21.createElement(
        React21.Fragment,
        null,
        dropClue,
        React21.createElement("div", { ref: this.hiddenElementRef, style: { display: "none" } })
      );
    };
    return DropClue2;
  }(React21.PureComponent)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/filters/TextFilter.js
var React22 = __toESM(require_react());
var PropTypes2 = __toESM(require_prop_types());
var __extends16 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign11 = function() {
  __assign11 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign11.apply(this, arguments);
};
var TextFilter = (
  /** @class */
  function(_super) {
    __extends16(TextFilter2, _super);
    function TextFilter2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onChange = function(event) {
        _this.props.onFilterChange.call(void 0, {
          nextFilter: __assign11(__assign11({}, _this.props.filter), { value: event.value })
        });
      };
      return _this;
    }
    TextFilter2.prototype.render = function() {
      var locService = provideLocalizationService(this);
      var _a4 = this.props.ariaLabel, ariaLabel = _a4 === void 0 ? locService.toLanguageString(filterTextFilterAriaLabel, messages[filterTextFilterAriaLabel]) : _a4;
      return React22.createElement(Input, { value: this.props.filter.value || "", onChange: this.onChange, ariaLabel });
    };
    TextFilter2.propTypes = {
      filter: PropTypes2.object.isRequired,
      onFilterChange: PropTypes2.func.isRequired,
      ariaLabel: PropTypes2.string
    };
    return TextFilter2;
  }(React22.Component)
);
registerForLocalization(TextFilter);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/filters/NumericFilter.js
var React23 = __toESM(require_react());
var PropTypes3 = __toESM(require_prop_types());
var __extends17 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign12 = function() {
  __assign12 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign12.apply(this, arguments);
};
var NumericFilter = (
  /** @class */
  function(_super) {
    __extends17(NumericFilter2, _super);
    function NumericFilter2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onChange = function(event) {
        _this.props.onFilterChange.call(void 0, {
          nextFilter: __assign12(__assign12({}, _this.props.filter), { value: event.value })
        });
      };
      return _this;
    }
    NumericFilter2.prototype.render = function() {
      var locService = provideLocalizationService(this);
      var _a4 = this.props, min = _a4.min, max = _a4.max, filter = _a4.filter, _b2 = _a4.ariaLabel, ariaLabel = _b2 === void 0 ? locService.toLanguageString(filterNumericFilterAriaLabel, messages[filterNumericFilterAriaLabel]) : _b2;
      return React23.createElement(NumericTextBox, { value: typeof filter.value === "number" ? filter.value : null, onChange: this.onChange, ariaLabel, "aria-valuemin": min, "aria-valuemax": max, min, max });
    };
    NumericFilter2.propTypes = {
      filter: PropTypes3.object.isRequired,
      onFilterChange: PropTypes3.func.isRequired
    };
    return NumericFilter2;
  }(React23.Component)
);
registerForLocalization(NumericFilter);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/filters/DateFilter.js
var React24 = __toESM(require_react());
var PropTypes4 = __toESM(require_prop_types());
var __extends18 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign13 = function() {
  __assign13 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign13.apply(this, arguments);
};
var DateFilter = (
  /** @class */
  function(_super) {
    __extends18(DateFilter2, _super);
    function DateFilter2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onChange = function(event) {
        _this.props.onFilterChange.call(void 0, {
          nextFilter: __assign13(__assign13({}, _this.props.filter), { value: event.value })
        });
      };
      return _this;
    }
    DateFilter2.prototype.render = function() {
      var filter = this.props.filter;
      return React24.createElement(DatePicker, { value: filter.value || null, onChange: this.onChange, ariaLabel: this.props.ariaLabel });
    };
    DateFilter2.propTypes = {
      filter: PropTypes4.object.isRequired,
      onFilterChange: PropTypes4.func.isRequired,
      ariaLabel: PropTypes4.string
    };
    return DateFilter2;
  }(React24.Component)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/filters/BooleanFilter.js
var React26 = __toESM(require_react());
var PropTypes6 = __toESM(require_prop_types());

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/filters/EnumFilter.js
var React25 = __toESM(require_react());
var PropTypes5 = __toESM(require_prop_types());
var __extends19 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign14 = function() {
  __assign14 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign14.apply(this, arguments);
};
var EnumFilter = (
  /** @class */
  function(_super) {
    __extends19(EnumFilter2, _super);
    function EnumFilter2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onChange = function(event) {
        _this.props.onFilterChange.call(void 0, {
          nextFilter: __assign14(__assign14({}, _this.props.filter), { value: event.value.value })
        });
      };
      return _this;
    }
    EnumFilter2.prototype.render = function() {
      var locService = provideLocalizationService(this);
      var _a4 = this.props, filter = _a4.filter, _b2 = _a4.data, data = _b2 === void 0 ? [] : _b2, defaultItem = _a4.defaultItem, _c2 = _a4.ariaLabel, ariaLabel = _c2 === void 0 ? locService.toLanguageString(filterEnumFilterDropdownAriaLabel, messages[filterEnumFilterDropdownAriaLabel]) : _c2;
      var locData = data.map(function(item) {
        return __assign14(__assign14({}, item), { text: locService.toLanguageString(item.text, messages[item.text] || item.text) });
      });
      return React25.createElement(DropDownList, { value: locData.find(function(i) {
        return i.value === filter.value;
      }) || null, onChange: this.onChange, defaultItem, data: locData, textField: "text", ariaLabel });
    };
    EnumFilter2.propTypes = {
      filter: PropTypes5.object.isRequired,
      onFilterChange: PropTypes5.func.isRequired,
      data: PropTypes5.arrayOf(PropTypes5.shape({ text: PropTypes5.string, value: PropTypes5.any })),
      defaultItem: PropTypes5.any,
      ariaLabel: PropTypes5.string
    };
    return EnumFilter2;
  }(React25.Component)
);
registerForLocalization(EnumFilter);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/filters/BooleanFilter.js
var __extends20 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var BooleanFilter = (
  /** @class */
  function(_super) {
    __extends20(BooleanFilter2, _super);
    function BooleanFilter2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.data = [{ text: filterIsTrue, value: true }, { text: filterIsFalse, value: false }];
      return _this;
    }
    BooleanFilter2.prototype.render = function() {
      return React26.createElement(EnumFilter, { filter: this.props.filter, onFilterChange: this.props.onFilterChange, data: this.props.data || this.data, defaultItem: this.props.defaultItem, ariaLabel: this.props.ariaLabel });
    };
    BooleanFilter2.propTypes = {
      filter: PropTypes6.object.isRequired,
      onFilterChange: PropTypes6.func.isRequired,
      data: PropTypes6.arrayOf(PropTypes6.shape({ text: PropTypes6.string, value: PropTypes6.any })),
      ariaLabel: PropTypes6.string,
      defaultItem: PropTypes6.any
    };
    return BooleanFilter2;
  }(React26.Component)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/Expression.js
var React27 = __toESM(require_react());
var PropTypes7 = __toESM(require_prop_types());

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/operators.js
var __spreadArray6 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var textOperators = [
  { text: filterContainsOperator, operator: "contains" },
  { text: filterNotContainsOperator, operator: "doesnotcontain" },
  { text: filterEqOperator, operator: "eq" },
  { text: filterNotEqOperator, operator: "neq" },
  { text: filterStartsWithOperator, operator: "startswith" },
  { text: filterEndsWithOperator, operator: "endswith" },
  { text: filterIsNullOperator, operator: "isnull" },
  { text: filterIsNotNullOperator, operator: "isnotnull" },
  { text: filterIsEmptyOperator, operator: "isempty" },
  { text: filterIsNotEmptyOperator, operator: "isnotempty" }
];
var numericOperators = [
  { text: filterEqOperator, operator: "eq" },
  { text: filterNotEqOperator, operator: "neq" },
  { text: filterGteOperator, operator: "gte" },
  { text: filterGtOperator, operator: "gt" },
  { text: filterLteOperator, operator: "lte" },
  { text: filterLtOperator, operator: "lt" },
  { text: filterIsNullOperator, operator: "isnull" },
  { text: filterIsNotNullOperator, operator: "isnotnull" }
];
var dateOperators = [
  { text: filterEqOperator, operator: "eq" },
  { text: filterNotEqOperator, operator: "neq" },
  { text: filterAfterOrEqualOperator, operator: "gte" },
  { text: filterAfterOperator, operator: "gt" },
  { text: filterBeforeOperator, operator: "lt" },
  { text: filterBeforeOrEqualOperator, operator: "lte" },
  { text: filterIsNullOperator, operator: "isnull" },
  { text: filterIsNotNullOperator, operator: "isnotnull" }
];
var booleanOperators = [
  { text: filterEqOperator, operator: "eq" },
  { text: filterNotEqOperator, operator: "neq" }
];
var Operators = (
  /** @class */
  function() {
    function Operators2() {
    }
    Object.defineProperty(Operators2, "text", {
      /**
       * An array containing the operators for the TextFilter component.
       *
       * The operators are:
       *
       * - { text: 'filter.containsOperator', operator: 'contains' }
       * - { text: 'filter.notContainsOperator', operator: 'doesnotcontain' }
       * - { text: 'filter.eqOperator', operator: 'eq' }
       * - { text: 'filter.notEqOperator', operator: 'neq' }
       * - { text: 'filter.startsWithOperator', operator: 'startswith' }
       * - { text: 'filter.endsWithOperator', operator: 'endswith' }
       * - { text: 'filter.isNullOperator', operator: 'isnull' }
       * - { text: 'filter.isNotNullOperator', operator: 'isnotnull' }
       * - { text: 'filter.isEmptyOperator', operator: 'isempty' }
       * - { text: 'filter.isNotEmptyOperator', operator: 'isnotempty' }
       */
      get: function() {
        return __spreadArray6([], textOperators, true);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Operators2, "numeric", {
      /**
       * An array containing the operators for the NumericFilter component.
       *
       * The operators are:
       *
       * - { text: 'filter.eqOperator', operator: 'eq' }
       * - { text: 'filter.notEqOperator', operator: 'neq' }
       * - { text: 'filter.gteOperator', operator: 'gte' }
       * - { text: 'filter.gtOperator', operator: 'gt' }
       * - { text: 'filter.lteOperator', operator: 'lte' }
       * - { text: 'filter.ltOperator', operator: 'lt' }
       * - { text: 'filter.isNullOperator', operator: 'isnull' }
       * - { text: 'filter.isNotNullOperator', operator: 'isnotnull' }
       */
      get: function() {
        return __spreadArray6([], numericOperators, true);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Operators2, "date", {
      /**
       * An array containing the operators for the DateFilter component.
       *
       * The operators are:
       *
       * - { text: 'filter.eqOperator', operator: 'eq' }
       * - { text: 'filter.notEqOperator', operator: 'neq' }
       * - { text: 'filter.afterOrEqualOperator', operator: 'gte' }
       * - { text: 'filter.afterOperator', operator: 'gt' }
       * - { text: 'filter.beforeOperator', operator: 'lt' }
       * - { text: 'filter.beforeOrEqualOperator', operator: 'lte' }
       * - { text: 'filter.isNullOperator', operator: 'isnull' }
       * - { text: 'filter.isNotNullOperator', operator: 'isnotnull' }
       */
      get: function() {
        return __spreadArray6([], dateOperators, true);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Operators2, "boolean", {
      /**
       * An array containing the operators for the BooleanFilter component.
       *
       * The operators are:
       *
       * - { text: 'filter.eqOperator', operator: 'eq' }
       * - { text: 'filter.notEqOperator', operator: 'neq' }
       */
      get: function() {
        return __spreadArray6([], booleanOperators, true);
      },
      enumerable: false,
      configurable: true
    });
    return Operators2;
  }()
);
var stringOperator = function(operator) {
  return operator === "contains" || operator === "doesnotcontain" || operator === "startswith" || operator === "endswith";
};
var unaryOperator = function(operator) {
  return operator === "isnull" || operator === "isnotnull" || operator === "isempty" || operator === "isnotempty";
};

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/Expression.js
var __extends21 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign15 = function() {
  __assign15 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign15.apply(this, arguments);
};
var __rest = function(s, e) {
  var t = {};
  for (var p in s)
    if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
      t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var setInitialValue = function(filter) {
  if (filter.value === void 0 && !unaryOperator(filter.operator)) {
    filter.value = stringOperator(filter.operator) ? "" : null;
  }
};
var Expression = (
  /** @class */
  function(_super) {
    __extends21(Expression2, _super);
    function Expression2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onFieldChange = function(event) {
        var nextFieldName = event.value.name;
        var nextField = _this.props.fields.find(function(f) {
          return f.name === nextFieldName;
        });
        var prevFilter = _this.props.filter;
        var operator = nextField && !nextField.operators.some(function(o) {
          return o.operator === prevFilter.operator;
        }) ? nextField.operators[0].operator : prevFilter.operator;
        var nextFilter = {
          field: nextFieldName,
          operator
        };
        setInitialValue(nextFilter);
        _this.triggerOnFilterChange(prevFilter, nextFilter, event);
      };
      _this.onOperatorChange = function(event) {
        var operator = event.value.operator;
        var prevFilter = _this.props.filter;
        var nextFilter;
        if (unaryOperator(operator)) {
          var value = prevFilter.value, prevFilterNoValue = __rest(prevFilter, ["value"]);
          nextFilter = __assign15(__assign15({}, prevFilterNoValue), { operator });
        } else {
          nextFilter = __assign15(__assign15({}, prevFilter), { operator });
        }
        setInitialValue(nextFilter);
        _this.triggerOnFilterChange(prevFilter, nextFilter, event);
      };
      _this.onInputChange = function(event) {
        var prevFilter = _this.props.filter;
        var nextFilter = event.nextFilter;
        if (unaryOperator(nextFilter.operator)) {
          var field = _this.props.fields.find(function(f) {
            return f.name === nextFilter.field;
          });
          nextFilter = __assign15(__assign15({}, nextFilter), { operator: field && field.operators[0].operator || nextFilter.operator });
        }
        _this.triggerOnFilterChange(prevFilter, nextFilter, event);
      };
      _this.triggerOnFilterChange = function(prevFilter, nextFilter, event) {
        var changeEvent = {
          prevFilter,
          nextFilter,
          syntheticEvent: event.syntheticEvent,
          nativeEvent: event.nativeEvent,
          target: _this
        };
        _this.props.onChange.call(void 0, changeEvent);
      };
      _this.onFilterRemove = function(event) {
        var removeEvent = {
          filter: _this.props.filter,
          syntheticEvent: event.syntheticEvent,
          nativeEvent: event.nativeEvent,
          target: _this
        };
        _this.props.onRemove.call(void 0, removeEvent);
      };
      return _this;
    }
    Expression2.prototype.render = function() {
      var locService = provideLocalizationService(this);
      var _a4 = this.props, fields = _a4.fields, filter = _a4.filter, _b2 = _a4.ariaLabel, ariaLabel = _b2 === void 0 ? locService.toLanguageString(filterExpressionAriaLabel, messages[filterExpressionAriaLabel]) : _b2;
      var field = fields.find(function(f) {
        return f.name === filter.field;
      });
      var operators2 = (field && field.operators || []).map(function(o) {
        return __assign15(__assign15({}, o), { text: locService.toLanguageString(o.text, messages[o.text] || o.text) });
      });
      return React27.createElement(
        "div",
        { className: "k-filter-toolbar" },
        React27.createElement(
          Toolbar,
          { keyboardNavigation: false, role: "toolbar", ariaLabel },
          React27.createElement(
            ToolbarItem,
            { className: "k-filter-field" },
            React27.createElement(DropDownList, { className: "k-filter-dropdown", data: fields, textField: "label", value: fields.find(function(f) {
              return f.name === filter.field;
            }), onChange: this.onFieldChange, ariaLabel: locService.toLanguageString(filterExpressionDropdownAriaLabel, messages[filterExpressionDropdownAriaLabel]) })
          ),
          React27.createElement(
            ToolbarItem,
            { className: "k-filter-operator" },
            React27.createElement(DropDownList, { data: operators2, textField: "text", value: operators2.find(function(o) {
              return o.operator === filter.operator;
            }), onChange: this.onOperatorChange, ariaLabel: locService.toLanguageString(filterExpressionOperatorDropdownAriaLabel, messages[filterExpressionOperatorDropdownAriaLabel]) })
          ),
          React27.createElement(ToolbarItem, { className: "k-filter-value" }, field && React27.createElement(field.filter, { filter, onFilterChange: this.onInputChange, min: field.min, max: field.max })),
          React27.createElement(
            ToolbarItem,
            null,
            React27.createElement(Button, { title: locService.toLanguageString(filterClose, messages[filterClose]), icon: "x", svgIcon: xIcon, fillMode: "flat", type: "button", onClick: this.onFilterRemove })
          )
        )
      );
    };
    Expression2.propTypes = {
      filter: PropTypes7.object.isRequired,
      fields: PropTypes7.array.isRequired,
      ariaLabel: PropTypes7.string,
      onChange: PropTypes7.func.isRequired,
      onRemove: PropTypes7.func.isRequired
    };
    return Expression2;
  }(React27.Component)
);
registerForLocalization(Expression);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/Filter.js
var React29 = __toESM(require_react());
var PropTypes9 = __toESM(require_prop_types());

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/Group.js
var React28 = __toESM(require_react());
var PropTypes8 = __toESM(require_prop_types());
var __extends22 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign16 = function() {
  __assign16 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign16.apply(this, arguments);
};
var __spreadArray7 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var Group = (
  /** @class */
  function(_super) {
    __extends22(Group2, _super);
    function Group2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onGroupRemove = function(event) {
        var ev = {
          filter: _this.props.filter,
          target: _this,
          syntheticEvent: event,
          nativeEvent: event.nativeEvent
        };
        _this.props.onRemove.call(void 0, ev);
      };
      _this.onAddExpression = function(event) {
        var prevFilter = _this.props.filter;
        var firstField = _this.props.fields[0];
        var newExpression = { field: firstField.name, operator: firstField.operators[0].operator };
        newExpression.value = stringOperator(newExpression.operator) ? "" : null;
        var ev = {
          nextFilter: __assign16(__assign16({}, prevFilter), { filters: __spreadArray7(__spreadArray7([], prevFilter.filters, true), [newExpression], false) }),
          prevFilter,
          target: _this,
          syntheticEvent: event,
          nativeEvent: event.nativeEvent
        };
        _this.props.onChange.call(void 0, ev);
      };
      _this.onAddGroup = function(event) {
        var prevFilter = _this.props.filter;
        var ev = {
          nextFilter: __assign16(__assign16({}, prevFilter), { filters: __spreadArray7(__spreadArray7([], prevFilter.filters, true), [__assign16({}, _this.props.defaultGroupFilter)], false) }),
          prevFilter,
          target: _this,
          syntheticEvent: event,
          nativeEvent: event.nativeEvent
        };
        _this.props.onChange.call(void 0, ev);
      };
      _this.onLogicAnd = function(event) {
        return _this.changeLogic("and", event);
      };
      _this.onLogicOr = function(event) {
        return _this.changeLogic("or", event);
      };
      _this.changeLogic = function(logic, event) {
        var prevFilter = _this.props.filter;
        if (prevFilter.logic !== logic) {
          var ev = {
            nextFilter: __assign16(__assign16({}, prevFilter), { logic }),
            prevFilter,
            target: _this,
            syntheticEvent: event,
            nativeEvent: event.nativeEvent
          };
          _this.props.onChange.call(void 0, ev);
        }
      };
      _this.replaceFilter = function(prevFilter, nextFilter) {
        var filter = _this.props.filter;
        var filters = filter.filters.map(function(f) {
          return f === prevFilter ? nextFilter : f;
        });
        return __assign16(__assign16({}, filter), { filters });
      };
      _this.onChange = function(event) {
        var nextFilter = _this.replaceFilter(event.prevFilter, event.nextFilter);
        var changeEvent = {
          nextFilter,
          prevFilter: _this.props.filter,
          syntheticEvent: event.syntheticEvent,
          nativeEvent: event.nativeEvent,
          target: _this
        };
        _this.props.onChange.call(void 0, changeEvent);
      };
      _this.onRemove = function(event) {
        var prevFilter = _this.props.filter;
        var filters = prevFilter.filters.filter(function(f) {
          return f !== event.filter;
        });
        var changeEvent = {
          nextFilter: __assign16(__assign16({}, prevFilter), { filters }),
          prevFilter,
          syntheticEvent: event.syntheticEvent,
          nativeEvent: event.nativeEvent,
          target: _this
        };
        _this.props.onChange.call(void 0, changeEvent);
      };
      return _this;
    }
    Group2.prototype.render = function() {
      var _this = this;
      var locService = provideLocalizationService(this);
      var _a4 = this.props, fields = _a4.fields, filter = _a4.filter, _b2 = _a4.ariaLabel, ariaLabel = _b2 === void 0 ? locService.toLanguageString(filterGroupAriaLabel, messages[filterGroupAriaLabel]) : _b2;
      return React28.createElement(
        React28.Fragment,
        null,
        React28.createElement(
          "div",
          { className: "k-filter-toolbar" },
          React28.createElement(
            Toolbar,
            { keyboardNavigation: false, role: "toolbar", ariaLabel },
            React28.createElement(
              ToolbarItem,
              null,
              React28.createElement(
                ButtonGroup,
                null,
                React28.createElement(Button, { togglable: true, onClick: this.onLogicAnd, selected: filter.logic === "and", type: "button" }, locService.toLanguageString(filterAndLogic, messages[filterAndLogic])),
                React28.createElement(Button, { togglable: true, onClick: this.onLogicOr, selected: filter.logic === "or", type: "button" }, locService.toLanguageString(filterOrLogic, messages[filterOrLogic]))
              )
            ),
            React28.createElement(
              ToolbarItem,
              null,
              React28.createElement(Button, { title: locService.toLanguageString(filterAddExpression, messages[filterAddExpression]), icon: "filter-add-expression", svgIcon: filterAddExpressionIcon, type: "button", onClick: this.onAddExpression })
            ),
            React28.createElement(
              ToolbarItem,
              null,
              React28.createElement(Button, { title: locService.toLanguageString(filterAddGroup, messages[filterAddGroup]), icon: "filter-add-group", svgIcon: filterAddGroupIcon, type: "button", onClick: this.onAddGroup })
            ),
            React28.createElement(
              ToolbarItem,
              null,
              React28.createElement(Button, { title: locService.toLanguageString(filterClose, messages[filterClose]), icon: "x", svgIcon: xIcon, fillMode: "flat", type: "button", onClick: this.onGroupRemove })
            )
          )
        ),
        filter.filters.length > 0 && React28.createElement("ul", { role: "group", className: "k-filter-lines" }, filter.filters.map(function(f, idx) {
          return React28.createElement("li", { role: "treeitem", key: idx, className: "k-filter-item" }, isCompositeFilterDescriptor(f) ? React28.createElement(Group2, { filter: f, fields, onChange: _this.onChange, onRemove: _this.onRemove, defaultGroupFilter: _this.props.defaultGroupFilter, ariaLabel: _this.props.ariaLabel, ariaLabelExpression: _this.props.ariaLabelExpression }) : React28.createElement(Expression, { filter: f, fields, ariaLabel: _this.props.ariaLabelExpression, onChange: _this.onChange, onRemove: _this.onRemove }));
        }))
      );
    };
    Group2.propTypes = {
      filter: PropTypes8.object.isRequired,
      fields: PropTypes8.array.isRequired,
      ariaLabel: PropTypes8.string,
      ariaLabelExpression: PropTypes8.string,
      onChange: PropTypes8.func.isRequired,
      onRemove: PropTypes8.func.isRequired,
      defaultGroupFilter: PropTypes8.object.isRequired
    };
    return Group2;
  }(React28.Component)
);
registerForLocalization(Group);

// node_modules/@progress/kendo-react-data-tools/dist/es/filter/Filter.js
var __extends23 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign17 = function() {
  __assign17 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign17.apply(this, arguments);
};
var Filter = (
  /** @class */
  function(_super) {
    __extends23(Filter2, _super);
    function Filter2(props) {
      var _this = _super.call(this, props) || this;
      _this.onFilterChange = function(event) {
        var changeEvent = {
          filter: event.nextFilter,
          syntheticEvent: event.syntheticEvent,
          nativeEvent: event.nativeEvent,
          target: _this
        };
        _this.props.onChange.call(void 0, changeEvent);
      };
      _this.onGroupRemove = function(event) {
        var changeEvent = {
          filter: __assign17(__assign17({}, _this.props.value), { filters: [] }),
          syntheticEvent: event.syntheticEvent,
          nativeEvent: event.nativeEvent,
          target: _this
        };
        _this.props.onChange.call(void 0, changeEvent);
      };
      validatePackage(packageMetadata);
      return _this;
    }
    Filter2.prototype.render = function() {
      return React29.createElement(
        "div",
        { className: "k-widget k-filter" + (this.props.className ? " " + this.props.className : ""), style: this.props.style },
        React29.createElement(
          "ul",
          { role: "tree", className: "k-filter-container", "aria-label": this.props.ariaLabel },
          React29.createElement(
            "li",
            { role: "treeitem", className: "k-filter-group-main" },
            React29.createElement(Group, { filter: this.props.value, fields: this.props.fields, ariaLabel: this.props.ariaLabelGroup, ariaLabelExpression: this.props.ariaLabelExpression, onChange: this.onFilterChange, onRemove: this.onGroupRemove, defaultGroupFilter: this.props.defaultGroupFilter || { logic: "and", filters: [] } })
          )
        )
      );
    };
    Filter2.propTypes = {
      className: PropTypes9.string,
      style: PropTypes9.object,
      fields: function(props, propName) {
        var prop = props[propName];
        if (prop === void 0) {
          return new Error("Property '".concat(propName, "' is missing."));
        } else if (!Array.isArray(prop)) {
          return new Error("Property '".concat(propName, "' needs to be Array<FieldSettings>."));
        } else if (Object.keys(prop.reduce(function(acc, cur) {
          var _a4;
          return __assign17(__assign17({}, acc), (_a4 = {}, _a4[cur.name] = 1, _a4));
        }, {})).length !== prop.length) {
          return new Error("Property '".concat(propName, "' needs to contain objects with unique 'name' field."));
        }
        return null;
      },
      ariaLabelGroup: PropTypes9.string,
      ariaLabelExpression: PropTypes9.string,
      value: PropTypes9.object.isRequired,
      onChange: PropTypes9.func.isRequired
    };
    return Filter2;
  }(React29.Component)
);

// node_modules/@progress/kendo-react-data-tools/dist/es/columnmenu/ColumnMenu.js
var React34 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/columnmenu/ColumnMenuItem.js
var React30 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/columnmenu/ColumnMenuFilters.js
var React31 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/columnmenu/ColumnMenuOperators.js
var __spreadArray8 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var textOperators2 = [
  { text: columnMenuFilterContainsOperator, operator: "contains" },
  { text: columnMenuFilterNotContainsOperator, operator: "doesnotcontain" },
  { text: columnMenuFilterEqOperator, operator: "eq" },
  { text: columnMenuFilterNotEqOperator, operator: "neq" },
  { text: columnMenuFilterStartsWithOperator, operator: "startswith" },
  { text: columnMenuFilterEndsWithOperator, operator: "endswith" },
  { text: columnMenuFilterIsNullOperator, operator: "isnull" },
  { text: columnMenuFilterIsNotNullOperator, operator: "isnotnull" },
  { text: columnMenuFilterIsEmptyOperator, operator: "isempty" },
  { text: columnMenuFilterIsNotEmptyOperator, operator: "isnotempty" }
];
var numericOperators2 = [
  { text: columnMenuFilterEqOperator, operator: "eq" },
  { text: columnMenuFilterNotEqOperator, operator: "neq" },
  { text: columnMenuFilterGteOperator, operator: "gte" },
  { text: columnMenuFilterGtOperator, operator: "gt" },
  { text: columnMenuFilterLteOperator, operator: "lte" },
  { text: columnMenuFilterLtOperator, operator: "lt" },
  { text: columnMenuFilterIsNullOperator, operator: "isnull" },
  { text: columnMenuFilterIsNotNullOperator, operator: "isnotnull" }
];
var dateOperators2 = [
  { text: columnMenuFilterEqOperator, operator: "eq" },
  { text: columnMenuFilterNotEqOperator, operator: "neq" },
  { text: columnMenuFilterAfterOrEqualOperator, operator: "gte" },
  { text: columnMenuFilterAfterOperator, operator: "gt" },
  { text: columnMenuFilterBeforeOperator, operator: "lt" },
  { text: columnMenuFilterBeforeOrEqualOperator, operator: "lte" },
  { text: columnMenuFilterIsNullOperator, operator: "isnull" },
  { text: columnMenuFilterIsNotNullOperator, operator: "isnotnull" }
];
var booleanOperators2 = [
  { text: columnMenuFilterIsTrue, operator: "" }
];
var ColumnMenuOperators = (
  /** @class */
  function() {
    function ColumnMenuOperators2() {
    }
    Object.defineProperty(ColumnMenuOperators2, "text", {
      /**
       * An array containing the operators for the ColumnMenuTextFilter component.
       *
       * The operators are:
       *
       * - { text: 'columnMenu.filterContainsOperator', operator: 'contains' }
       * - { text: 'columnMenu.filterNotContainsOperator', operator: 'doesnotcontain' }
       * - { text: 'columnMenu.filterEqOperator', operator: 'eq' }
       * - { text: 'columnMenu.filterNotEqOperator', operator: 'neq' }
       * - { text: 'columnMenu.filterStartsWithOperator', operator: 'startswith' }
       * - { text: 'columnMenu.filterEndsWithOperator', operator: 'endswith' }
       * - { text: 'columnMenu.filterIsNullOperator', operator: 'isnull' }
       * - { text: 'columnMenu.filterIsNotNullOperator', operator: 'isnotnull' }
       * - { text: 'columnMenu.filterIsEmptyOperator', operator: 'isempty' }
       * - { text: 'columnMenu.filterIsNotEmptyOperator', operator: 'isnotempty' }
       */
      get: function() {
        return __spreadArray8([], textOperators2, true);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(ColumnMenuOperators2, "numeric", {
      /**
       * An array containing the operators for the ColumnMenuNumericFilter component.
       *
       * The operators are:
       *
       * - { text: 'columnMenu.filterEqOperator', operator: 'eq' }
       * - { text: 'columnMenu.filterNotEqOperator', operator: 'neq' }
       * - { text: 'columnMenu.filterGteOperator', operator: 'gte' }
       * - { text: 'columnMenu.filterGtOperator', operator: 'gt' }
       * - { text: 'columnMenu.filterLteOperator', operator: 'lte' }
       * - { text: 'columnMenu.filterLtOperator', operator: 'lt' }
       * - { text: 'columnMenu.filterIsNullOperator', operator: 'isnull' }
       * - { text: 'columnMenu.filterIsNotNullOperator', operator: 'isnotnull' }
       */
      get: function() {
        return __spreadArray8([], numericOperators2, true);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(ColumnMenuOperators2, "date", {
      /**
       * An array containing the operators for the ColumnMenuDateFilter component.
       *
       * The operators are:
       *
       * - { text: 'columnMenu.filterEqOperator', operator: 'eq' }
       * - { text: 'columnMenu.filterNotEqOperator', operator: 'neq' }
       * - { text: 'columnMenu.filterAfterOrEqualOperator', operator: 'gte' }
       * - { text: 'columnMenu.filterAfterOperator', operator: 'gt' }
       * - { text: 'columnMenu.filterBeforeOperator', operator: 'lt' }
       * - { text: 'columnMenu.filterBeforeOrEqualOperator', operator: 'lte' }
       * - { text: 'columnMenu.filterIsNullOperator', operator: 'isnull' }
       * - { text: 'columnMenu.filterIsNotNullOperator', operator: 'isnotnull' }
       */
      get: function() {
        return __spreadArray8([], dateOperators2, true);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(ColumnMenuOperators2, "boolean", {
      /**
       * An array containing the operators for the ColumnMenuBooleanFilter component.
       *
       * The operators are:
       *
       * - { text: 'columnMenu.filterIsTrue', operator: '' }
       */
      get: function() {
        return __spreadArray8([], booleanOperators2, true);
      },
      enumerable: false,
      configurable: true
    });
    return ColumnMenuOperators2;
  }()
);

// node_modules/@progress/kendo-react-data-tools/dist/es/columnmenu/ColumnMenuFilterLogic.js
var React32 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/columnmenu/ColumnMenuFilterForm.js
var React33 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/navigation/TableKeyboardNavigation.js
var __spreadArray9 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var onConstructor = function(options) {
  var navigatable = options.navigatable, contextStateRef = options.contextStateRef, navigationStateRef = options.navigationStateRef, idPrefix = options.idPrefix;
  if (navigatable) {
    contextStateRef.current = {
      activeId: "",
      level: 0
    };
    navigationStateRef.current = {
      activeElementIsFocused: false,
      prevNavigationIndexes: void 0,
      idPrefix: idPrefix || guid(),
      navigationMatrix: [],
      lastHeaderIndex: -1
    };
  }
};
var onComponentDidMount = function(options) {
  var scope = options.scope, contextStateRef = options.contextStateRef, navigationStateRef = options.navigationStateRef;
  if (contextStateRef.current && navigationStateRef.current && scope) {
    generateMatrix(options);
    var firstId = navigationStateRef.current.navigationMatrix[0][0];
    var firstIdElement = tableKeyboardNavigationTools.getActiveElement(scope, firstId);
    if (firstId && firstIdElement) {
      contextStateRef.current.activeId = firstId;
      firstIdElement.setAttribute("tabIndex", "0");
    }
  }
};
var onGetSnapshotBeforeUpdate = function(options) {
  var contextStateRef = options.contextStateRef, navigationStateRef = options.navigationStateRef, currentDocument = options.document;
  if (contextStateRef.current && navigationStateRef.current && currentDocument) {
    var activeElement = currentDocument.activeElement;
    var activeId = tableKeyboardNavigationTools.getNavigatableId(activeElement);
    if (activeId && activeId === contextStateRef.current.activeId) {
      navigationStateRef.current.activeElementIsFocused = true;
    }
  }
};
var onComponentDidUpdate = function(options) {
  var scope = options.scope, contextStateRef = options.contextStateRef, navigationStateRef = options.navigationStateRef;
  generateMatrix(options);
  if (contextStateRef.current && navigationStateRef.current && scope) {
    var activeElement = tableKeyboardNavigationTools.getActiveElement(scope, contextStateRef.current.activeId);
    if (!activeElement) {
      var firstId = navigationStateRef.current.navigationMatrix[0][0];
      var firstIdElement = tableKeyboardNavigationTools.getActiveElement(scope, firstId);
      if (firstId && firstIdElement) {
        contextStateRef.current.activeId = firstId;
        firstIdElement.setAttribute("tabIndex", "0");
        if (navigationStateRef.current.activeElementIsFocused) {
          firstIdElement.focus();
        }
      }
    }
    navigationStateRef.current.activeElementIsFocused = false;
  }
};
var onFocus = function(event, options) {
  var contextStateRef = options.contextStateRef;
  if (event.isDefaultPrevented()) {
    return;
  }
  if (!contextStateRef.current) {
    return;
  }
  var focusedElement = event.target;
  var activeId = tableKeyboardNavigationTools.getNavigatableId(focusedElement);
  if (activeId && activeId !== contextStateRef.current.activeId) {
    var scope = tableKeyboardNavigationTools.getClosestScope(focusedElement);
    if (!scope) {
      return;
    }
    var prevElement = tableKeyboardNavigationTools.getActiveElement(scope, contextStateRef.current.activeId);
    if (prevElement) {
      prevElement.setAttribute("tabIndex", "-1");
    }
    focusedElement.setAttribute("tabIndex", "0");
    contextStateRef.current.activeId = activeId;
  }
};
var onKeyDown = function(event, options) {
  var contextStateRef = options.contextStateRef, navigationStateRef = options.navigationStateRef, onNavigationAction = options.onNavigationAction;
  if (event.isDefaultPrevented()) {
    return;
  }
  if (!contextStateRef.current || !navigationStateRef.current) {
    return;
  }
  if (event.keyCode === Keys.esc) {
    var elementForFocus = tableKeyboardNavigationTools.getClosestNavigatableElement(event.target);
    tableKeyboardNavigationTools.focusElement({ elementForFocus, event, contextStateRef });
    return;
  }
  var element = event.target;
  var elementId = tableKeyboardNavigationTools.getNavigatableId(element);
  var dataLevel = tableKeyboardNavigationTools.getNavigatableLevel(element);
  var scope = tableKeyboardNavigationTools.getClosestScope(element);
  var matrix = navigationStateRef.current.navigationMatrix;
  if (dataLevel !== void 0 && scope) {
    if (event.keyCode === Keys.enter) {
      var navigatableElement = tableKeyboardNavigationTools.getNavigatableElement(element, { level: dataLevel + 1 });
      if (navigatableElement) {
        tableKeyboardNavigationTools.focusElement({
          elementForFocus: navigatableElement,
          event,
          contextStateRef,
          prevElement: element
        });
        return;
      } else {
        var elementForFocus = tableKeyboardNavigationTools.getFocusableElements(element)[0];
        tableKeyboardNavigationTools.focusElement({ elementForFocus, event, contextStateRef, prevElement: element });
        return;
      }
    }
    if (event.keyCode === Keys.up || event.keyCode === Keys.down || event.keyCode === Keys.left || event.keyCode === Keys.right) {
      var isReverse = event.keyCode === Keys.up || event.keyCode === Keys.left;
      var isVertical = event.keyCode === Keys.up || event.keyCode === Keys.down;
      var currentIdIndexes = void 0;
      if (navigationStateRef.current && navigationStateRef.current.prevNavigationIndexes) {
        var _a4 = navigationStateRef.current.prevNavigationIndexes, rowIndex = _a4[0], cellIndex = _a4[1];
        if (matrix[rowIndex][cellIndex] === elementId) {
          currentIdIndexes = navigationStateRef.current.prevNavigationIndexes;
        } else {
          currentIdIndexes = findId(matrix, elementId);
        }
      } else {
        currentIdIndexes = findId(matrix, elementId);
      }
      if (currentIdIndexes) {
        var rowIndex = currentIdIndexes[0], cellIndex = currentIdIndexes[1];
        var _b2 = isVertical ? findNextIdByRowIndex(rowIndex, cellIndex, elementId, matrix, isReverse) : findNextIdByCellIndex(rowIndex, cellIndex, elementId, matrix, isReverse), idForFocus = _b2[0], currentIndexes = _b2[1];
        if (idForFocus) {
          var elementForFocus = tableKeyboardNavigationTools.getActiveElement(scope, idForFocus);
          tableKeyboardNavigationTools.focusElement({ elementForFocus, event, contextStateRef, prevElement: element });
          navigationStateRef.current.prevNavigationIndexes = currentIndexes;
          if (onNavigationAction) {
            onNavigationAction({ focusElement: elementForFocus, event });
          }
        }
      }
    }
  }
};
var generateMatrix = function(options) {
  var navigationStateRef = options.navigationStateRef, scope = options.scope;
  if (!navigationStateRef.current || !scope) {
    return;
  }
  var matrix = [];
  var thead = getHeaderElement(scope);
  var tbody = getBodyElement(scope);
  if (!thead || !tbody) {
    return;
  }
  var headerRows = Array.from(thead.children);
  var bodyRows = Array.from(tbody.children);
  __spreadArray9(__spreadArray9([], headerRows, true), bodyRows, true).forEach(function(row, rowIndex) {
    Array.from(row.children).forEach(function(cell) {
      var cellId = getNavigatableId(cell);
      if (!cellId) {
        return;
      }
      var rowSpan = cell.rowSpan || 1;
      var cellSpan = cell.colSpan || 1;
      var cellIndex;
      for (var depth = rowIndex, maxDepth = rowIndex + rowSpan; depth < maxDepth; depth++) {
        if (!matrix[depth]) {
          matrix[depth] = [];
        }
        if (cellIndex === void 0) {
          var freeSlotIndex = matrix[depth].findIndex(function(mi) {
            return !mi;
          });
          cellIndex = freeSlotIndex > -1 ? freeSlotIndex : matrix[depth].length;
        }
        matrix[depth][cellIndex] = cellId || "";
      }
      for (var depth = cellIndex + 1, maxDepth = cellIndex + cellSpan; depth < maxDepth; depth++) {
        matrix[rowIndex][depth] = cellId || "";
      }
    });
  });
  navigationStateRef.current.navigationMatrix = matrix.filter(function(row) {
    return !!row;
  });
  navigationStateRef.current.lastHeaderIndex = headerRows.length - 1;
};
var tableKeyboardNavigation = {
  onConstructor,
  onComponentDidMount,
  onGetSnapshotBeforeUpdate,
  onComponentDidUpdate,
  onFocus,
  onKeyDown,
  generateMatrix
};

// node_modules/@progress/kendo-react-data-tools/dist/es/selection/TableSelection.js
var React35 = __toESM(require_react());

// node_modules/@progress/kendo-react-data-tools/dist/es/selection/constants.js
var TABLE_ROW_INDEX_ATTRIBUTE = "data-grid-row-index";
var TABLE_COL_INDEX_ATTRIBUTE = "data-grid-col-index";
var TABLE_PREVENT_SELECTION_ELEMENT = "data-prevent-selection";

// node_modules/@progress/kendo-react-data-tools/dist/es/selection/utils.js
var __assign18 = function() {
  __assign18 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign18.apply(this, arguments);
};
var __spreadArray10 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var GROUP_SUBITEMS_COLLECTION = "items";
var LAST_SELECTION_STATE = {};
var getCellSelection = function(currentSelection, selectedColumn) {
  if (!currentSelection || Array.isArray(currentSelection) && currentSelection.length === 0) {
    return [selectedColumn];
  } else {
    var foundIndex = currentSelection.findIndex(function(columnIndex) {
      return columnIndex === selectedColumn;
    });
    if (foundIndex > -1) {
      return currentSelection.filter(function(columnIndex) {
        return columnIndex === selectedColumn;
      });
    } else {
      return __spreadArray10(__spreadArray10([], currentSelection, true), [selectedColumn], false);
    }
  }
};
var closestTagName = function(target, tagName) {
  var currentTarget = target;
  while (currentTarget) {
    if (currentTarget.tagName === tagName) {
      return currentTarget;
    } else if (currentTarget.tagName === "TABLE") {
      return null;
    }
    currentTarget = currentTarget.parentElement;
  }
  return null;
};
var getRowIndex = function(element) {
  if (!element) {
    return;
  }
  var rowIndex = element.getAttribute(TABLE_ROW_INDEX_ATTRIBUTE);
  return rowIndex ? parseInt(rowIndex, 10) : void 0;
};
var getColumnIndex = function(element) {
  if (!element) {
    return;
  }
  var colIndex = element.getAttribute(TABLE_COL_INDEX_ATTRIBUTE);
  return colIndex ? parseInt(colIndex, 10) : void 0;
};
var setSelectedState = function(options) {
  var subItemsField = options.subItemsField || GROUP_SUBITEMS_COLLECTION;
  var idGetter = getter(options.dataItemKey);
  return mapTree(options.data, subItemsField, function(item) {
    var _a4;
    return extendDataItem(item, subItemsField, (_a4 = {}, _a4[options.selectedField] = options.selectedState[idGetter(item)], _a4));
  });
};
var getSelectedStateFromKeyDown = function(options) {
  var event = options.event, dataItemKey = options.dataItemKey, selectedState = options.selectedState;
  var syntheticEvent = event.syntheticEvent, nativeEvent = event.nativeEvent, dataItems = event.dataItems, componentId = event.componentId, mode = event.mode, cell = event.cell, selectedField = event.selectedField;
  var target = nativeEvent.target, ctrlKey = nativeEvent.ctrlKey, metaKey = nativeEvent.metaKey, altKey = nativeEvent.altKey, shiftKey = nativeEvent.shiftKey;
  if (syntheticEvent.isDefaultPrevented() || nativeEvent.keyCode !== Keys.enter) {
    return selectedState;
  }
  var tdElement = closestTagName(target, "TD");
  var trElement = closestTagName(target, "TR");
  var colIndex = getColumnIndex(tdElement);
  var rowIndex = getRowIndex(trElement);
  var selectionState = {};
  if (tdElement && trElement && rowIndex !== void 0 && colIndex !== void 0) {
    selectionState = getSelectedState({
      selectedState,
      dataItemKey,
      event: {
        ctrlKey,
        metaKey,
        altKey,
        shiftKey,
        dataItems,
        cell,
        mode,
        selectedField,
        componentId,
        startColIndex: colIndex,
        endColIndex: colIndex,
        startRowIndex: rowIndex,
        endRowIndex: rowIndex,
        isDrag: false,
        syntheticEvent,
        nativeEvent,
        target: event.target,
        dataItem: null
      }
    });
  }
  return selectionState;
};
var getSelectedState = function(options) {
  var event = options.event, dataItemKey = options.dataItemKey, prevSelectedState = options.selectedState;
  var dataItems = event.dataItems, startRowIndex = event.startRowIndex, endRowIndex = event.endRowIndex, startColIndex = event.startColIndex, endColIndex = event.endColIndex, cell = event.cell, isDrag = event.isDrag, ctrlKey = event.ctrlKey, shiftKey = event.shiftKey, metaKey = event.metaKey, componentId = event.componentId, mode = event.mode, dataItem = event.dataItem;
  var idGetter = getter(dataItemKey);
  var selectionState = {};
  var isInputEvent = dataItem !== null;
  if (mode === "single") {
    var currentDataItem = dataItems.slice(startRowIndex, startRowIndex + 1)[0];
    var itemId = idGetter(currentDataItem);
    LAST_SELECTION_STATE[componentId] = {
      rowIndex: startRowIndex,
      colIndex: startColIndex
    };
    selectionState[itemId] = cell ? [startColIndex] : true;
  } else if (isDrag || !shiftKey && !ctrlKey && !metaKey && !isInputEvent) {
    if (!isDrag) {
      LAST_SELECTION_STATE[componentId] = {
        rowIndex: startRowIndex,
        colIndex: startColIndex
      };
    }
    var selectedColumns_1 = [];
    for (var i = startColIndex; i <= endColIndex; i++) {
      selectedColumns_1.push(i);
    }
    var selectedDataItems = dataItems.slice(startRowIndex, endRowIndex + 1);
    selectedDataItems.forEach(function(currentItem) {
      var itemId2 = idGetter(currentItem);
      selectionState[itemId2] = cell ? __spreadArray10([], selectedColumns_1, true) : true;
    });
  } else {
    if (ctrlKey || metaKey || isInputEvent) {
      LAST_SELECTION_STATE[componentId] = {
        rowIndex: startRowIndex,
        colIndex: startColIndex
      };
      var selectedDataItems = dataItems.slice(startRowIndex, endRowIndex + 1);
      var currentDataItem = selectedDataItems[0];
      var itemId = idGetter(currentDataItem);
      selectionState = __assign18({}, prevSelectedState);
      selectionState[itemId] = cell && !isInputEvent ? getCellSelection(selectionState[itemId], startColIndex) : !selectionState[itemId];
    } else if (shiftKey) {
      var lastRowIndex = LAST_SELECTION_STATE[componentId] && LAST_SELECTION_STATE[componentId].rowIndex || 0;
      var lastColIndex = LAST_SELECTION_STATE[componentId] && LAST_SELECTION_STATE[componentId].colIndex || 0;
      var currentStartRowIndex = Math.min(lastRowIndex, startRowIndex, endRowIndex);
      var currentEndRowIndex = Math.max(lastRowIndex, startRowIndex, endRowIndex);
      var currentStarColIndex = Math.min(lastColIndex, startColIndex, endColIndex);
      var currentEndColIndex = Math.max(lastColIndex, startColIndex, endColIndex);
      var selectedColumns_2 = [];
      for (var i = currentStarColIndex; i <= currentEndColIndex; i++) {
        selectedColumns_2.push(i);
      }
      var selectedDataItems = dataItems.slice(currentStartRowIndex, currentEndRowIndex + 1);
      selectedDataItems.forEach(function(currentItem) {
        var itemId2 = idGetter(currentItem);
        selectionState[itemId2] = cell ? __spreadArray10([], selectedColumns_2, true) : true;
      });
    }
  }
  return selectionState;
};
var HAS_RELATIVE_STACKING_CONTEXT = hasRelativeStackingContext();
var relativeContextElement = function(element) {
  if (!element || !HAS_RELATIVE_STACKING_CONTEXT) {
    return null;
  }
  var node = element.parentElement;
  while (node) {
    if (window.getComputedStyle(node).transform !== "none") {
      return node;
    }
    node = node.parentElement;
  }
};
var getOffset = function(offsetParent) {
  if (offsetParent && offsetParent.ownerDocument) {
    if (offsetParent !== offsetParent.ownerDocument.body) {
      var rect = offsetParent.getBoundingClientRect();
      return {
        left: rect.left - offsetParent.scrollLeft,
        top: rect.top - offsetParent.scrollTop
      };
    }
  }
  return { left: 0, top: 0 };
};
var getSelectionOptions = function(selectable) {
  var enabled = selectable ? typeof selectable.enabled === "boolean" ? selectable.enabled : true : false;
  var drag = selectable ? Boolean(selectable.drag) : false;
  var mode = selectable && selectable.mode ? selectable.mode : "multiple";
  var cell = Boolean(selectable && selectable.cell);
  return { enabled, drag, mode, cell };
};

// node_modules/@progress/kendo-react-data-tools/dist/es/selection/TableSelection.js
var __assign19 = function() {
  __assign19 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign19.apply(this, arguments);
};
var TableSelection = function(props) {
  var onRelease = props.onRelease, childRef = props.childRef;
  var _a4 = getSelectionOptions(props.selectable), selectionEnabled = _a4.enabled, selectionDrag = _a4.drag, selectionMode = _a4.mode, selectionCell = _a4.cell;
  var draggableRef = React35.useRef(null);
  var overlayElementRef = React35.useRef(null);
  var dragStartRef = React35.useRef({ clientX: 0, clientY: 0 });
  var _b2 = React35.useState(null), overlaySettings = _b2[0], setOverlaySettings = _b2[1];
  var offsetParentRef = React35.useRef();
  var isDraggedRef = React35.useRef(false);
  var isPreventedRef = React35.useRef(false);
  var onPressHandler = React35.useCallback(function(eventData) {
    var event = eventData.event;
    isPreventedRef.current = event.originalEvent.target.hasAttribute(TABLE_PREVENT_SELECTION_ELEMENT);
    if (isPreventedRef.current || !draggableRef.current) {
      return;
    }
    offsetParentRef.current = relativeContextElement(draggableRef.current.element);
    dragStartRef.current = {
      clientY: event.clientY,
      clientX: event.clientX
    };
  }, []);
  var onDragHandler = React35.useCallback(function(eventData) {
    var event = eventData.event;
    var _a5 = dragStartRef.current, clientX = _a5.clientX, clientY = _a5.clientY;
    if (isPreventedRef.current) {
      return;
    }
    if (!selectionDrag || selectionMode === "single") {
      return;
    }
    if (Math.abs(clientY - event.clientY) > 5 || Math.abs(clientX - event.clientX) > 5) {
      isDraggedRef.current = true;
    }
    if (isDraggedRef.current) {
      var offset = getOffset(offsetParentRef.current);
      setOverlaySettings({
        top: Math.min(clientY, event.clientY) - offset.top,
        left: Math.min(clientX, event.clientX) - offset.left,
        width: Math.abs(event.clientX - clientX),
        height: Math.abs(event.clientY - clientY)
      });
    }
  }, [setOverlaySettings, selectionDrag, selectionMode]);
  var onReleaseHandler = React35.useCallback(function(eventData) {
    var event = eventData.event;
    var _a5 = dragStartRef.current, clientX = _a5.clientX, clientY = _a5.clientY;
    if (isPreventedRef.current || !draggableRef.current) {
      return;
    }
    var currentDocument = draggableRef.current.element && draggableRef.current.element.ownerDocument;
    if (!currentDocument) {
      return;
    }
    if (!isDraggedRef.current) {
      var startElement = currentDocument.elementFromPoint(clientX, clientY);
      if (!startElement) {
        return;
      }
      var tdElement = closestTagName(startElement, "TD");
      var trElement = closestTagName(startElement, "TR");
      var colIndex = getColumnIndex(tdElement);
      var rowIndex = getRowIndex(trElement);
      if (tdElement && trElement && rowIndex !== void 0 && colIndex !== void 0) {
        onRelease({
          nativeEvent: event.originalEvent,
          startRowIndex: rowIndex,
          startColIndex: colIndex,
          endRowIndex: rowIndex,
          endColIndex: colIndex,
          altKey: event.altKey,
          shiftKey: event.shiftKey,
          ctrlKey: event.ctrlKey,
          metaKey: event.metaKey,
          mode: selectionMode,
          cell: selectionCell,
          isDrag: false
        });
      }
    } else {
      var top_1 = Math.min(clientY, event.clientY);
      var left = Math.min(clientX, event.clientX);
      var bottom = Math.max(clientY, event.clientY);
      var right = Math.max(clientX, event.clientX);
      var overlayElement = overlayElementRef.current;
      if (!overlayElement) {
        return;
      }
      overlayElement.style.visibility = "hidden";
      var startElement = currentDocument.elementFromPoint(left, top_1);
      var endElement = currentDocument.elementFromPoint(right, bottom);
      overlayElement.style.visibility = "";
      if (!startElement || !endElement) {
        return;
      }
      var startTdElement = closestTagName(startElement, "TD");
      var startTrElement = closestTagName(startTdElement, "TR");
      var endTdElement = closestTagName(endElement, "TD");
      var endTrElement = closestTagName(endTdElement, "TR");
      var startColIndex = getColumnIndex(startTdElement);
      var startRowIndex = getRowIndex(startTrElement);
      var endColIndex = getColumnIndex(endTdElement);
      var endRowIndex = getRowIndex(endTrElement);
      if (startColIndex !== void 0 && startRowIndex !== void 0 && endColIndex !== void 0 && endRowIndex !== void 0) {
        onRelease({
          nativeEvent: event.originalEvent,
          startRowIndex,
          startColIndex,
          endRowIndex,
          endColIndex,
          altKey: event.altKey,
          shiftKey: event.shiftKey,
          ctrlKey: event.ctrlKey,
          metaKey: event.metaKey,
          mode: selectionMode,
          cell: selectionCell,
          isDrag: true
        });
      }
    }
    setOverlaySettings(null);
    isDraggedRef.current = false;
    dragStartRef.current = { clientX: 0, clientY: 0 };
  }, [setOverlaySettings, selectionMode, selectionCell, onRelease]);
  if (!selectionEnabled) {
    return React35.cloneElement(React35.Children.only(props.children), { ref: childRef });
  }
  return React35.createElement(
    React35.Fragment,
    null,
    React35.createElement(Draggable, { onPress: onPressHandler, onDrag: onDragHandler, onRelease: onReleaseHandler, ref: draggableRef, childRef }, props.children),
    overlaySettings && React35.createElement("div", { ref: overlayElementRef, style: __assign19(__assign19({}, overlaySettings), { position: "fixed" }), className: "k-marquee k-marquee-color" })
  );
};

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridSelectionCell.js
var React36 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/GridColumn.js
var PropTypes10 = __toESM(require_prop_types());
var GridColumn = function(_) {
  return null;
};
GridColumn.displayName = "KendoReactGridColumn";
GridColumn.defaultProps = {
  filterable: true,
  editable: true,
  sortable: true,
  resizable: true,
  reorderable: true,
  groupable: true
};
GridColumn.propTypes = {
  field: PropTypes10.string,
  title: PropTypes10.string,
  editable: PropTypes10.bool,
  sortable: PropTypes10.oneOfType([
    PropTypes10.bool,
    PropTypes10.shape({
      allowUnsort: PropTypes10.bool.isRequired
    })
  ]),
  cell: PropTypes10.any,
  filterCell: PropTypes10.any,
  filterTitle: PropTypes10.string,
  filterable: PropTypes10.bool,
  filter: PropTypes10.oneOf(["text", "numeric", "boolean", "date"]),
  editor: PropTypes10.oneOf(["text", "numeric", "boolean", "date"]),
  width: PropTypes10.oneOfType([PropTypes10.string, PropTypes10.number]),
  minResizableWidth: PropTypes10.number,
  headerCell: PropTypes10.any,
  footerCell: PropTypes10.any,
  headerSelectionValue: PropTypes10.bool,
  format: PropTypes10.string,
  headerClassName: PropTypes10.string,
  className: PropTypes10.string,
  reorderable: PropTypes10.bool,
  resizable: PropTypes10.bool,
  orderIndex: PropTypes10.number,
  groupable: PropTypes10.bool,
  columnMenu: PropTypes10.any,
  locked: PropTypes10.bool
};

// node_modules/@progress/kendo-react-grid/dist/es/utils/index.js
var __assign20 = function() {
  __assign20 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign20.apply(this, arguments);
};
var __rest2 = function(s, e) {
  var t = {};
  for (var p in s)
    if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
      t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
function getNestedValue(fieldName, dataItem) {
  var path = (fieldName || "").split(".");
  var data = dataItem;
  path.forEach(function(p) {
    data = data ? data[p] : void 0;
  });
  return data;
}
function flatData(output, input, footer, dataIndex, groupDefined, field, level) {
  if (level === void 0) {
    level = 0;
  }
  var maxLevel = level;
  for (var i = 0; i < input.length; i++) {
    if (!groupDefined || input[i].value === void 0 || input[i].items === void 0) {
      output[output.length] = {
        dataIndex: dataIndex.index,
        dataItem: input[i],
        rowType: "data",
        level,
        expanded: field === void 0 || getNestedValue(field, input[i])
      };
      dataIndex.index++;
      continue;
    }
    maxLevel = Math.max(maxLevel, level + 1);
    var expanded = field === void 0 || getNestedValue(field, input[i]) === void 0 || getNestedValue(field, input[i]);
    output[output.length] = {
      dataIndex: -1,
      dataItem: input[i],
      level,
      rowType: "groupHeader",
      expanded
    };
    if (expanded) {
      maxLevel = Math.max(flatData(output, input[i].items, footer, dataIndex, groupDefined, field, level + 1), maxLevel);
    }
    if (footer === "always" || expanded && footer === "visible") {
      output[output.length] = {
        dataIndex: -1,
        dataItem: input[i],
        rowType: "groupFooter",
        level,
        expanded
      };
    }
  }
  return maxLevel;
}
function readColumns(elements, oldColumns, idInfo, depth) {
  if (depth === void 0) {
    depth = 0;
  }
  var columns = [];
  if (!elements) {
    return [];
  }
  if (elements && elements.length === void 0) {
    elements = [elements];
  }
  elements.forEach(function(columnProps, index) {
    columnProps = columnProps.props ? columnProps.props : columnProps;
    var oldColumn = oldColumns[index] || null;
    var children = readColumns(columnProps.children, oldColumn && oldColumn.children || [], idInfo, depth + 1);
    columns.push(Object.assign({ depth }, GridColumn.defaultProps, children.length ? { cell: function() {
      return null;
    }, filterCell: function() {
      return null;
    } } : {}, oldColumn ? { width: oldColumn.width, orderIndex: oldColumn.orderIndex } : {}, columnProps, {
      id: columnProps.id ? columnProps.id : tableKeyboardNavigationTools.generateNavigatableId("".concat(idInfo.prevId++), idInfo.idPrefix, "column"),
      declarationIndex: columns.length,
      children,
      rowSpan: 0,
      colSpan: 0,
      isAccessible: true
    }));
  });
  var comparator = function(a, b) {
    return a.orderIndex === b.orderIndex ? a.declarationIndex - b.declarationIndex : (a.orderIndex || 0) - (b.orderIndex || 0);
  };
  columns.sort(comparator);
  if (depth === 0) {
    var ret_1 = [];
    var flat_1 = function(cols, pIndex) {
      return cols.forEach(function(c) {
        c.parentIndex = pIndex;
        flat_1(c.children, ret_1.push(c) - 1);
      });
    };
    flat_1(columns, -1);
    return ret_1;
  }
  return columns;
}
function autoGenerateColumns(data, group, expandField, idInfo) {
  var propData = [];
  if (Array.isArray(data)) {
    propData = data;
  } else if (data) {
    propData = data.data;
  }
  var columns = [];
  if (propData.length > 0) {
    var itemForColumnsGen = propData[0];
    if (group) {
      for (var i = 0; i < group.length; i++) {
        itemForColumnsGen = itemForColumnsGen.items && itemForColumnsGen.items[0];
      }
    }
    var fields = Object.getOwnPropertyNames(itemForColumnsGen);
    fields.forEach(function(field) {
      if (field !== expandField) {
        columns.push(Object.assign({
          id: tableKeyboardNavigationTools.generateNavigatableId("".concat(idInfo.prevId++), idInfo.idPrefix, "column"),
          declarationIndex: -1,
          parentIndex: -1,
          depth: 0,
          colSpan: 0,
          rowSpan: 0,
          index: 0,
          left: 0,
          right: 0,
          rightBorder: false,
          children: [],
          ariaColumnIndex: 0,
          isAccessible: true
        }, GridColumn.defaultProps, { field }));
      }
    });
  }
  return columns;
}
var hasParentFooterCell = function(columns, column) {
  var parent = columns[column.parentIndex];
  while (parent) {
    if (parent.footerCell) {
      return true;
    }
    parent = columns[parent.parentIndex];
  }
  return false;
};
var footerColumns = function(columns) {
  return columns.filter(function(column) {
    if (hasParentFooterCell(columns, column)) {
      return false;
    }
    return Boolean(column.footerCell) || !(column.children && column.children.length > 0);
  });
};
var sanitizeColumns = function(columns) {
  columns.sort(function(a, b) {
    return a.declarationIndex - b.declarationIndex;
  });
  return columns.map(function(column) {
    var declarationIndex = column.declarationIndex, parentIndex = column.parentIndex, depth = column.depth, colSpan = column.colSpan, rowSpan = column.rowSpan, index = column.index, kFirst = column.kFirst, children = column.children, props = __rest2(column, ["declarationIndex", "parentIndex", "depth", "colSpan", "rowSpan", "index", "kFirst", "children"]);
    return children.length ? __assign20({ children: sanitizeColumns(children) }, props) : props;
  });
};
var firefox = typeof window !== "undefined" && /Firefox/.test(window.navigator.userAgent);
var firefoxMaxHeight = 17895697;

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridSelectionCell.js
var __assign21 = function() {
  __assign21 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign21.apply(this, arguments);
};
var GridSelectionCell = function(props) {
  var handleOnChange = React36.useCallback(function(syntheticEvent) {
    if (props.selectionChange) {
      props.selectionChange({ syntheticEvent });
    }
  }, [props.selectionChange]);
  var inputId = React36.useMemo(function() {
    return guid();
  }, []);
  var selectedValue = getNestedValue(props.field, props.dataItem);
  var navigationAttributes = useTableKeyboardNavigation(props.id);
  var defaultRendering = props.rowType !== "groupHeader" ? React36.createElement(
    "td",
    __assign21({ colSpan: props.colSpan, style: props.style, className: props.className, "aria-colindex": props.ariaColumnIndex, role: "gridcell" }, navigationAttributes),
    React36.createElement("input", { checked: typeof selectedValue === "boolean" && selectedValue, id: inputId, type: "checkbox", className: "k-checkbox k-checkbox-md k-rounded-md", onChange: handleOnChange }),
    React36.createElement("label", { className: "k-checkbox-label", htmlFor: inputId })
  ) : null;
  return props.render ? props.render.call(void 0, defaultRendering, props) : defaultRendering;
};

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridHierarchyCell.js
var React37 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/messages/index.js
var _a3;
var noRecords = "grid.noRecords";
var pagerInfo2 = "grid.pagerInfo";
var pagerFirstPage2 = "grid.pagerFirstPage";
var pagerPreviousPage2 = "grid.pagerPreviousPage";
var pagerNextPage2 = "grid.pagerNextPage";
var pagerLastPage2 = "grid.pagerLastPage";
var pagerItemPerPage2 = "grid.pagerItemsPerPage";
var pagerPage2 = "grid.pagerPage";
var pagerOf2 = "grid.pagerOf";
var pagerTotalPages2 = "grid.pagerTotalPages";
var groupPanelEmpty = "grid.groupPanelEmpty";
var groupColumn = "grid.groupColumn";
var ungroupColumn = "grid.ungroupColumn";
var filterClearButton = "grid.filterClearButton";
var filterSubmitButton = "grid.filterSubmitButton";
var filterTitle = "grid.filterTitle";
var sortAscending = "grid.sortAscending";
var sortDescending = "grid.sortDescending";
var searchPlaceholder = "grid.searchPlaceholder";
var filterCheckAll = "grid.filterCheckAll";
var filterChooseOperator = "grid.filterChooseOperator";
var filterSelectedItems = "grid.filterSelectedItems";
var sortAriaLabel2 = "grid.sortAriaLabel";
var filterAriaLabel2 = "grid.filterAriaLabel";
var groupPanelAriaLabel = "grid.groupPanelAriaLabel";
var groupExpand = "grid.groupExpand";
var groupCollapse = "grid.groupCollapse";
var detailExpand = "grid.detailExpand";
var detailCollapse = "grid.detailCollapse";
var messages2 = (_a3 = {}, _a3[detailExpand] = "Expand detail row", _a3[detailCollapse] = "Collapse detail row", _a3[groupExpand] = "Expand group", _a3[groupCollapse] = "Collapse Group", _a3[noRecords] = "No records available", _a3[groupPanelEmpty] = "Drag a column header and drop it here to group by that column", _a3[filterClearButton] = "Clear", _a3[filterSubmitButton] = "Filter", _a3[filterTitle] = "Filter", _a3[sortAscending] = "Sort Ascending", _a3[sortDescending] = "Sort Descending", _a3[sortAriaLabel2] = "Sortable", _a3[groupColumn] = "Group Column", _a3[ungroupColumn] = "Ungroup Column", _a3[pagerItemPerPage2] = "items per page", _a3[pagerInfo2] = "{0} - {1} of {2} items", _a3[pagerFirstPage2] = "Go to the first page", _a3[pagerPreviousPage2] = "Go to the previous page", _a3[pagerNextPage2] = "Go to the next page", _a3[pagerLastPage2] = "Go to the last page", _a3[pagerPage2] = "Page", _a3[pagerOf2] = "of", _a3[pagerTotalPages2] = "{0}", _a3[searchPlaceholder] = "Search", _a3[filterCheckAll] = "Check All", _a3[filterChooseOperator] = "Choose Operator", _a3[filterSelectedItems] = "selected items", _a3[filterAriaLabel2] = "Filter", _a3[groupPanelAriaLabel] = "Group panel", _a3["grid.filterEqOperator"] = "Is equal to", _a3["grid.filterNotEqOperator"] = "Is not equal to", _a3["grid.filterIsNullOperator"] = "Is null", _a3["grid.filterIsNotNullOperator"] = "Is not null", _a3["grid.filterIsEmptyOperator"] = "Is empty", _a3["grid.filterIsNotEmptyOperator"] = "Is not empty", _a3["grid.filterStartsWithOperator"] = "Starts with", _a3["grid.filterContainsOperator"] = "Contains", _a3["grid.filterNotContainsOperator"] = "Does not contain", _a3["grid.filterEndsWithOperator"] = "Ends with", _a3["grid.filterGteOperator"] = "Is greater than or equal to", _a3["grid.filterGtOperator"] = "Is greater than", _a3["grid.filterLteOperator"] = "Is less than or equal to", _a3["grid.filterLtOperator"] = "Is less than", _a3["grid.filterIsTrue"] = "Is true", _a3["grid.filterIsFalse"] = "Is false", _a3["grid.filterBooleanAll"] = "(All)", _a3["grid.filterAfterOrEqualOperator"] = "Is after or equal to", _a3["grid.filterAfterOperator"] = "Is after", _a3["grid.filterBeforeOperator"] = "Is before", _a3["grid.filterBeforeOrEqualOperator"] = "Is before or equal to", _a3["grid.filterAndLogic"] = "And", _a3["grid.filterOrLogic"] = "Or", _a3);
function pagerMessagesMap(pagerMessageKey) {
  var messageKey = pagerMessageKey.replace(/^pager\.([a-z])/, function(_match, group) {
    return "grid.pager" + group.toUpperCase();
  });
  return { messageKey, defaultMessage: messages2[messageKey] };
}

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridHierarchyCell.js
var __assign22 = function() {
  __assign22 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign22.apply(this, arguments);
};
var GridHierarchyCell = function(props) {
  var defaultRendering = null;
  var expanded = getNestedValue(props.field, props.dataItem);
  var navigationAttributes = useTableKeyboardNavigation(props.id);
  var localization = useLocalization();
  var onKeyDownHandler = React37.useCallback(function(event) {
    if (event.isDefaultPrevented()) {
      return;
    }
    if (event.keyCode === Keys.enter && props.onChange) {
      event.preventDefault();
      props.onChange({
        dataItem: props.dataItem,
        dataIndex: props.dataIndex,
        syntheticEvent: event,
        field: props.field,
        value: !expanded
      });
    }
  }, [props.dataItem, props.dataIndex, props.field, props.onChange, expanded]);
  if (props.rowType === "groupFooter") {
    defaultRendering = React37.createElement("td", __assign22({ className: "k-table-td k-hierarchy-cell", role: "gridcell" }, navigationAttributes));
  } else if (props.rowType !== "groupHeader") {
    var baseMessage = expanded ? detailCollapse : detailExpand;
    var message = localization.toLanguageString(baseMessage, messages2[baseMessage]);
    defaultRendering = React37.createElement(
      "td",
      __assign22({ className: "k-table-td k-hierarchy-cell", onKeyDown: onKeyDownHandler, "aria-expanded": expanded ? "true" : "false", role: "gridcell", "aria-colindex": props.ariaColumnIndex }, navigationAttributes),
      React37.createElement(
        "a",
        { onClick: function(e) {
          e.preventDefault();
          if (props.onChange) {
            props.onChange({
              dataItem: props.dataItem,
              dataIndex: props.dataIndex,
              syntheticEvent: e,
              field: props.field,
              value: !expanded
            });
          }
        }, "aria-label": message, href: "#", tabIndex: -1 },
        React37.createElement(IconWrap, { name: expanded ? "minus" : "plus", icon: expanded ? minusIcon : plusIcon })
      )
    );
  }
  return props.render ? props.render.call(void 0, defaultRendering, props) : defaultRendering;
};

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridEditCell.js
var React38 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/constants/index.js
var GRID_ROW_INDEX_ATTRIBUTE = TABLE_ROW_INDEX_ATTRIBUTE;
var GRID_COL_INDEX_ATTRIBUTE = TABLE_COL_INDEX_ATTRIBUTE;

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridEditCell.js
var __assign23 = function() {
  __assign23 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign23.apply(this, arguments);
};
var GridEditCell = function(props) {
  var _a4, _b2, _c2, _d, _e;
  var data = getNestedValue(props.field, props.dataItem);
  var defaultRendering = null;
  var editCellClasses = classNames("k-table-td", "k-grid-edit-cell", (_a4 = {}, _a4["k-selected"] = props.isSelected, _a4), props.className);
  var inputId = React38.useMemo(function() {
    return guid();
  }, []);
  var navigationAttributes = useTableKeyboardNavigation(props.id);
  var handleOnChange = React38.useCallback(function(syntheticEvent) {
    if (props.onChange) {
      var value = syntheticEvent.target.type === "checkbox" ? syntheticEvent.target.checked : syntheticEvent.target.value;
      props.onChange({
        dataItem: props.dataItem,
        dataIndex: props.dataIndex,
        field: props.field,
        syntheticEvent,
        value
      });
    }
  }, [props.onChange, props.dataItem, props.dataIndex, props.field]);
  var onContextMenu = React38.useCallback(function(event) {
    if (props.onContextMenu) {
      props.onContextMenu.call(void 0, event, props.dataItem, props.field);
    }
  }, [props.onContextMenu, props.dataItem, props.field]);
  var inputChange = React38.useCallback(function(e) {
    if (props.onChange) {
      props.onChange({
        dataItem: props.dataItem,
        dataIndex: props.dataIndex,
        field: props.field,
        syntheticEvent: e.syntheticEvent,
        value: e.value
      });
    }
  }, [props.onChange, props.dataItem, props.dataIndex, props.field]);
  switch (props.editor) {
    case "numeric":
      defaultRendering = React38.createElement(
        "td",
        __assign23({ onContextMenu, colSpan: props.colSpan, style: props.style, className: editCellClasses, "aria-colindex": props.ariaColumnIndex, "aria-selected": props.isSelected }, (_b2 = {}, _b2[GRID_COL_INDEX_ATTRIBUTE] = props.columnIndex, _b2), { role: "gridcell" }, navigationAttributes),
        React38.createElement(NumericTextBox, { width: "100%", value: data === void 0 ? null : data, onChange: inputChange })
      );
      break;
    case "date":
      defaultRendering = React38.createElement(
        "td",
        __assign23({ onContextMenu, colSpan: props.colSpan, style: props.style, className: editCellClasses, "aria-colindex": props.ariaColumnIndex, "aria-selected": props.isSelected }, (_c2 = {}, _c2[GRID_COL_INDEX_ATTRIBUTE] = props.columnIndex, _c2), { role: "gridcell" }, navigationAttributes),
        React38.createElement(DatePicker, { width: "100%", value: data, onChange: inputChange })
      );
      break;
    case "boolean":
      defaultRendering = React38.createElement(
        "td",
        __assign23({ onContextMenu, colSpan: props.colSpan, style: props.style, className: editCellClasses, "aria-colindex": props.ariaColumnIndex, "aria-selected": props.isSelected }, (_d = {}, _d[GRID_COL_INDEX_ATTRIBUTE] = props.columnIndex, _d), { role: "gridcell" }, navigationAttributes),
        React38.createElement("input", { checked: data || false, id: inputId, type: "checkbox", className: "k-checkbox k-checkbox-md k-rounded-md", onChange: handleOnChange }),
        React38.createElement("label", { className: "k-checkbox-label", htmlFor: inputId })
      );
      break;
    default:
      defaultRendering = React38.createElement(
        "td",
        __assign23({ onContextMenu, colSpan: props.colSpan, style: props.style, className: editCellClasses, "aria-colindex": props.ariaColumnIndex, "aria-selected": props.isSelected }, (_e = {}, _e[GRID_COL_INDEX_ATTRIBUTE] = props.columnIndex, _e), { role: "gridcell" }, navigationAttributes),
        React38.createElement("input", { style: { width: "100%" }, className: "k-input", value: data || "", onChange: handleOnChange })
      );
  }
  return props.render ? props.render.call(void 0, defaultRendering, props) : defaultRendering;
};

// node_modules/@progress/kendo-react-grid/dist/es/header/Header.js
var React39 = __toESM(require_react());
var __extends24 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign24 = function() {
  __assign24 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign24.apply(this, arguments);
};
var Header2 = (
  /** @class */
  function(_super) {
    __extends24(Header3, _super);
    function Header3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.headerWrap = null;
      _this.table = null;
      _this.syncScroll = false;
      _this.scrollbarWidth = getScrollbarWidth();
      _this.onScroll = function(_event) {
        if (_this.syncScroll) {
          _this.syncScroll = false;
          return;
        }
        if (!_this.headerWrap) {
          return;
        }
        var scrollLeft = _this.headerWrap.scrollLeft;
        var scrollableDataElement = _this.props.scrollableDataElement();
        if (scrollableDataElement && scrollableDataElement.scrollLeft !== scrollLeft) {
          scrollableDataElement.scrollLeft = scrollLeft;
        }
      };
      return _this;
    }
    Object.defineProperty(Header3.prototype, "element", {
      get: function() {
        return this.props.elemRef.current;
      },
      enumerable: false,
      configurable: true
    });
    Header3.prototype.componentDidMount = function() {
      var rtl = isRtl(this.element);
      if (this.props.columnResize) {
        this.props.columnResize.setIsRtl(rtl);
      }
      this.forceUpdate();
    };
    Header3.prototype.setScrollLeft = function(scrollLeft) {
      if (this.headerWrap && this.headerWrap.scrollLeft !== scrollLeft) {
        this.syncScroll = true;
        this.headerWrap.scrollLeft = scrollLeft;
      }
    };
    Header3.prototype.setWidth = function(width) {
      if (this.table) {
        this.table.style.width = width + "px";
      }
    };
    Header3.prototype.render = function() {
      var _a4;
      var _this = this;
      if (!this.props.staticHeaders) {
        return React39.createElement(
          "thead",
          __assign24({ role: "presentation", className: classNames("k-table-thead", { "k-grid-draggable-header": this.props.draggable }) }, tableKeyboardNavigationHeaderAttributes),
          this.props.headerRow,
          this.props.filterRow
        );
      }
      return React39.createElement(
        "div",
        { ref: this.props.elemRef, className: classNames("k-grid-header", { "k-grid-draggable-header": this.props.draggable }), role: "presentation" },
        React39.createElement(
          "div",
          { ref: function(div) {
            _this.headerWrap = div;
          }, className: "k-grid-header-wrap", style: this.scrollbarWidth ? {} : { borderWidth: 0 }, onScroll: this.onScroll, role: "presentation" },
          React39.createElement(
            "table",
            { ref: function(table) {
              _this.table = table;
            }, className: classNames("k-table k-grid-header-table", (_a4 = {
              "k-table-md": !this.props.size
            }, _a4["k-table-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _a4), this.props.className), role: "presentation" },
            React39.createElement("colgroup", { ref: function(e) {
              _this.props.columnResize.colGroupHeader = e;
            } }, this.props.cols),
            React39.createElement(
              "thead",
              __assign24({ className: "k-table-thead", role: "rowgroup" }, tableKeyboardNavigationHeaderAttributes),
              this.props.headerRow,
              this.props.filterRow
            )
          )
        )
      );
    };
    return Header3;
  }(React39.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/header/HeaderRow.js
var React44 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/interfaces/GridSortSettings.js
var __spreadArray11 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var normalize2 = function() {
  var settings5 = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    settings5[_i] = arguments[_i];
  }
  return Object.assign.apply(Object, __spreadArray11([{ allowUnsort: true, mode: "single" }], settings5, false));
};

// node_modules/@progress/kendo-react-grid/dist/es/drag/ColumnResizer.js
var React40 = __toESM(require_react());
var __extends25 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var ColumnResizer2 = (
  /** @class */
  function(_super) {
    __extends25(ColumnResizer3, _super);
    function ColumnResizer3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.draggable = null;
      _this.isDragged = false;
      _this.onDrag = function(data) {
        _this.isDragged = true;
        var element = _this.draggable && _this.draggable.element;
        if (element) {
          _this.props.resize(data.event, element, false);
        }
      };
      _this.onRelease = function(data) {
        if (!_this.isDragged) {
          return;
        }
        var element = _this.draggable && _this.draggable.element;
        if (element) {
          _this.props.resize(data.event, element, true);
        }
        _this.isDragged = false;
      };
      _this.onDoubleClick = function(event) {
        if (_this.isDragged) {
          return;
        }
        var element = _this.draggable && _this.draggable.element;
        if (element) {
          _this.props.autofit(event, element);
        }
      };
      return _this;
    }
    ColumnResizer3.prototype.render = function() {
      var _this = this;
      return React40.createElement(
        Draggable,
        { onPress: function(e) {
          e.event.originalEvent.stopPropagation();
        }, onDrag: this.onDrag, onRelease: this.onRelease, ref: function(component) {
          _this.draggable = component;
        } },
        React40.createElement("span", { className: "k-column-resizer", style: { touchAction: "none" }, draggable: false, onDoubleClick: this.onDoubleClick })
      );
    };
    return ColumnResizer3;
  }(React40.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/drag/ColumnDraggable.js
var React41 = __toESM(require_react());
var __extends26 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var ColumnDraggable2 = (
  /** @class */
  function(_super) {
    __extends26(ColumnDraggable3, _super);
    function ColumnDraggable3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.draggable = null;
      _this.onPress = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.pressHandler) {
          _this.props.pressHandler(data.event, element);
        }
      };
      _this.onDrag = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.dragHandler) {
          _this.props.dragHandler(data.event, element);
        }
      };
      _this.onRelease = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.releaseHandler) {
          _this.props.releaseHandler(data.event);
        }
      };
      return _this;
    }
    ColumnDraggable3.prototype.render = function() {
      var _this = this;
      return React41.createElement(
        Draggable,
        { onPress: this.onPress, onDrag: this.onDrag, onRelease: this.onRelease, hint: this.props.dragClue, autoScroll: { boundaryElementRef: this.props.headerRef, direction: { horizontal: true, vertical: false } }, scrollContainer: this.props.containerRef || void 0, ref: function(component) {
          _this.draggable = component;
        } },
        React41.createElement("tr", { className: "k-table-row", style: { touchAction: "none" }, role: "row", "aria-rowindex": this.props.ariaRowIndex }, this.props.children)
      );
    };
    return ColumnDraggable3;
  }(React41.PureComponent)
);

// node_modules/@progress/kendo-react-grid/dist/es/header/GridHeaderCell.js
var React43 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuWrapper.js
var React42 = __toESM(require_react());
var __extends27 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign25 = function() {
  __assign25 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign25.apply(this, arguments);
};
var __rest3 = function(s, e) {
  var t = {};
  for (var p in s)
    if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
      t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var GridColumnMenuWrapper = (
  /** @class */
  function(_super) {
    __extends27(GridColumnMenuWrapper2, _super);
    function GridColumnMenuWrapper2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.state = {
        show: false
      };
      _this._anchor = null;
      _this._content = null;
      _this.blurTimeout = void 0;
      _this.willBlur = false;
      _this.blur = function() {
        if (_this.willBlur) {
          _this.willBlur = false;
          return;
        }
        clearTimeout(_this.blurTimeout);
        _this.blurTimeout = window.setTimeout(function() {
          _this.closeMenu();
        });
      };
      _this.focus = function() {
        clearTimeout(_this.blurTimeout);
      };
      _this.anchorClick = function(event) {
        event.preventDefault();
        _this.setState({
          show: !_this.state.show
        }, function() {
          return _this.state.show && _this._content && _this._content.focus();
        });
      };
      _this.closeMenu = function() {
        _this.setState({ show: false });
      };
      _this.onAnchorMouseDown = function(event) {
        _this.willBlur = _this.state.show && event.currentTarget === _this._anchor;
      };
      return _this;
    }
    GridColumnMenuWrapper2.prototype.render = function() {
      var _this = this;
      var _a4 = this.props, ColumnMenu = _a4.columnMenu, others = __rest3(_a4, ["columnMenu"]);
      return React42.createElement(
        React42.Fragment,
        null,
        React42.createElement(
          "a",
          { className: "k-grid-header-menu k-grid-column-menu", ref: function(e) {
            return _this._anchor = e;
          }, onClick: this.anchorClick, onMouseDown: this.onAnchorMouseDown, href: "#" },
          React42.createElement(IconWrap, { name: "more-vertical", icon: moreVerticalIcon })
        ),
        React42.createElement(
          Popup,
          { anchor: this._anchor, show: this.state.show },
          React42.createElement("div", { ref: function(e) {
            return _this._content = e;
          }, className: "k-grid-columnmenu-popup", tabIndex: 0, onBlur: this.blur, onFocus: this.focus, style: { outline: "none" } }, ColumnMenu && React42.createElement(ColumnMenu, __assign25({}, others, { onCloseMenu: this.closeMenu })))
        )
      );
    };
    return GridColumnMenuWrapper2;
  }(React42.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/header/GridHeaderCell.js
var __extends28 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign26 = function() {
  __assign26 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign26.apply(this, arguments);
};
var GridHeaderCell = (
  /** @class */
  function(_super) {
    __extends28(GridHeaderCell2, _super);
    function GridHeaderCell2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridHeaderCell2.prototype.render = function() {
      var columnMenuWrapperProps = this.props.columnMenuWrapperProps;
      var clickProps = this.props.onClick ? {
        onClick: this.props.onClick
      } : {};
      var defaultRendering = React43.createElement(
        "span",
        { className: "k-cell-inner" },
        React43.createElement(
          "span",
          __assign26({ className: classNames("k-link", {
            "!k-cursor-default": !this.props.columnMenuWrapperProps.sortable
          }) }, clickProps),
          React43.createElement("span", { className: "k-column-title" }, this.props.title || this.props.field || " "),
          this.props.children
        ),
        columnMenuWrapperProps.columnMenu && React43.createElement(GridColumnMenuWrapper, __assign26({}, columnMenuWrapperProps))
      );
      if (this.props.render) {
        return this.props.render.call(void 0, defaultRendering, this.props);
      }
      return defaultRendering;
    };
    return GridHeaderCell2;
  }(React43.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/header/HeaderRow.js
var __extends29 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign27 = function() {
  __assign27 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign27.apply(this, arguments);
};
var sortSeqMap2 = {
  true: { "asc": "desc", "desc": "", "": "asc" },
  false: { "asc": "desc", "desc": "asc", "": "asc" }
};
var ariaSortMap2 = {
  "none": "none",
  "asc": "ascending",
  "desc": "descending"
};
var HeaderRow2 = (
  /** @class */
  function(_super) {
    __extends29(HeaderRow3, _super);
    function HeaderRow3(props) {
      var _this = _super.call(this, props) || this;
      _this.serviceIndex = 0;
      _this.index = -1;
      _this.cellKeyDown = function(event, column) {
        if (event.isDefaultPrevented()) {
          return;
        }
        if (event.keyCode === Keys.enter) {
          _this.cellClick(event, column);
        }
      };
      _this.cells = function(rowIndexes) {
        return rowIndexes.map(function(columnIndex) {
          var column = _this.props.columns[columnIndex];
          var sortable = _this.props.sortable && column.sortable;
          var sortIndex2 = _this.props.sort ? _this.props.sort.findIndex(function(s) {
            return s.field === column.field;
          }) : -1;
          var sortDir = sortIndex2 >= 0 ? _this.props.sort[sortIndex2].dir || "none" : "none";
          var columnMenu = column.columnMenu === null ? null : column.columnMenu || _this.props.columnMenu;
          var className = classNames({
            "k-first": column.kFirst,
            "k-filterable": Boolean(columnMenu),
            "k-table-th": true,
            "k-header": true,
            "k-grid-header-sticky": column.locked,
            "k-sorted": _this.props.sort && _this.props.sort.some(function(descriptor) {
              return descriptor.field === column.field;
            })
          }, column.headerClassName);
          var style = column.left !== void 0 ? !_this.props.isRtl ? { left: column.left, right: column.right } : { left: column.right, right: column.left } : {};
          var sortLabel = provideLocalizationService(_this).toLanguageString(sortAriaLabel2, messages2[sortAriaLabel2]);
          var ariaAttrs = column.isAccessible ? {
            ariaSort: ariaSortMap2[sortDir],
            role: "columnheader",
            ariaColumnIndex: column.ariaColumnIndex,
            ariaSelected: false,
            ariaDescription: sortable ? sortLabel : ""
          } : {
            "role": "presentation"
          };
          var key = column.declarationIndex >= 0 ? ++_this.index : --_this.serviceIndex;
          var HeaderCell2 = column.headerCell ? column.headerCell : GridHeaderCell;
          var headerCell = React44.createElement(HeaderCell2, { field: column.field, onClick: sortable && function(e) {
            return _this.cellClick(e, column);
          } || void 0, selectionChange: _this.props.selectionChange, title: column.title, selectionValue: column.headerSelectionValue, render: _this.props.cellRender, children: _this.sortIcon(sortIndex2), columnMenuWrapperProps: {
            column: {
              field: column.field,
              filter: column.filter
            },
            sortable: sortable && _this.props.sortable,
            sort: _this.props.sort,
            onSortChange: _this.props.sortChange,
            filter: _this.props.filter,
            filterable: _this.props.filterable && column.filterable,
            filterOperators: _this.props.filterOperators,
            onFilterChange: _this.props.filterChange,
            group: _this.props.group,
            groupable: _this.props.groupable,
            onGroupChange: _this.props.groupChange,
            columnMenu
          } });
          return React44.createElement(
            HeaderThElement,
            __assign27({}, ariaAttrs, { key, colSpan: column.colSpan, rowSpan: column.rowSpan, className, style, columnId: column.id, navigatable: column.navigatable, onKeyDown: sortable && function(e) {
              return _this.cellKeyDown(e, column);
            } || void 0, role: "columnheader" }),
            headerCell,
            _this.props.columnResize && _this.props.columnResize.resizable && column.resizable && React44.createElement(ColumnResizer2, { key: 2, resize: function(e, element, end) {
              return _this.props.columnResize && _this.props.columnResize.dragHandler(e, column, element, end);
            }, autofit: function(e) {
              return _this.props.columnResize && _this.props.columnResize.dblClickHandler(e, [column.id]);
            } })
          );
        });
      };
      _this.cellClick = _this.cellClick.bind(_this);
      return _this;
    }
    HeaderRow3.prototype.cellClick = function(e, column) {
      e.preventDefault();
      if (!this.props.sortChange) {
        return;
      }
      var _a4 = normalize2(this.props.sortable || false, column.sortable || false), allowUnsort = _a4.allowUnsort, mode = _a4.mode;
      var oldDescriptor = (this.props.sort || []).filter(function(d) {
        return d.field === column.field;
      })[0];
      var dir = sortSeqMap2[allowUnsort][oldDescriptor && oldDescriptor.dir || ""];
      var newDescriptor = mode === "single" ? [] : (this.props.sort || []).filter(function(d) {
        return d.field !== column.field;
      });
      if (dir !== "" && column.field) {
        newDescriptor.push({ field: column.field, dir });
      }
      this.props.sortChange(newDescriptor, e);
    };
    HeaderRow3.prototype.sortIcon = function(sortIndex2) {
      if (!this.props.sort) {
        return null;
      }
      return sortIndex2 >= 0 && [
        React44.createElement(
          "span",
          { key: 1, className: "k-sort-icon" },
          React44.createElement(IconWrap, { name: "sort-" + this.props.sort[sortIndex2].dir + "-small", icon: this.props.sort[sortIndex2].dir === "asc" ? sortAscSmallIcon : sortDescSmallIcon })
        ),
        this.props.sort.length > 1 && React44.createElement(
          "span",
          { key: 2, className: "k-sort-icon" },
          React44.createElement("span", { className: "k-sort-order" }, sortIndex2 + 1)
        )
      ];
    };
    HeaderRow3.prototype.render = function() {
      var _this = this;
      this.serviceIndex = 0;
      this.index = -1;
      return this.props.columnsMap.map(function(rowIndexes, i) {
        return _this.props.pressHandler && React44.createElement(ColumnDraggable2, { key: i, pressHandler: _this.props.pressHandler, dragHandler: _this.props.dragHandler, releaseHandler: _this.props.releaseHandler, ariaRowIndex: i + 1, dragClue: _this.props.dragClue, headerRef: _this.props.headerRef, containerRef: _this.props.containerRef }, _this.cells(rowIndexes)) || React44.createElement("tr", { className: "k-table-row", role: "row", "aria-rowindex": i + 1 }, _this.cells(rowIndexes));
      });
    };
    return HeaderRow3;
  }(React44.Component)
);
registerForLocalization(HeaderRow2);

// node_modules/@progress/kendo-react-grid/dist/es/header/FilterRow.js
var React46 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridFilterCell.js
var React45 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/filterCommon.js
var filterLogicList = [
  { text: "grid.filterAndLogic", operator: "and" },
  { text: "grid.filterOrLogic", operator: "or" }
];
var operators = {
  "text": [
    { text: "grid.filterContainsOperator", operator: "contains" },
    { text: "grid.filterNotContainsOperator", operator: "doesnotcontain" },
    { text: "grid.filterEqOperator", operator: "eq" },
    { text: "grid.filterNotEqOperator", operator: "neq" },
    { text: "grid.filterStartsWithOperator", operator: "startswith" },
    { text: "grid.filterEndsWithOperator", operator: "endswith" },
    { text: "grid.filterIsNullOperator", operator: "isnull" },
    { text: "grid.filterIsNotNullOperator", operator: "isnotnull" },
    { text: "grid.filterIsEmptyOperator", operator: "isempty" },
    { text: "grid.filterIsNotEmptyOperator", operator: "isnotempty" }
  ],
  "numeric": [
    { text: "grid.filterEqOperator", operator: "eq" },
    { text: "grid.filterNotEqOperator", operator: "neq" },
    { text: "grid.filterGteOperator", operator: "gte" },
    { text: "grid.filterGtOperator", operator: "gt" },
    { text: "grid.filterLteOperator", operator: "lte" },
    { text: "grid.filterLtOperator", operator: "lt" },
    { text: "grid.filterIsNullOperator", operator: "isnull" },
    { text: "grid.filterIsNotNullOperator", operator: "isnotnull" }
  ],
  "date": [
    { text: "grid.filterEqOperator", operator: "eq" },
    { text: "grid.filterNotEqOperator", operator: "neq" },
    { text: "grid.filterAfterOrEqualOperator", operator: "gte" },
    { text: "grid.filterAfterOperator", operator: "gt" },
    { text: "grid.filterBeforeOperator", operator: "lt" },
    { text: "grid.filterBeforeOrEqualOperator", operator: "lte" },
    { text: "grid.filterIsNullOperator", operator: "isnull" },
    { text: "grid.filterIsNotNullOperator", operator: "isnotnull" }
  ],
  "boolean": [
    { text: "grid.filterEqOperator", operator: "eq" }
  ]
};
var IsUnaryFilter = function(operator) {
  return operator === "isnull" || operator === "isnotnull" || operator === "isempty" || operator === "isnotempty";
};
var operatorMap = function(iterable, service) {
  return iterable.map(function(operator) {
    return {
      text: service.toLanguageString(operator.text, messages2[operator.text]),
      operator: operator.operator
    };
  });
};
var defaultBooleanOperator = "eq";
var booleanFilterValues = [
  { text: "grid.filterBooleanAll", operator: "" },
  { text: "grid.filterIsTrue", operator: true },
  { text: "grid.filterIsFalse", operator: false }
];
var defaultHideSecondFilter = {
  text: false,
  numeric: false,
  date: false,
  boolean: true
};
var cellInputChange = function(value, e, props) {
  var defaultOperator = getDefaultOperator(props.operators);
  var operator = props.operator;
  switch (props.filterType) {
    case "numeric":
      if (!operator || IsUnaryFilter(operator)) {
        operator = defaultOperator;
      }
      if (value === null && operator === defaultOperator) {
        operator = "";
      }
      break;
    case "date":
      if (!operator || IsUnaryFilter(operator)) {
        operator = defaultOperator;
      }
      if (value === null && operator === defaultOperator) {
        operator = "";
      }
      break;
    case "text":
      if (!operator || IsUnaryFilter(operator)) {
        operator = defaultOperator;
      }
      if (!value && operator === defaultOperator) {
        operator = "";
      }
      break;
    default:
      return;
  }
  props.onChange({ value, operator, syntheticEvent: e });
};
var getDefaultOperator = function(filterOperators, filterType) {
  if (filterType) {
    return filterOperators[filterType][0].operator;
  } else {
    return filterOperators[0].operator;
  }
};
var getFilterType = function(filterType) {
  return filterType || "text";
};
var cellBoolDropdownChange = function(event, onChange) {
  var item = event.target.value;
  onChange({
    value: item.operator,
    operator: item.operator === "" ? "" : defaultBooleanOperator,
    syntheticEvent: event.syntheticEvent
  });
};
var cellOperatorChange = function(event, currentValue, onChange) {
  var value = currentValue;
  var item = event.target.value;
  if (!event.target.state.opened) {
    return;
  }
  if (IsUnaryFilter(item.operator)) {
    value = null;
  }
  if (currentValue === null && !IsUnaryFilter(item.operator)) {
    value = void 0;
  }
  onChange({ value, operator: item.operator, syntheticEvent: event.syntheticEvent });
};

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridFilterCell.js
var __extends30 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridFilterCell = (
  /** @class */
  function(_super) {
    __extends30(GridFilterCell2, _super);
    function GridFilterCell2(props) {
      var _this = _super.call(this, props) || this;
      _this.renderOperatorEditor = function(localization) {
        if (_this.props.filterType === "boolean") {
          return;
        }
        var value = _this.props.operators.find(function(item) {
          return item.operator === _this.props.operator;
        }) || null;
        return React45.createElement(DropDownList, { size: _this.props.size, value, onChange: _this.operatorChange, className: "k-dropdown-operator", iconClassName: "k-i-filter k-icon", svgIcon: filterIcon, data: _this.props.operators, textField: "text", title: localization.toLanguageString(filterChooseOperator, messages2[filterChooseOperator]), popupSettings: {
          width: ""
        } });
      };
      _this.inputChange = _this.inputChange.bind(_this);
      _this.clear = _this.clear.bind(_this);
      _this.operatorChange = _this.operatorChange.bind(_this);
      _this.boolDropdownChange = _this.boolDropdownChange.bind(_this);
      return _this;
    }
    GridFilterCell2.prototype.render = function() {
      var _a4;
      var localizationService = provideLocalizationService(this);
      var defaultRendering = React45.createElement(
        "div",
        { className: "k-filtercell" },
        React45.createElement(
          "div",
          { className: "k-filtercell-wrapper" },
          this.filterComponent(this.props.filterType, this.props.value, this.props.booleanValues),
          React45.createElement(
            "div",
            { className: "k-filtercell-operator" },
            this.renderOperatorEditor(localizationService),
            " ",
            React45.createElement(Button, { size: this.props.size, icon: "filter-clear", svgIcon: filterClearIcon, className: classNames((_a4 = {}, _a4["k-clear-button-visible"] = Boolean(!(this.props.value === null || this.props.value === "") || this.props.operator), _a4)), title: localizationService.toLanguageString(filterClearButton, messages2[filterClearButton]), type: "button", onClick: this.clear, disabled: !(!(this.props.value === null || this.props.value === "") || this.props.operator) })
          )
        )
      );
      if (this.props.render) {
        return this.props.render.call(void 0, defaultRendering, this.props);
      }
      return defaultRendering;
    };
    GridFilterCell2.prototype.inputChange = function(value, e) {
      cellInputChange(value, e, this.props);
    };
    GridFilterCell2.prototype.operatorChange = function(event) {
      cellOperatorChange(event, this.props.value, this.props.onChange);
    };
    GridFilterCell2.prototype.boolDropdownChange = function(event) {
      cellBoolDropdownChange(event, this.props.onChange);
    };
    GridFilterCell2.prototype.clear = function(e) {
      e.preventDefault();
      this.props.onChange({ value: "", operator: "", syntheticEvent: e });
    };
    GridFilterCell2.prototype.filterComponent = function(filterType, value, booleanValues) {
      var _this = this;
      switch (filterType) {
        case "numeric":
          return React45.createElement(NumericTextBox, { size: this.props.size, value, onChange: function(e) {
            _this.inputChange(e.value, e.syntheticEvent);
          }, title: this.props.title, ariaLabel: this.props.ariaLabel });
        case "date":
          return React45.createElement(DatePicker, { size: this.props.size, value, onChange: function(e) {
            _this.inputChange(e.value, e.syntheticEvent);
          }, title: this.props.title, ariaLabel: this.props.ariaLabel });
        case "boolean":
          var noFilterSet_1 = function(filter) {
            return filter === null || filter === void 0;
          };
          return React45.createElement(DropDownList, { size: this.props.size, onChange: this.boolDropdownChange, value: booleanValues.find(function(item) {
            return item.operator === (noFilterSet_1(value) ? "" : value);
          }), data: booleanValues, textField: "text", title: this.props.title, ariaLabel: this.props.ariaLabel });
        default:
          return React45.createElement(TextBox, { size: this.props.size, value: value || "", onChange: function(e) {
            _this.inputChange(e.target.value, e.syntheticEvent);
          }, title: this.props.title, "aria-label": this.props.ariaLabel });
      }
    };
    return GridFilterCell2;
  }(React45.Component)
);
registerForLocalization(GridFilterCell);

// node_modules/@progress/kendo-react-grid/dist/es/header/FilterRow.js
var __extends31 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign28 = function() {
  __assign28 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign28.apply(this, arguments);
};
var FILTER_ROW_CLASS = "k-table-row k-filter-row";
var FilterRow = (
  /** @class */
  function(_super) {
    __extends31(FilterRow2, _super);
    function FilterRow2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    FilterRow2.prototype.headerCellClassName = function(field, locked) {
      var result = "".concat(locked ? "k-grid-header-sticky" : "");
      if (this.props.sort && this.props.sort.filter(function(descriptor) {
        return descriptor.field === field;
      }).length > 0) {
        result += " k-sorted";
      }
      return result;
    };
    FilterRow2.prototype.setFilter = function(value, operator, field, e) {
      if (!this.props.filterChange) {
        return;
      }
      var filters = [];
      if (value !== "" && value !== null || operator !== "") {
        filters.push({
          field,
          operator,
          value
        });
      }
      if (this.props.filter && this.props.filter.filters) {
        var oldFilters = this.props.filter.filters || [];
        oldFilters.forEach(function(filter) {
          var descriptor = filter;
          if (descriptor && descriptor.field !== field) {
            filters.push(descriptor);
          }
        });
      }
      var logic = this.props.filter && this.props.filter.logic ? this.props.filter.logic : "and";
      this.props.filterChange(filters.length > 0 ? { logic, filters } : null, e);
    };
    FilterRow2.prototype.render = function() {
      var _this = this;
      var intl = provideLocalizationService(this);
      var oldFilters = this.props.filter && this.props.filter.filters || [];
      var activeFilter = function(field) {
        if (field === void 0) {
          return;
        }
        var currentFilters = oldFilters.filter(function(filter) {
          return filter.field === field;
        });
        return currentFilters.length ? currentFilters[0] : void 0;
      };
      var serviceIndex = 0, index = -1;
      var filterCells = this.props.columns.filter(function(c) {
        return c.children.length === 0;
      }).map(function(column) {
        var filterType = getFilterType(column.filter);
        var currentActiveFilter = activeFilter(column.field);
        var ariaLabel = column.filterable ? intl.toLanguageString(filterAriaLabel2, messages2[filterAriaLabel2]) : void 0;
        var value = currentActiveFilter && currentActiveFilter.value;
        if (value === void 0) {
          value = filterType === "text" ? "" : null;
        }
        var filterCellProps = column.filterable && {
          render: _this.props.cellRender,
          field: column.field,
          title: column.filterTitle,
          value,
          operator: currentActiveFilter && currentActiveFilter.operator,
          operators: operatorMap(_this.props.filterOperators[filterType] || [], intl),
          booleanValues: operatorMap(booleanFilterValues, intl),
          filterType,
          ariaLabel,
          onChange: function(e) {
            _this.setFilter(e.value, e.operator, column.field, e.syntheticEvent);
          }
        };
        var key = column.declarationIndex >= 0 ? ++index : --serviceIndex;
        var ariaAttrs = {
          ariaLabel,
          ariaColumnIndex: column.ariaColumnIndex
        };
        var style = column.left !== void 0 ? !_this.props.isRtl ? { left: column.left, right: column.right } : { left: column.right, right: column.left } : {};
        return React46.createElement(HeaderThElement, __assign28({ key, columnId: tableKeyboardNavigationTools.getFilterColumnId(column.id), navigatable: column.navigatable, style, className: classNames("k-table-th", _this.headerCellClassName(column.field, column.locked) || void 0), role: "columnheader" }, ariaAttrs), filterCellProps && (column.filterCell ? React46.createElement(column.filterCell, __assign28({}, filterCellProps)) : React46.createElement(GridFilterCell, __assign28({ size: _this.props.size }, filterCellProps))));
      });
      return React46.createElement("tr", { className: FILTER_ROW_CLASS, "aria-rowindex": this.props.ariaRowIndex, role: "row" }, filterCells);
    };
    return FilterRow2;
  }(React46.Component)
);
registerForLocalization(FilterRow);

// node_modules/@progress/kendo-react-grid/dist/es/header/GroupPanel.js
var React48 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/drag/GroupingIndicator.js
var React47 = __toESM(require_react());
var __extends32 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GroupingIndicator = (
  /** @class */
  function(_super) {
    __extends32(GroupingIndicator2, _super);
    function GroupingIndicator2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.draggable = null;
      _this.onPress = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.onPress) {
          _this.props.onPress(data.event, element);
        }
      };
      _this.onDrag = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.onDrag) {
          _this.props.onDrag(data.event, element);
        }
      };
      _this.onRelease = function(data) {
        var element = _this.draggable && _this.draggable.element;
        if (element && _this.props.onRelease) {
          _this.props.onRelease(data.event);
        }
      };
      _this.sortChange = function(event) {
        event.preventDefault();
        if (_this.props.onSortChange) {
          var sort = _this.props.dir === "asc" ? "desc" : "asc";
          _this.props.onSortChange(event, sort);
        }
      };
      _this.groupRemove = function(event) {
        event.preventDefault();
        if (_this.props.onRemove) {
          _this.props.onRemove(event);
        }
      };
      return _this;
    }
    GroupingIndicator2.prototype.render = function() {
      var _this = this;
      var localizationService = provideLocalizationService(this);
      var dir = this.props.dir;
      return React47.createElement(
        Draggable,
        { onPress: this.onPress, onDrag: this.onDrag, onRelease: this.onRelease, ref: function(component) {
          _this.draggable = component;
        } },
        React47.createElement(
          "div",
          { className: "k-chip k-chip-md k-chip-solid k-chip-solid-base k-rounded-md", role: "button", style: { touchAction: "none" } },
          React47.createElement(
            "span",
            { onClick: this.sortChange },
            React47.createElement(IconWrap, { name: "sort-" + dir + "-small", icon: dir === "asc" ? sortAscSmallIcon : sortDescSmallIcon })
          ),
          React47.createElement("span", { className: "k-chip-content" }, this.props.title),
          React47.createElement(
            "span",
            { className: "k-chip-actions" },
            React47.createElement(
              "span",
              { className: "k-chip-action k-chip-remove-action", onClick: this.groupRemove, "aria-label": localizationService.toLanguageString(ungroupColumn, messages2[ungroupColumn]) },
              React47.createElement(IconWrap, { name: "x-circle", icon: xCircleIcon })
            )
          )
        )
      );
    };
    return GroupingIndicator2;
  }(React47.Component)
);
registerForLocalization(GroupingIndicator);

// node_modules/@progress/kendo-react-grid/dist/es/header/GroupPanel.js
var __extends33 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GroupPanel = (
  /** @class */
  function(_super) {
    __extends33(GroupPanel2, _super);
    function GroupPanel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onGroupRemove = function(event, index) {
        var newGroups = _this.props.group.slice();
        newGroups.splice(index, 1);
        _this.props.groupChange(newGroups, event);
      };
      _this.onGroupSortChange = function(event, index, groupDesc, dir) {
        var group = Object.assign({}, groupDesc, { dir });
        var newGroups = _this.props.group.slice();
        newGroups.splice(index, 1, group);
        _this.props.groupChange(newGroups, event);
      };
      return _this;
    }
    GroupPanel2.prototype.render = function() {
      var _this = this;
      var groupsProp = this.props.group || [];
      var groups = groupsProp.map(function(groupDesc, index) {
        return React48.createElement(GroupingIndicator, { key: index, dir: groupDesc.dir || "asc", title: _this.props.resolveTitle(groupDesc.field), onRemove: function(event) {
          _this.onGroupRemove(event, index);
        }, onSortChange: function(event, dir) {
          _this.onGroupSortChange(event, index, groupDesc, dir);
        }, onPress: _this.props.pressHandler, onDrag: _this.props.dragHandler, onRelease: _this.props.releaseHandler });
      });
      return React48.createElement(
        "div",
        { ref: this.props.refCallback, className: "k-grouping-header", role: "toolbar", "aria-label": provideLocalizationService(this).toLanguageString(groupPanelAriaLabel, messages2[groupPanelAriaLabel]) },
        React48.createElement("div", { className: "k-chip-list k-chip-list-md", role: "none" }, groups),
        React48.createElement(
          "div",
          { className: "k-grouping-drop-container" },
          !groups.length && provideLocalizationService(this).toLanguageString(groupPanelEmpty, messages2[groupPanelEmpty]),
          " "
        )
      );
    };
    return GroupPanel2;
  }(React48.Component)
);
registerForLocalization(GroupPanel);

// node_modules/@progress/kendo-react-grid/dist/es/footer/Footer.js
var React49 = __toESM(require_react());
var __extends34 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var Footer = (
  /** @class */
  function(_super) {
    __extends34(Footer2, _super);
    function Footer2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.footerWrap = null;
      _this.table = null;
      _this.scrollbarWidth = getScrollbarWidth();
      return _this;
    }
    Footer2.prototype.componentDidMount = function() {
      this.setState({});
    };
    Footer2.prototype.setScrollLeft = function(scrollLeft) {
      if (this.footerWrap) {
        this.footerWrap.scrollLeft = scrollLeft;
      }
    };
    Footer2.prototype.setWidth = function(width) {
      if (this.table) {
        this.table.style.width = width + "px";
      }
    };
    Footer2.prototype.render = function() {
      var _a4;
      var _this = this;
      if (!this.props.staticHeaders) {
        return React49.createElement("tfoot", { className: "k-table-tfoot" }, this.props.row);
      }
      return React49.createElement(
        "div",
        { className: "k-grid-footer", role: "presentation" },
        React49.createElement(
          "div",
          { ref: function(div) {
            _this.footerWrap = div;
          }, className: "k-grid-footer-wrap", style: this.scrollbarWidth ? {} : { borderWidth: 0 }, role: "presentation" },
          React49.createElement(
            "table",
            { ref: function(table) {
              _this.table = table;
            }, className: classNames("k-table k-grid-footer-table", (_a4 = {
              "k-table-md": !this.props.size
            }, _a4["k-table-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _a4), this.props.className), role: "presentation" },
            React49.createElement("colgroup", { ref: function(e) {
              _this.props.columnResize.colGroupFooter = e;
            }, role: "presentation" }, this.props.cols),
            React49.createElement("tfoot", { className: "k-table-tfoot", role: "presentation" }, this.props.row)
          )
        )
      );
    };
    return Footer2;
  }(React49.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/VirtualScroll.js
var React50 = __toESM(require_react());
var VirtualScroll = (
  /** @class */
  function() {
    function VirtualScroll2(cached) {
      var _this = this;
      this.table = null;
      this.containerHeight = 0;
      this.topCacheCount = 0;
      this.attendedSkip = 0;
      this.propsSkip = 0;
      this.total = 0;
      this.scrollableVirtual = false;
      this.realSkip = 0;
      this.pageSize = 0;
      this.PageChange = null;
      this.tableBodyRef = React50.createRef();
      this.fixedScroll = false;
      this.askedSkip = void 0;
      this.containerRef = React50.createRef();
      this.tableTransform = "";
      this.prevScrollPos = 0;
      this.tableTranslate = 0;
      this.scrollSyncing = false;
      this.reactVersion = Number.parseFloat(React50.version);
      this.topItems = function(heights, skipTopBuffer) {
        if (!_this.container || skipTopBuffer) {
          return { topItemsCount: 0, topItemsHeight: 0 };
        }
        var screenHeight = _this.container.clientHeight;
        var itemsOnScreen = Math.ceil(screenHeight / heights[0].line);
        var topItemsCount = Math.ceil((heights.length - itemsOnScreen) / 2);
        var topItemsHeight = 0;
        for (var i = 0; i < topItemsCount; i++) {
          topItemsHeight += heights[i].line + heights[i].acc;
        }
        return {
          topItemsCount,
          topItemsHeight,
          itemsNeededOnScreen: itemsOnScreen + itemsOnScreen / 2
        };
      };
      this.horizontalScrollbarHeight = function() {
        if (!_this.container) {
          return 0;
        }
        return _this.container.offsetHeight - _this.container.clientHeight;
      };
      if (cached) {
        this.topCacheCount = 4;
        this.attendedSkip = -this.topCacheCount;
      }
      this.scrollHandler = this.scrollHandler.bind(this);
    }
    Object.defineProperty(VirtualScroll2.prototype, "container", {
      get: function() {
        return this.containerRef.current;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(VirtualScroll2.prototype, "rowHeights", {
      /**
       * @return - The row heights in an array.
       */
      get: function() {
        var result = [];
        var allRows = this.tableBodyRef.current && this.tableBodyRef.current.children || [];
        var accumulate = 0;
        for (var i = 0; i < allRows.length; i++) {
          if (allRows[i].className.indexOf("k-grouping-row") > -1) {
            accumulate += allRows[i].scrollHeight;
            continue;
          }
          if (allRows[i].className.indexOf("k-detail-row") > -1) {
            result[result.length - 1].line += allRows[i].scrollHeight;
          } else {
            result.push({
              line: allRows[i].scrollHeight,
              acc: accumulate
            });
            accumulate = 0;
          }
        }
        return result;
      },
      enumerable: false,
      configurable: true
    });
    VirtualScroll2.prototype.changePage = function(skip, e) {
      this.attendedSkip = skip - this.topCacheCount;
      if (this.PageChange) {
        this.PageChange({
          skip: Math.max(0, skip - this.topCacheCount),
          take: this.pageSize
        }, e);
      }
    };
    VirtualScroll2.prototype.translate = function(dY, forceSet) {
      this.tableTranslate = dY;
      if (this.scrollableVirtual && this.table) {
        if (this.reactVersion <= 17 || forceSet) {
          this.table.style.transform = "translateY(" + dY + "px)";
        } else {
          this.tableTransform = "translateY(" + dY + "px)";
        }
      }
    };
    VirtualScroll2.prototype.reset = function() {
      this.scrollSyncing = true;
      if (this.fixedScroll) {
        return;
      }
      if (this.container) {
        this.container.scrollTop = 0;
      }
      this.translate(0, true);
    };
    VirtualScroll2.prototype.localScrollUp = function(e) {
      if (!this.container) {
        return;
      }
      var heights = this.rowHeights;
      var scrollTop = this.container.scrollTop;
      var targetTranslate = this.tableTranslate;
      var rowsCount = 0;
      var _a4 = this.topItems(heights, Boolean(this.topCacheCount)), topItemsCount = _a4.topItemsCount, topItemsHeight = _a4.topItemsHeight, itemsNeededOnScreen = _a4.itemsNeededOnScreen;
      var additionalOnTop = scrollTop - targetTranslate;
      if (additionalOnTop > topItemsHeight || heights.length <= itemsNeededOnScreen) {
        return;
      }
      while (rowsCount < this.topCacheCount + this.attendedSkip - this.realSkip + topItemsCount && this.propsSkip - rowsCount > 0 && !(targetTranslate + (heights[heights.length - 1 - rowsCount].line + heights[heights.length - 1 - rowsCount].acc) + additionalOnTop <= scrollTop)) {
        targetTranslate -= heights[heights.length - 1 - rowsCount].line + heights[heights.length - 1 - rowsCount].acc;
        rowsCount++;
      }
      if (rowsCount === 0 && this.topCacheCount === 0 && this.attendedSkip > 0) {
        targetTranslate = Math.max(targetTranslate - heights[0].line, 0);
        rowsCount = 1;
      }
      if (this.propsSkip - rowsCount <= 0 && targetTranslate > scrollTop) {
        this.translate(0);
        this.changePage(0, e);
        this.container.scrollTop = 0;
        return;
      }
      if (targetTranslate > scrollTop) {
        targetTranslate = scrollTop;
      }
      if (targetTranslate !== this.tableTranslate) {
        this.translate(Math.max(0, targetTranslate - topItemsHeight));
        var nextSkip = Math.max(0, this.propsSkip - rowsCount - topItemsCount);
        this.changePage(nextSkip, e);
      }
    };
    VirtualScroll2.prototype.localScrollDown = function(e) {
      if (!this.container) {
        return;
      }
      var heights = this.rowHeights;
      var scrollTop = this.container.scrollTop;
      var targetTranslate = this.tableTranslate;
      var rowsCount = 0;
      var _a4 = this.topItems(heights, Boolean(this.topCacheCount)), topItemsCount = _a4.topItemsCount, topItemsHeight = _a4.topItemsHeight, itemsNeededOnScreen = _a4.itemsNeededOnScreen;
      while (rowsCount < heights.length - this.topCacheCount && !(targetTranslate + heights[rowsCount].line + heights[rowsCount].acc > scrollTop)) {
        targetTranslate += heights[rowsCount].line + heights[rowsCount].acc;
        rowsCount++;
      }
      if (topItemsCount > this.propsSkip + rowsCount || heights.length <= itemsNeededOnScreen) {
        return;
      }
      if (rowsCount >= heights.length - this.topCacheCount && this.propsSkip + rowsCount >= this.total) {
        this.translate(targetTranslate - topItemsHeight);
        this.changePage(this.total - 1 - topItemsCount, e);
      } else if (targetTranslate !== this.tableTranslate && this.propsSkip + rowsCount - topItemsCount !== this.propsSkip) {
        this.translate(targetTranslate - topItemsHeight);
        this.changePage(this.propsSkip + rowsCount - topItemsCount, e);
      }
    };
    VirtualScroll2.prototype.scrollNonStrict = function(e) {
      var floatRowIndex = this.total * this.prevScrollPos / this.containerHeight;
      var rowIndex = Math.floor(floatRowIndex);
      if (rowIndex >= this.total) {
        rowIndex = this.total - 1;
      }
      var rowpercentage = Math.min(floatRowIndex - rowIndex, 1);
      var microAdjust = 0;
      var rowIndexOffset = rowIndex - this.propsSkip;
      var heights = this.rowHeights;
      if (rowIndexOffset >= 0 && rowIndexOffset <= 1) {
        microAdjust = -((heights[0].line + heights[0].acc) * rowpercentage);
      } else if (rowIndexOffset === -1) {
        microAdjust = -((heights[heights.length - 1].line + heights[heights.length - 1].acc) * rowpercentage);
      }
      var _a4 = this.topItems(heights, Boolean(this.topCacheCount)), topItemsCount = _a4.topItemsCount, topItemsHeight = _a4.topItemsHeight;
      this.translate(Math.max(0, microAdjust - topItemsHeight - this.horizontalScrollbarHeight() + this.containerHeight * floatRowIndex / this.total));
      this.changePage(rowIndex - topItemsCount, e);
    };
    VirtualScroll2.prototype.scrollHandler = function(e) {
      if (!this.scrollableVirtual) {
        return;
      }
      if (this.scrollSyncing || !this.container || !this.table) {
        this.scrollSyncing = false;
        return;
      }
      var scrollTop = this.container.scrollTop;
      var prev = this.prevScrollPos;
      this.prevScrollPos = scrollTop;
      if (this.askedSkip !== void 0) {
        this.translate(this.containerHeight * this.askedSkip / this.total);
        this.changePage(this.askedSkip, e);
        this.prevScrollPos = scrollTop;
        this.askedSkip = void 0;
        return;
      }
      if (scrollTop - prev < 0 && scrollTop > this.tableTranslate - this.table.scrollHeight / 10) {
        this.localScrollUp(e);
      } else if (scrollTop - prev > 0 && scrollTop < this.tableTranslate + this.table.scrollHeight * 2 / 3) {
        this.localScrollDown(e);
      } else {
        this.scrollNonStrict(e);
      }
      this.prevScrollPos = scrollTop;
    };
    return VirtualScroll2;
  }()
);

// node_modules/@progress/kendo-react-grid/dist/es/VirtualScrollFixed.js
var React51 = __toESM(require_react());
var RowHeightService = (
  /** @class */
  function() {
    function RowHeightService2(total, rowHeight, detailRowHeight, data) {
      if (total === void 0) {
        total = 0;
      }
      this.total = total;
      this.offsets = [];
      this.heights = [];
      var agg = 0;
      for (var idx = 0; idx < total; idx++) {
        this.offsets.push(agg);
        var currHeight = data && data[idx].expanded && data[idx].rowType === "data" ? detailRowHeight : rowHeight;
        agg += currHeight;
        this.heights.push(currHeight);
      }
    }
    RowHeightService2.prototype.height = function(rowIndex) {
      return this.heights[rowIndex];
    };
    RowHeightService2.prototype.index = function(position) {
      if (position < 0) {
        return void 0;
      }
      var result = this.offsets.reduce(function(prev, current, idx) {
        if (prev !== void 0) {
          return prev;
        } else if (current === position) {
          return idx;
        } else if (current > position) {
          return idx - 1;
        }
        return void 0;
      }, void 0);
      return result === void 0 ? this.total - 1 : result;
    };
    RowHeightService2.prototype.offset = function(rowIndex) {
      return this.offsets[rowIndex];
    };
    RowHeightService2.prototype.totalHeight = function() {
      var lastOffset = this.offsets[this.offsets.length - 1];
      var lastHeight = this.heights[this.heights.length - 1];
      return lastOffset + lastHeight;
    };
    return RowHeightService2;
  }()
);
var VirtualScrollFixed = (
  /** @class */
  function() {
    function VirtualScrollFixed2(_cached) {
      this.table = null;
      this.containerHeight = 0;
      this.topCacheCount = 0;
      this.attendedSkip = 0;
      this.propsSkip = 0;
      this.total = 0;
      this.scrollableVirtual = false;
      this.realSkip = 0;
      this.pageSize = 0;
      this.PageChange = null;
      this.tableBodyRef = React51.createRef();
      this.fixedScroll = false;
      this.askedSkip = void 0;
      this.containerRef = React51.createRef();
      this.tableTransform = "";
      this.scrollSyncing = false;
      this.lastLoaded = 0;
      this.firstLoaded = 0;
      this.lastScrollTop = 0;
      this.reactVersion = Number.parseFloat(React51.version);
      this.firstLoaded = this.pageSize;
      this.lastLoaded = this.realSkip + this.pageSize;
      this.scrollHandler = this.scrollHandler.bind(this);
    }
    Object.defineProperty(VirtualScrollFixed2.prototype, "container", {
      get: function() {
        return this.containerRef.current;
      },
      enumerable: false,
      configurable: true
    });
    VirtualScrollFixed2.prototype.translate = function(dY, forceSet) {
      if (this.scrollableVirtual && this.table) {
        if (this.reactVersion <= 17 || forceSet) {
          this.table.style.transform = "translateY(" + dY + "px)";
        } else {
          this.tableTransform = "translateY(" + dY + "px)";
        }
      }
    };
    VirtualScrollFixed2.prototype.changePage = function(skip, e) {
      if (this.PageChange) {
        this.PageChange({ skip: Math.max(0, skip), take: this.pageSize }, e);
      }
    };
    VirtualScrollFixed2.prototype.reset = function() {
      this.scrollSyncing = true;
      if (this.fixedScroll) {
        return;
      }
      if (this.container) {
        this.container.scrollTop = 0;
      }
      this.translate(0, true);
    };
    VirtualScrollFixed2.prototype.scrollHandler = function(e) {
      if (!this.scrollableVirtual || !this.container || !this.table || !this.rowHeightService || !this.containerRef.current) {
        return;
      }
      if (this.scrollSyncing) {
        this.scrollSyncing = false;
        return;
      }
      var scrollTop = this.container.scrollTop;
      var up = this.lastScrollTop >= scrollTop;
      var down = !up;
      this.lastScrollTop = scrollTop;
      var firstItemIndex = this.rowHeightService.index(scrollTop);
      var firstItemOffset = this.rowHeightService.offset(firstItemIndex);
      var offsetHeight = this.containerRef.current.offsetHeight;
      var lastItemIndex = this.rowHeightService.index(scrollTop + offsetHeight);
      if (down && lastItemIndex >= this.lastLoaded && this.lastLoaded < this.total) {
        var overflow = firstItemIndex + this.pageSize - this.total;
        if (overflow > 0) {
          firstItemIndex = firstItemIndex - overflow;
          firstItemOffset = this.rowHeightService.offset(firstItemIndex);
        }
        this.firstLoaded = firstItemIndex;
        this.translate(firstItemOffset);
        var nextTake = this.firstLoaded + this.pageSize;
        this.lastLoaded = Math.min(nextTake, this.total);
        this.changePage(this.firstLoaded, e);
      } else if (up && firstItemIndex < this.firstLoaded) {
        var nonVisibleBuffer = Math.floor(this.pageSize * 0.3);
        this.firstLoaded = Math.max(firstItemIndex - nonVisibleBuffer, 0);
        this.translate(this.rowHeightService.offset(this.firstLoaded));
        this.lastLoaded = Math.min(this.firstLoaded + this.pageSize, this.total);
        this.changePage(this.firstLoaded, e);
      }
    };
    return VirtualScrollFixed2;
  }()
);

// node_modules/@progress/kendo-react-grid/dist/es/drag/ColumnResize.js
var OFFSET_ROUND = 1;
var ColumnResize2 = (
  /** @class */
  function() {
    function ColumnResize3(triggerResize) {
      var _this = this;
      this.columns = [];
      this.resizable = false;
      this.isRtl = false;
      this.setIsRtl = function(isRtl2) {
        _this.isRtl = isRtl2;
      };
      this.dragHandler = function(event, column, dragCue, end) {
        var e = event.originalEvent;
        if (!end) {
          e.preventDefault();
          e.stopPropagation();
          e.stopImmediatePropagation();
        }
        var tdElement = dragCue.parentElement;
        if (!tdElement || !tdElement.parentElement) {
          return;
        }
        var oldWidth = tdElement.clientWidth;
        var newWidth = oldWidth;
        if (_this.isRtl) {
          newWidth += dragCue.getBoundingClientRect().right - dragCue.offsetWidth / 2 - event.clientX;
        } else {
          newWidth += event.clientX - dragCue.getBoundingClientRect().left - dragCue.offsetWidth / 2;
        }
        if (!end && Math.abs(newWidth - oldWidth) < 1) {
          return;
        }
        _this.fixateInitialWidths(tdElement.parentElement.clientWidth);
        _this.setWidths(column, Math.floor(newWidth) / oldWidth);
        var index = _this.columns.filter(function(c) {
          return !c.children.length;
        }).findIndex(function(c) {
          return c.id === column.id;
        });
        _this.onResize(index, oldWidth, newWidth, e, end, column.id);
      };
      this.dblClickHandler = function(event, columnIds) {
        var columns = _this.columns.filter(function(c) {
          return columnIds.indexOf(c.id) > -1;
        });
        if (!_this.colGroupMain || !columns.length) {
          return;
        }
        var leafsForAdjust = {};
        var stack = columns;
        while (stack.length > 0) {
          var currentCol = stack.pop();
          if (!currentCol) {
            break;
          }
          if (currentCol.children.length) {
            stack.push.apply(stack, currentCol.children);
          } else {
            leafsForAdjust[currentCol.id] = currentCol;
          }
        }
        var leafs = _this.columns.filter(function(c) {
          return !c.children.length;
        });
        var indexesForAdjust = [];
        leafs.forEach(function(currentColumn, index) {
          if (leafsForAdjust[currentColumn.id]) {
            indexesForAdjust.push(index);
          }
        });
        var tables = [_this.colGroupMain.parentElement];
        var colgroups = [_this.colGroupMain];
        if (_this.colGroupHeader) {
          tables.push(_this.colGroupHeader.parentElement);
          colgroups.push(_this.colGroupHeader);
        }
        if (_this.colGroupFooter) {
          tables.push(_this.colGroupFooter.parentElement);
          colgroups.push(_this.colGroupFooter);
        }
        tables.forEach(function(table) {
          return table.classList.add("k-autofitting");
        });
        var maxWidths = [];
        colgroups.forEach(function(colgroup) {
          indexesForAdjust.forEach(function(columnIndex) {
            if (colgroup.children[columnIndex]) {
              colgroup.children[columnIndex].width = "";
              maxWidths[columnIndex] = Math.max(maxWidths[columnIndex] || 0, colgroup.children[columnIndex].offsetWidth + OFFSET_ROUND);
            }
          });
        });
        colgroups.forEach(function(colgroup) {
          indexesForAdjust.forEach(function(columnIndex) {
            if (colgroup.children[columnIndex]) {
              colgroup.children[columnIndex].width = maxWidths[columnIndex] + "px";
              leafs[columnIndex].width = maxWidths[columnIndex];
            }
          });
        });
        tables.forEach(function(table) {
          return table.classList.remove("k-autofitting");
        });
        _this.onResize(indexesForAdjust[0], 0, 0, event, true, columnIds[0]);
      };
      this.updateColElements = function(affectedColumns) {
        var leafColumns = _this.columns.filter(function(c) {
          return !c.children.length;
        });
        var difference = 1e-10;
        var _loop_1 = function(i2) {
          var currentColumn = affectedColumns[i2];
          var colIndex = leafColumns.findIndex(function(c) {
            return c.id === currentColumn.id;
          });
          var currentColumnFloatWidth = parseFloat((currentColumn.width || 0).toString());
          var currentColumnFloorWidth = Math.floor(currentColumnFloatWidth);
          difference += currentColumnFloatWidth - currentColumnFloorWidth;
          var currentWidth = currentColumnFloorWidth + Math.floor(difference);
          difference -= Math.floor(difference);
          if (_this.colGroupMain && _this.colGroupMain.children[colIndex]) {
            _this.colGroupMain.children[colIndex].width = currentWidth + "px";
          }
          if (_this.colGroupHeader && _this.colGroupHeader.children[colIndex]) {
            _this.colGroupHeader.children[colIndex].width = currentWidth + "px";
          }
          if (_this.colGroupFooter && _this.colGroupFooter.children[colIndex]) {
            _this.colGroupFooter.children[colIndex].width = currentWidth + "px";
          }
        };
        for (var i = 0; i < affectedColumns.length; i++) {
          _loop_1(i);
        }
      };
      this.onResize = triggerResize;
    }
    ColumnResize3.prototype.fixateInitialWidths = function(width) {
      var columns = this.columns.filter(function(c) {
        return !c.children.length;
      });
      var remainingCount = 0;
      var cols = this.colGroupMain ? this.colGroupMain.children : [];
      for (var i = 0; i < cols.length; i++) {
        if (cols[i].width) {
          width -= parseFloat(cols[i].width);
        } else {
          remainingCount++;
        }
      }
      if (remainingCount === 0) {
        return;
      }
      var perCol = Math.floor(width / remainingCount);
      for (var i = 0; i < cols.length; i++) {
        var col = cols[i];
        if (!col.width) {
          col.width = perCol;
          columns[i].width = perCol.toString();
          if (this.colGroupHeader) {
            this.colGroupHeader.children[i].width = perCol;
          }
          if (this.colGroupFooter) {
            this.colGroupFooter.children[i].width = perCol;
          }
        }
      }
    };
    ColumnResize3.prototype.setWidths = function(column, coef) {
      var indexInOriginal = this.columns.findIndex(function(c) {
        return c.id === column.id;
      });
      var toAdjust = [];
      var more = column.children.length;
      for (var i = indexInOriginal + 1; more > 0 && i < this.columns.length; i++, more--) {
        var cc = this.columns[i];
        if (!cc.children.length) {
          toAdjust.push(cc);
        } else {
          more += cc.children.length;
        }
      }
      if (toAdjust.length === 0) {
        toAdjust.push(column);
      }
      toAdjust.forEach(function(colToAdjust) {
        var targetWidth = colToAdjust.width ? parseFloat(colToAdjust.width.toString()) * coef : 0;
        var min = colToAdjust.minResizableWidth === void 0 ? 10 : colToAdjust.minResizableWidth;
        if (targetWidth < min) {
          targetWidth = min;
        }
        colToAdjust.width = targetWidth;
      });
      this.updateColElements(toAdjust);
      return toAdjust;
    };
    return ColumnResize3;
  }()
);

// node_modules/@progress/kendo-react-grid/dist/es/drag/CommonDragLogic.js
var React52 = __toESM(require_react());
var CommonDragLogic2 = (
  /** @class */
  function() {
    function CommonDragLogic3(columnReorder, groupReorder, columnToGroup) {
      var _this = this;
      this.refDragElementClue = React52.createRef();
      this.refDropElementClue = React52.createRef();
      this.columns = [];
      this.reorderable = false;
      this.groupable = false;
      this.startColumn = -1;
      this.startGroup = -1;
      this.currentColumn = -1;
      this.currentGroup = -1;
      this.groupPanelDivElement = null;
      this.refGroupPanelDiv = function(e) {
        _this.groupPanelDivElement = e && e.children ? e.children[0] : e;
      };
      this.pressHandler = function(event, element) {
        var startColumn = _this.getColumnIndex(event, element);
        _this.startGroup = _this.getGroupIndex(event);
        if (startColumn >= 0) {
          var col = _this.columns[startColumn];
          if (col.reorderable && _this.reorderable || col.groupable && _this.groupable) {
            _this.startColumn = startColumn;
          }
        }
      };
      this.dragHandler = function(event, element) {
        if (!event.isTouch) {
          event.originalEvent.preventDefault();
        }
        event.originalEvent.stopPropagation();
        if (_this.startColumn === -1 && _this.startGroup === -1) {
          return;
        }
        _this.currentColumn = _this.getColumnIndex(event, element);
        var groupPanelChildren = _this.groupPanelDivElement && _this.groupPanelDivElement.children;
        _this.currentGroup = _this.isTargetGroupingContainer(event) ? groupPanelChildren && groupPanelChildren.length ? groupPanelChildren.length : 0 : _this.getGroupIndex(event);
        var invalidIndex = !_this.isValid();
        if (invalidIndex) {
          _this.currentColumn = -1;
          _this.currentGroup = -1;
        }
        var targetElement = _this.currentColumn >= 0 ? element.children[_this.columns[_this.currentColumn].index] : _this.isTargetGroupingContainer(event) ? event.originalEvent.target : _this.groupPanelDivElement && _this.groupPanelDivElement.children[_this.currentGroup];
        _this.updateDragElementClue(event, element, targetElement, invalidIndex);
        _this.updateDropElementClue(event, element, targetElement, invalidIndex);
      };
      this.releaseHandler = function(event) {
        var prevColumnIndex = _this.startColumn;
        var nextColumnIndex = _this.currentColumn;
        var prevGroupIndex = _this.startGroup;
        var nextGroupIndex = _this.currentGroup;
        event.originalEvent.preventDefault();
        if (_this.dropElementClue) {
          _this.dropElementClue.setState({ visible: false });
        }
        if (_this.dragElementClue) {
          _this.dragElementClue.setState({ visible: false });
        }
        var isValid = _this.isValid();
        _this.startColumn = _this.startGroup = _this.currentColumn = _this.currentGroup = -1;
        if (!isValid) {
          return;
        }
        if (prevColumnIndex >= 0 && nextColumnIndex >= 0) {
          _this.columnReorder(prevColumnIndex, nextColumnIndex, event.originalEvent);
        } else if (prevGroupIndex >= 0 && nextGroupIndex >= 0) {
          _this.groupReorder(prevGroupIndex, nextGroupIndex, event.originalEvent);
        } else if (prevColumnIndex >= 0 && nextGroupIndex >= 0) {
          _this.columnToGroup(prevColumnIndex, nextGroupIndex, event.originalEvent);
        }
      };
      this.columnReorder = columnReorder;
      this.groupReorder = groupReorder;
      this.columnToGroup = columnToGroup;
    }
    Object.defineProperty(CommonDragLogic3.prototype, "dragClueRef", {
      get: function() {
        return this.refDragElementClue;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(CommonDragLogic3.prototype, "dropClueRef", {
      get: function() {
        return this.refDropElementClue;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(CommonDragLogic3.prototype, "dragElementClue", {
      get: function() {
        return this.refDragElementClue.current;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(CommonDragLogic3.prototype, "dropElementClue", {
      get: function() {
        return this.refDropElementClue.current;
      },
      enumerable: false,
      configurable: true
    });
    CommonDragLogic3.prototype.getColumnIndex = function(event, parent) {
      if (!parent || parent.parentElement === this.groupPanelDivElement) {
        return -1;
      }
      var index = getIndex(event, parent);
      if (index === -1) {
        return -1;
      }
      var _loop_1 = function(i2) {
        if (parent.parentNode.children[i2] === parent) {
          return { value: this_1.columns.findIndex(function(c) {
            return c.index === index && c.depth === i2;
          }) };
        }
      };
      var this_1 = this;
      for (var i = 0; i < parent.parentNode.children.length; i++) {
        var state_1 = _loop_1(i);
        if (typeof state_1 === "object")
          return state_1.value;
      }
      return -1;
    };
    CommonDragLogic3.prototype.isTargetGroupingContainer = function(event) {
      var target = event.originalEvent.target;
      return target.className.indexOf("k-grouping-drop-container") !== -1;
    };
    CommonDragLogic3.prototype.getGroupIndex = function(event) {
      return getIndex(event, this.groupPanelDivElement);
    };
    CommonDragLogic3.prototype.isValid = function() {
      if (this.startGroup >= 0) {
        return this.currentGroup >= 0 && this.currentGroup !== this.startGroup;
      }
      if (this.startColumn === -1) {
        return false;
      }
      if (this.currentGroup >= 0) {
        return this.columns[this.startColumn].groupable === true && this.groupable === true;
      }
      return this.reorderable === true && this.currentColumn >= 0 && this.currentColumn !== this.startColumn && this.columns[this.startColumn].reorderable === true && this.columns[this.currentColumn].parentIndex === this.columns[this.startColumn].parentIndex;
    };
    CommonDragLogic3.prototype.updateDragElementClue = function(event, element, targetElement, invalidIndex) {
      if (!this.dragElementClue) {
        return;
      }
      var innerText = this.startColumn >= 0 ? element.children[this.columns[this.startColumn].index].innerText : element.innerText;
      this.dragElementClue.setState({
        visible: true,
        top: event.pageY + 10,
        left: event.pageX,
        innerText,
        status: invalidIndex || !targetElement ? "k-i-cancel" : "k-i-plus"
      });
    };
    CommonDragLogic3.prototype.updateDropElementClue = function(event, element, targetElement, invalidIndex) {
      if (!this.dropElementClue) {
        return;
      }
      if (invalidIndex || !targetElement) {
        this.dropElementClue.setState({ visible: false });
        return;
      }
      var rect = targetElement.getBoundingClientRect();
      var groupElement = targetElement.closest(".k-grouping-header");
      var rectParent = (groupElement || targetElement).getBoundingClientRect();
      var left = rect.left + event.pageX - event.clientX - 6;
      if (!this.isTargetGroupingContainer(event) && (this.currentColumn > this.startColumn || this.currentGroup > this.startGroup && this.startGroup !== -1)) {
        left += rect.width;
      }
      var top = rectParent.top + event.pageY - event.clientY;
      this.dropElementClue.setState({
        visible: true,
        top,
        left,
        height: this.currentColumn >= 0 ? element.clientHeight : rectParent.height
      });
    };
    return CommonDragLogic3;
  }()
);

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridCell.js
var React53 = __toESM(require_react());
var __assign29 = function() {
  __assign29 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign29.apply(this, arguments);
};
var GridCell = function(props) {
  var _a4;
  var defaultRendering = null;
  var intl = useInternationalization();
  var navigationAttributes = useTableKeyboardNavigation(props.id);
  var onContextMenu = React53.useCallback(function(event) {
    if (props.onContextMenu) {
      props.onContextMenu.call(void 0, event, props.dataItem, props.field);
    }
  }, [props.onContextMenu, props.dataItem, props.field]);
  if (props.rowType === "groupFooter") {
    defaultRendering = React53.createElement("td", { onContextMenu, className: props.className });
  } else if (props.field !== void 0 && props.rowType !== "groupHeader") {
    var data = getNestedValue(props.field, props.dataItem);
    var dataAsString = "";
    if (data !== void 0 && data !== null) {
      dataAsString = props.format ? intl.format(props.format, data) : data.toString();
    }
    var className = classNames("k-table-td", props.className, { "k-selected": props.isSelected });
    defaultRendering = React53.createElement("td", __assign29({ onContextMenu, colSpan: props.colSpan, style: props.style, className, role: "gridcell", "aria-colindex": props.ariaColumnIndex, "aria-selected": props.isSelected }, (_a4 = {}, _a4[GRID_COL_INDEX_ATTRIBUTE] = props.columnIndex, _a4), navigationAttributes), dataAsString);
  }
  return props.render ? props.render.call(void 0, defaultRendering, props) : defaultRendering;
};

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridGroupCell.js
var React54 = __toESM(require_react());
var __assign30 = function() {
  __assign30 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign30.apply(this, arguments);
};
var GridGroupCell = function(props) {
  var defaultRendering = null;
  var columnIndex = props.columnIndex, level = props.level, columnsCount = props.columnsCount, rowType = props.rowType, dataItem = props.dataItem, field = props.field, onChange = props.onChange, expanded = props.expanded, render = props.render, dataIndex = props.dataIndex, ariaColumnIndex = props.ariaColumnIndex;
  var definedField = field || "";
  var navigationAttributes = useTableKeyboardNavigation(props.id);
  var localization = useLocalization();
  var onKeyDownHandler = React54.useCallback(function(event) {
    if (event.isDefaultPrevented()) {
      return;
    }
    if (event.keyCode === Keys.enter && onChange) {
      event.preventDefault();
      onChange({
        dataItem,
        dataIndex,
        syntheticEvent: event,
        field: void 0,
        value: !expanded
      });
    }
  }, [expanded, onChange, dataItem, dataIndex]);
  var onContextMenu = React54.useCallback(function(event) {
    if (props.onContextMenu) {
      props.onContextMenu.call(void 0, event, props.dataItem, props.field);
    }
  }, [props.onContextMenu, props.dataItem, props.field]);
  var renderCellCollapse = function(colspan, styles) {
    var baseMessage = expanded ? groupCollapse : groupExpand;
    var message = localization.toLanguageString(baseMessage, messages2[baseMessage]);
    return React54.createElement(
      "td",
      __assign30({ className: "k-table-td", style: __assign30(__assign30({}, props.style), styles), colSpan: colspan, key: "g-colspan", role: "gridcell", "aria-selected": false, "aria-expanded": expanded, "aria-colindex": ariaColumnIndex, onKeyDown: onKeyDownHandler, onContextMenu }, navigationAttributes),
      React54.createElement(
        "p",
        { className: "k-reset" },
        React54.createElement(
          "a",
          { onClick: function(e) {
            e.preventDefault();
            if (onChange) {
              onChange({
                dataItem,
                dataIndex,
                syntheticEvent: e,
                field: void 0,
                value: !expanded
              });
            }
          }, href: "#", tabIndex: -1, "aria-label": message },
          React54.createElement(IconWrap, { name: expanded ? "caret-alt-down" : "caret-alt-right", icon: expanded ? caretAltDownIcon : caretAltRightIcon })
        ),
        dataItem[definedField] instanceof Date && dataItem[definedField].toString ? dataItem[definedField].toString() : dataItem[definedField]
      )
    );
  };
  if (columnIndex === void 0 || level === void 0 || columnIndex < level || columnsCount === void 0 || rowType !== "groupHeader" || dataItem[definedField] === void 0) {
    defaultRendering = React54.createElement("td", { style: props.style, key: "g" + columnIndex, className: classNames("k-table-td", "k-group-cell", { "k-grid-content-sticky": props.locked }), role: "gridcell", onContextMenu });
  } else if (columnIndex <= level && !props.locked) {
    defaultRendering = renderCellCollapse(columnsCount - columnIndex, {});
  } else if (columnIndex <= level && props.locked) {
    defaultRendering = React54.createElement(
      React54.Fragment,
      null,
      renderCellCollapse(0, { position: "sticky", zIndex: 2 }),
      React54.createElement("td", { className: "k-table-td", role: "gridcell", colSpan: columnsCount - columnIndex, style: { borderLeftWidth: 0, borderRightWidth: 0 }, onContextMenu })
    );
  }
  return render ? render.call(void 0, defaultRendering, props) : defaultRendering;
};

// node_modules/@progress/kendo-react-grid/dist/es/rows/GridRow.js
var React55 = __toESM(require_react());
var __extends35 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign31 = function() {
  __assign31 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign31.apply(this, arguments);
};
var GridRow = (
  /** @class */
  function(_super) {
    __extends35(GridRow2, _super);
    function GridRow2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridRow2.prototype.render = function() {
      var _a4;
      var className = classNames("k-table-row", {
        "k-grouping-row": this.props.rowType === "groupHeader",
        "k-group-footer": this.props.rowType === "groupFooter",
        "k-master-row": this.props.rowType !== "groupHeader" && this.props.rowType !== "groupFooter",
        "k-table-alt-row k-alt": this.props.rowType !== "groupHeader" && this.props.rowType !== "groupFooter" && this.props.isAltRow,
        "k-selected": this.props.isSelected,
        "k-grid-edit-row": this.props.isInEdit
      });
      var defaultRendering = React55.createElement("tr", __assign31({ id: this.props.id, onClick: this.props.onClick, onDoubleClick: this.props.onDoubleClick, className, style: {
        height: this.props.rowHeight ? this.props.rowHeight + "px" : "",
        visibility: this.props.isHidden ? "hidden" : ""
      }, role: "row", "aria-rowindex": this.props.ariaRowIndex, "absolute-row-index": this.props.absoluteRowIndex }, (_a4 = {}, _a4[GRID_ROW_INDEX_ATTRIBUTE] = this.props.rowType === "data" ? this.props.dataIndex : void 0, _a4)), this.props.children);
      return this.props.render ? this.props.render.call(void 0, defaultRendering, this.props) : defaultRendering;
    };
    return GridRow2;
  }(React55.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/header/GridHeaderSelectionCell.js
var React56 = __toESM(require_react());
var __extends36 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign32 = function() {
  __assign32 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign32.apply(this, arguments);
};
var GridHeaderSelectionCell = (
  /** @class */
  function(_super) {
    __extends36(GridHeaderSelectionCell2, _super);
    function GridHeaderSelectionCell2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this._inputId = guid();
      return _this;
    }
    GridHeaderSelectionCell2.prototype.render = function() {
      var _this = this;
      var columnMenuWrapperProps = this.props.columnMenuWrapperProps;
      var defaultRendering = [
        React56.createElement("input", { key: 0, checked: this.props.selectionValue, id: this._inputId, type: "checkbox", className: "k-checkbox k-checkbox-md k-rounded-md", onChange: function(e) {
          return _this.props.selectionChange({
            field: _this.props.field,
            syntheticEvent: e
          });
        } }),
        React56.createElement("label", { key: 1, className: "k-checkbox-label", htmlFor: this._inputId }),
        columnMenuWrapperProps.columnMenu && React56.createElement(GridColumnMenuWrapper, __assign32({}, columnMenuWrapperProps))
      ];
      return this.props.render ? this.props.render.call(void 0, defaultRendering, this.props) : defaultRendering;
    };
    return GridHeaderSelectionCell2;
  }(React56.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/GridNoRecords.js
var React57 = __toESM(require_react());
var __extends37 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridNoRecords = (
  /** @class */
  function(_super) {
    __extends37(GridNoRecords2, _super);
    function GridNoRecords2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridNoRecords2.prototype.render = function() {
      return this.props.children || provideLocalizationService(this).toLanguageString(noRecords, messages2[noRecords]);
    };
    GridNoRecords2.displayName = "KendoReactGridNoRecords";
    return GridNoRecords2;
  }(React57.Component)
);
registerForLocalization(GridNoRecords);

// node_modules/@progress/kendo-react-grid/dist/es/footer/FooterRow.js
var React58 = __toESM(require_react());
var __extends38 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign33 = function() {
  __assign33 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign33.apply(this, arguments);
};
var FooterRow = (
  /** @class */
  function(_super) {
    __extends38(FooterRow2, _super);
    function FooterRow2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this._element = null;
      _this.renderCell = function(column, index) {
        var tdProps = {
          colSpan: column.colSpan !== 1 ? column.colSpan : void 0,
          style: column.left !== void 0 ? !_this.props.isRtl ? { left: column.left, right: column.right } : { left: column.right, right: column.left } : {},
          className: classNames("k-table-td", column.locked && column.left !== void 0 ? "k-grid-footer-sticky" : "")
        };
        var cellProps = __assign33({ field: column.field, ariaColumnIndex: column.ariaColumnIndex }, tdProps);
        return column.footerCell && React58.createElement(column.footerCell, __assign33({}, cellProps, { key: index })) || React58.createElement("td", __assign33({ key: index }, tdProps, { role: "columnfooter", "aria-selected": false, "aria-colindex": column.ariaColumnIndex }));
      };
      return _this;
    }
    Object.defineProperty(FooterRow2.prototype, "element", {
      get: function() {
        return this._element;
      },
      enumerable: false,
      configurable: true
    });
    FooterRow2.prototype.render = function() {
      return React58.createElement("tr", { className: "k-table-row", role: "row", "aria-rowindex": this.props.ariaRowIndex }, footerColumns(this.props.columns).map(this.renderCell));
    };
    return FooterRow2;
  }(React58.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/paging/GridPagerSettings.js
var normalizeSettings = function(_a4) {
  var _b2 = _a4.buttonCount, buttonCount = _b2 === void 0 ? 10 : _b2, _c2 = _a4.info, info = _c2 === void 0 ? true : _c2, _d = _a4.type, type = _d === void 0 ? "numeric" : _d, _e = _a4.pageSizes, pageSizes = _e === void 0 ? void 0 : _e, _f = _a4.previousNext, previousNext = _f === void 0 ? true : _f, _g = _a4.responsive, responsive = _g === void 0 ? true : _g, _h = _a4.pageSizeValue, pageSizeValue = _h === void 0 ? void 0 : _h;
  return {
    buttonCount,
    info,
    pageSizes: pageSizes === true ? [5, 10, 20] : pageSizes || void 0,
    previousNext,
    responsive,
    type,
    pageSizeValue
  };
};
var normalize3 = function(settings5) {
  return normalizeSettings(settings5 === true ? {} : settings5);
};

// node_modules/@progress/kendo-react-grid/dist/es/package-metadata.js
var packageMetadata2 = {
  name: "@progress/kendo-react-grid",
  productName: "KendoReact",
  productCodes: ["KENDOUIREACT", "KENDOUICOMPLETE"],
  publishDate: 1680172407,
  version: "",
  licensingDocsUrl: "https://www.telerik.com/kendo-react-ui/my-license/?utm_medium=product&utm_source=kendoreact&utm_campaign=kendo-ui-react-purchase-license-keys-warning"
};

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridDetailCell.js
var React59 = __toESM(require_react());
var __assign34 = function() {
  __assign34 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign34.apply(this, arguments);
};
var GridDetailCell = function(props) {
  var colSpan = props.colSpan, ariaColIndex = props.ariaColIndex, dataItem = props.dataItem, dataIndex = props.dataIndex, id = props.id;
  var navigationAttributes = useTableKeyboardNavigation(id);
  var onContextMenu = React59.useCallback(function(event) {
    if (props.onContextMenu) {
      props.onContextMenu.call(void 0, event, props.dataItem);
    }
  }, [props.onContextMenu, props.dataItem]);
  return React59.createElement(
    "td",
    __assign34({ onContextMenu, className: "k-table-td k-detail-cell", colSpan, "aria-colindex": ariaColIndex, role: "gridcell" }, navigationAttributes),
    React59.createElement(props.detail, { dataItem, dataIndex })
  );
};

// node_modules/@progress/kendo-react-grid/dist/es/cells/GridDetailHierarchyCell.js
var React60 = __toESM(require_react());
var __assign35 = function() {
  __assign35 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign35.apply(this, arguments);
};
var GridDetailHierarchyCell = function(props) {
  var navigationAttributes = useTableKeyboardNavigation(props.id);
  return React60.createElement("td", __assign35({ className: "k-table-td k-hierarchy-cell" }, navigationAttributes, { role: "gridcell" }));
};

// node_modules/@progress/kendo-react-grid/dist/es/Grid.js
var __extends39 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign36 = function() {
  __assign36 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign36.apply(this, arguments);
};
var __spreadArray12 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var Grid = (
  /** @class */
  function(_super) {
    __extends39(Grid2, _super);
    function Grid2(props) {
      var _this = _super.call(this, props) || this;
      _this._columns = [];
      _this._columnsMap = [[]];
      _this._columnsMutations = 0;
      _this.contextStateRef = { current: void 0 };
      _this.navigationStateRef = { current: void 0 };
      _this._data = [];
      _this._slicedData = void 0;
      _this.wrapperScrollTop = 0;
      _this.headerRef = React61.createRef();
      _this.headerElementRef = React61.createRef();
      _this._element = null;
      _this.tableElement = null;
      _this.containerRef = React61.createRef();
      _this.tableBodyRef = React61.createRef();
      _this._footer = null;
      _this.forceUpdateTimeout = void 0;
      _this.isRtl = false;
      _this._gridId = guid();
      _this.observer = null;
      _this.handleIntersection = function(entries) {
        var options = { rowIndex: _this.rowIndex };
        entries.forEach(function(entry) {
          if (!entry.isIntersecting) {
            _this.setCurrentOnTop(options);
          }
        });
      };
      _this.setCurrentOnTop = function(options) {
        if (!_this.vs.container || _this.props.scrollable === "none") {
          return;
        }
        if (_this.observer) {
          _this.observer.disconnect();
        }
        var rowIndex = options.rowIndex;
        if (_this.props.scrollable === "virtual") {
          _this.vs.askedSkip = rowIndex;
          _this.vs.container.scroll(0, Math.round(_this.vs.askedSkip / _this.vs.total * _this.vs.container.scrollHeight));
        } else if (_this.element) {
          var selectedRow = rowIndex < 1 ? _this.element.querySelector("tbody > tr:nth-child(".concat(1, ")")) : _this.element.querySelector("tbody > tr:nth-child(".concat(rowIndex + 1, ")"));
          if (selectedRow) {
            selectedRow.scrollIntoView(true);
          }
        }
      };
      _this.scrollIntoView = function(options) {
        if (!_this.vs.container || _this.props.scrollable === "none") {
          return;
        }
        _this.rowIndex = options.rowIndex;
        var rowIndex = options.rowIndex;
        var element = _this.element;
        if (_this.observer && element) {
          _this.observer.disconnect();
          var target = element.querySelector('[absolute-row-index="'.concat(rowIndex, '"]'));
          if (target) {
            _this.observer.observe(target);
          } else {
            _this.setCurrentOnTop(options);
          }
        }
      };
      _this.fitColumns = function(columnIds) {
        _this.columnResize.dblClickHandler(null, columnIds);
      };
      _this.onContextMenu = function(event, dataItem, field) {
        if (_this.props.onContextMenu) {
          var ev = {
            target: _this,
            syntheticEvent: event,
            nativeEvent: event.nativeEvent,
            dataItem,
            field
          };
          _this.props.onContextMenu.call(void 0, ev);
        }
      };
      _this.getVirtualScroll = function(props2) {
        var rowHeight = props2.rowHeight, detailRowHeight = props2.detailRowHeight, detail = props2.detail, expandField = props2.expandField, groupable2 = props2.groupable;
        var hasAllData = _this.isAllData();
        var hasRowHeight = rowHeight !== void 0 && rowHeight > 0;
        var hasDetailRowHeight = detailRowHeight !== void 0 && detailRowHeight > 0;
        var isGroupable = groupable2 === true || typeof groupable2 === "object" && groupable2.enabled !== false;
        var hasDetails = Boolean(detail && expandField);
        if (!hasRowHeight || isGroupable && !hasAllData || hasDetails && (!hasDetailRowHeight || !hasAllData)) {
          return VirtualScroll;
        }
        return VirtualScrollFixed;
      };
      _this.isAllData = function() {
        var _a4 = _this.props, data = _a4.data, total = _a4.total;
        if (Array.isArray(data)) {
          return data.length === total;
        } else if (data) {
          return total === data.total;
        }
        return false;
      };
      _this.initializeVirtualization = function(total, groupable2) {
        _this.reInitVirtualization(_this.props.scrollable === "virtual", _this.props.total, _this.props.filter, _this.props.group, _this.props.sort, groupable2 || _this.props.rowHeight === void 0 || _this.props.rowHeight === 0);
        _this.vs.fixedScroll = _this.props.fixedScroll || false;
        _this.vs.PageChange = _this.pageChange;
        _this.vs.realSkip = _this.props.skip || 0;
        _this.vs.pageSize = (_this.props.take !== void 0 ? _this.props.take : _this.props.pageSize) || 0;
        _this.vs.scrollableVirtual = _this.props.scrollable === "virtual";
        _this.vs.total = total;
        _this.vs.propsSkip = (_this.props.skip || 0) + (_this.props.scrollable === "virtual" ? _this.vs.topCacheCount + (_this.vs.attendedSkip - (_this.props.skip || 0)) : 0);
        if (_this.props.rowHeight !== void 0 && _this.props.rowHeight > 0 && !groupable2) {
          var containerHeight = _this.props.rowHeight * total;
          _this.vs.containerHeight = firefox ? Math.min(firefoxMaxHeight, containerHeight) : containerHeight;
        } else {
          _this.vs.containerHeight = 1533915;
        }
        _this.vs.containerRef = _this.containerRef;
        _this.vs.tableBodyRef = _this.tableBodyRef;
        _this.vs.table = _this.tableElement;
        _this._slicedData = void 0;
        if (_this.vs instanceof VirtualScrollFixed) {
          var _a4 = _this.props, _b2 = _a4.rowHeight, rowHeight = _b2 === void 0 ? 0 : _b2, detail = _a4.detail, expandField = _a4.expandField;
          var _c2 = _this.props.detailRowHeight, detailRowHeight = _c2 === void 0 ? 0 : _c2;
          detailRowHeight = Boolean(detail && expandField) ? detailRowHeight : rowHeight;
          if (_this.isAllData()) {
            _this.vs.total = _this._data.length;
            _this._slicedData = _this._data.slice(_this.vs.realSkip, _this.vs.realSkip + _this.vs.pageSize);
            _this.vs.rowHeightService = _this.rowHeightService(_this.vs, _this._data.length, rowHeight, detailRowHeight, _this._data);
          } else {
            _this.vs.rowHeightService = new RowHeightService(total, rowHeight, detailRowHeight);
          }
          var containerHeight = _this.vs.rowHeightService.totalHeight();
          _this.vs.containerHeight = firefox ? Math.min(firefoxMaxHeight, containerHeight) : containerHeight;
        }
      };
      _this.scrollHandler = function(event) {
        if (event.target !== event.currentTarget) {
          return;
        }
        clearTimeout(_this.forceUpdateTimeout);
        _this.vs.table = _this.tableElement;
        var scrollLeft = event.currentTarget.scrollLeft;
        var scrollTop = event.currentTarget.scrollTop;
        if (_this.props.columnVirtualization && (!_this.vs.scrollableVirtual || scrollTop === _this.wrapperScrollTop)) {
          _this.forceUpdateTimeout = window.setTimeout(function() {
            _this.forceUpdate();
          }, 0);
        }
        if (_this._header) {
          _this._header.setScrollLeft(scrollLeft);
        }
        if (_this._footer) {
          _this._footer.setScrollLeft(scrollLeft);
        }
        if (_this.vs && scrollTop !== _this.wrapperScrollTop) {
          _this.vs.scrollHandler(event);
        }
        dispatchEvent(_this.props.onScroll, event, _this, void 0);
        _this.wrapperScrollTop = scrollTop;
      };
      _this.onKeyDown = function(event) {
        tableKeyboardNavigation.onKeyDown(event, {
          contextStateRef: _this.contextStateRef,
          navigationStateRef: _this.navigationStateRef,
          onNavigationAction: _this.onNavigationAction
        });
        if (_this.props.onKeyDown) {
          var _a4 = getSelectionOptions(_this.props.selectable), mode = _a4.mode, cell = _a4.cell;
          var ev = __assign36({ dataItems: _this.getLeafDataItems(), mode, cell, componentId: _this._gridId, selectedField: _this.props.selectedField }, _this.getArguments(event));
          _this.props.onKeyDown.call(void 0, ev);
        }
      };
      _this.onFocus = function(event) {
        tableKeyboardNavigation.onFocus(event, {
          contextStateRef: _this.contextStateRef
        });
      };
      _this.onNavigationAction = function(options) {
        if (_this.props.onNavigationAction) {
          var ev = __assign36({ focusElement: options.focusElement }, _this.getArguments(options.event));
          _this.props.onNavigationAction.call(void 0, ev);
        }
      };
      _this.rowClick = function(e, item) {
        if (_this.props.onRowClick && e.target.nodeName === "TD") {
          _this.props.onRowClick.call(void 0, __assign36({ dataItem: item.dataItem }, _this.getArguments(e)));
        }
      };
      _this.rowDblClick = function(e, item) {
        if (_this.props.onRowDoubleClick && e.target.nodeName === "TD") {
          _this.props.onRowDoubleClick.call(void 0, __assign36({ dataItem: item.dataItem }, _this.getArguments(e)));
        }
      };
      _this.itemChange = function(event) {
        var itemChange = _this.props.onItemChange;
        if (event.field === _this.props.expandField || _this.props.group && event.field === void 0) {
          var expandChange = _this.props.onExpandChange;
          if (expandChange) {
            expandChange.call(void 0, __assign36(__assign36({}, _this.getArguments(event.syntheticEvent)), { dataItem: event.dataItem, dataIndex: event.dataIndex, value: event.value }));
          }
          return;
        }
        if (itemChange) {
          itemChange.call(void 0, __assign36(__assign36({}, _this.getArguments(event.syntheticEvent)), { dataItem: event.dataItem, field: event.field, value: event.value }));
        }
      };
      _this.onHeaderSelectionChange = function(event) {
        if (_this.props.onHeaderSelectionChange) {
          _this.props.onHeaderSelectionChange.call(void 0, {
            field: event.field,
            nativeEvent: event.syntheticEvent && event.syntheticEvent.nativeEvent,
            syntheticEvent: event.syntheticEvent,
            target: _this,
            dataItems: _this.getLeafDataItems(),
            selectedField: _this.props.selectedField
          });
        }
      };
      _this.selectionRelease = function(event) {
        if (_this.props.onSelectionChange) {
          var selectionEvent = __assign36({ syntheticEvent: void 0, target: _this, selectedField: _this.props.selectedField || "", componentId: _this._gridId, dataItems: _this.getLeafDataItems(), dataItem: null }, event);
          _this.props.onSelectionChange.call(void 0, selectionEvent);
        }
      };
      _this.pageChange = function(page, syntheticEvent, targetEvent) {
        _this.raiseDataEvent(_this.props.onPageChange, { page }, { skip: page.skip, take: page.take }, syntheticEvent, targetEvent);
      };
      _this.pagerPageChange = function(event) {
        _this.pageChange({ skip: event.skip, take: event.take }, event.syntheticEvent, event.targetEvent);
      };
      _this.sortChange = function(sort, syntheticEvent) {
        _this.raiseDataEvent(_this.props.onSortChange, { sort }, __assign36({ sort }, _this.props.scrollable === "virtual" ? { skip: 0 } : {}), syntheticEvent);
      };
      _this.filterChange = function(filter, syntheticEvent) {
        _this.raiseDataEvent(_this.props.onFilterChange, { filter }, { filter: filter || void 0, skip: 0 }, syntheticEvent);
      };
      _this.groupChange = function(groups, event) {
        var syntheticEvent = event.nativeEvent ? event : { nativeEvent: event.nativeEvent || event.originalEvent };
        _this.raiseDataEvent(_this.props.onGroupChange, { group: groups }, { group: groups, skip: 0 }, syntheticEvent);
      };
      _this.onResize = function(index, newWidth, oldWidth, nativeEvent, end, targetId) {
        _this.resetTableWidth();
        _this._columnsMutations++;
        if (_this.props.onColumnResize) {
          _this.props.onColumnResize.call(void 0, {
            columns: _this.columns,
            nativeEvent,
            targetColumnId: targetId,
            index,
            newWidth,
            oldWidth,
            end,
            target: _this
          });
        }
      };
      _this.reInitVirtualization = memoizeOne(function(_vScrolling, _total, _filter, _group, _sort, useCached) {
        _this.vs.reset();
        var VS2 = _this.getVirtualScroll(_this.props);
        _this.vs = new VS2(useCached);
      });
      _this.flatData = memoizeOne(function(propsData, groupingFooter, skip, group, expandField, _filter, _sort) {
        var flattedData = [];
        var resolvedGroupsCount = flatData(flattedData, propsData, groupingFooter, { index: skip }, group !== void 0, expandField);
        return { flattedData, resolvedGroupsCount };
      });
      _this.rowHeightService = memoizeOne(function(_vs, total, rowHeight, detailRowHeight, data) {
        return new RowHeightService(total, rowHeight, detailRowHeight, data);
      });
      _this.childrenToArray = memoizeOne(function(children) {
        return React61.Children.toArray(children);
      });
      _this.readColumns = memoizeOne(function(children, idPrefix, _mutationsCount) {
        var columnElements = children.filter(function(child) {
          return child && child.type && child.type.displayName === "KendoReactGridColumn";
        });
        return readColumns(columnElements, _this.columns, { prevId: 0, idPrefix });
      });
      _this.mapColumns = memoizeOne(function(columns) {
        return mapColumns(columns);
      });
      _this.configureColumns = memoizeOne(function(columns, groupCount, idPrefix) {
        if (_this.props.selectedField) {
          columns.filter(function(c) {
            return c.field === _this.props.selectedField;
          }).forEach(function(c) {
            c.width = c.width || "50px";
            c.cell = c.cell || GridSelectionCell;
            c.headerCell = c.headerCell || GridHeaderSelectionCell;
          });
        }
        var defaultServiceProps = {
          id: "",
          resizable: true,
          width: "32px",
          title: " ",
          declarationIndex: -1,
          orderIndex: -1,
          children: [],
          parentIndex: -1,
          depth: 0,
          colSpan: 0,
          rowSpan: 0,
          left: 0,
          right: 0,
          index: 0,
          rightBorder: false,
          ariaColumnIndex: 0,
          isAccessible: true
        };
        var columnIndexOffset = 0;
        if (_this.props.expandField && _this.props.onExpandChange && _this.props.detail) {
          columns.unshift(__assign36(__assign36({}, defaultServiceProps), { cell: GridHierarchyCell, field: _this.props.expandField, headerClassName: "k-hierarchy-cell k-header", id: tableKeyboardNavigationTools.generateNavigatableId("".concat(_this._columns.length), idPrefix, "column") }));
          columnIndexOffset++;
        }
        for (var i = 0; i < groupCount; i++) {
          columns.unshift(__assign36(__assign36({}, defaultServiceProps), { isAccessible: false, cell: GridGroupCell, field: "value", locked: _this.props.lockGroups }));
          columnIndexOffset++;
        }
        columns.slice(columnIndexOffset).forEach(function(c) {
          return c.parentIndex >= 0 && (c.parentIndex += columnIndexOffset);
        });
      });
      _this.getHeaderRow = memoizeOne(function(sort, sortable, group, groupable2, filter, filterable, filterOperators, headerCellRender, columnMenu, columns, columnsMap) {
        return React61.createElement(HeaderRow2, { sort, sortable, sortChange: _this.sortChange, group: group || [], groupable: groupable2, groupChange: _this.groupChange, filter, filterable, filterOperators, filterChange: _this.filterChange, columnMenu, selectionChange: _this.onHeaderSelectionChange, columns, columnResize: _this.columnResize, pressHandler: _this.dragLogic.pressHandler, dragHandler: _this.dragLogic.dragHandler, releaseHandler: _this.dragLogic.releaseHandler, columnsMap, cellRender: headerCellRender, isRtl: _this.isRtl, dragClue: _this.dragLogic.dragClueRef, headerRef: _this.headerElementRef, containerRef: _this.containerRef });
      });
      _this.resolveTitle = function(field) {
        var column = _this._columns.find(function(c) {
          return c.field === field;
        });
        var title = column && (column.title || column.field);
        return title === void 0 ? field : title;
      };
      _this.getLeafDataItems = function() {
        return _this._data.filter(function(item) {
          return item.rowType === "data";
        }).map(function(item) {
          return item.dataItem;
        });
      };
      validatePackage(packageMetadata2);
      var groupable = _this.props.groupable === true || typeof _this.props.groupable === "object" && _this.props.groupable.enabled !== false;
      var VS = _this.getVirtualScroll(props);
      _this.vs = new VS(groupable || props.rowHeight === void 0 || props.rowHeight === 0);
      _this.dragLogic = new CommonDragLogic2(_this.columnReorder.bind(_this), _this.groupReorder.bind(_this), _this.columnToGroup.bind(_this));
      _this.columnResize = new ColumnResize2(_this.onResize);
      tableKeyboardNavigation.onConstructor({
        navigatable: Boolean(props.navigatable),
        contextStateRef: _this.contextStateRef,
        navigationStateRef: _this.navigationStateRef,
        idPrefix: _this._gridId
      });
      return _this;
    }
    Object.defineProperty(Grid2.prototype, "columns", {
      /* eslint-disable max-len */
      /**
       * A getter of the current columns. Gets the current column width or current columns, or any other [`GridColumnProps`]({% slug api_grid_gridcolumnprops %}) for each defined column. Can be used on each Grid instance. To obtain the instance of the rendered Grid, use the `ref` callback. The following example demonstrates how to reorder the columns by dragging their handlers and check the properties afterwards. You can check the result in the browser console.
       *
       * @example
       * ```jsx
       * class App extends React.Component {
       *     constructor(props) {
       *         super(props);
       *         this.state = {
       *             data: [
       *                 { 'foo': 'A1', 'bar': 'B1' },
       *                 { 'foo': 'A2', 'bar': 'B2' },
       *                 { 'foo': 'A3', 'bar': 'B2' }
       *             ]
       *         };
       *         this.grid = null;
       *     }
       *     render() {
       *         return (
       *             <div>
       *                 <Grid
       *                     data={this.state.data}
       *                     reorderable={true}
       *                     ref={(g) => { this.grid = g; }}
       *                 >
       *                     <GridColumn field="foo" />
       *                     <GridColumn field="bar" />
       *                 </Grid>
       *                 <button onClick={() => console.log(JSON.stringify(this.grid.columns))}>
       *                     log current properties into browser console.
       *                 </button>
       *             </div>
       *         );
       *     }
       * }
       * ReactDOM.render(<App />, document.querySelector('my-app'));
       * ```
       */
      // tslint:enable:max-line-length
      get: function() {
        var shuffledColumns = this._columns.filter(function(q) {
          return q.declarationIndex >= 0 && q.parentIndex === -1;
        });
        return sanitizeColumns(shuffledColumns);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "element", {
      /**
       * Returns the HTML element of the Grid component.
       */
      get: function() {
        return this._element;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "document", {
      get: function() {
        if (!canUseDOM) {
          return;
        }
        return this.element && this.element.ownerDocument || document;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "_header", {
      get: function() {
        return this.headerRef.current;
      },
      enumerable: false,
      configurable: true
    });
    Grid2.prototype.componentDidMount = function() {
      this.resetTableWidth();
      setScrollbarWidth();
      var rtl = isRtl(this.element);
      this.isRtl = rtl;
      tableKeyboardNavigation.onComponentDidMount({
        scope: this.element || void 0,
        contextStateRef: this.contextStateRef,
        navigationStateRef: this.navigationStateRef
      });
      if (this.isRtl) {
        this.forceUpdate();
      }
      var observerOptions = {
        rootMargin: "0px",
        threshold: 0.9
      };
      this.observer = canUseDOM && window.IntersectionObserver && new window.IntersectionObserver(this.handleIntersection, observerOptions) || null;
    };
    Grid2.prototype.getSnapshotBeforeUpdate = function() {
      tableKeyboardNavigation.onGetSnapshotBeforeUpdate({
        document: this.document,
        contextStateRef: this.contextStateRef,
        navigationStateRef: this.navigationStateRef
      });
      return null;
    };
    Grid2.prototype.componentDidUpdate = function() {
      setScrollbarWidth();
      if (this.vs.tableTransform && this.vs.table) {
        this.vs.table.style.transform = this.vs.tableTransform;
        this.vs.tableTransform = "";
      }
      tableKeyboardNavigation.onComponentDidUpdate({
        scope: this.element || void 0,
        contextStateRef: this.contextStateRef,
        navigationStateRef: this.navigationStateRef
      });
    };
    Grid2.prototype.componentWillUnmount = function() {
      clearTimeout(this.forceUpdateTimeout);
    };
    Grid2.prototype.render = function() {
      var _a4, _b2, _c2;
      var _this = this;
      var total = this.props.total || 0;
      var idPrefix = tableKeyboardNavigationTools.getIdPrefix(this.navigationStateRef);
      var propsData = [];
      if (Array.isArray(this.props.data)) {
        propsData = this.props.data;
        total = total || this.props.data.length;
      } else if (this.props.data) {
        propsData = this.props.data.data;
        total = total || this.props.data.total;
      }
      var groupingFooter = typeof this.props.groupable === "object" && this.props.groupable.footer || "none";
      var _d = this.flatData(propsData, groupingFooter, this.props.skip || 0, this.props.group, this.props.expandField, this.props.filter, this.props.sort), resolvedGroupsCount = _d.resolvedGroupsCount, flattedData = _d.flattedData;
      this._data = flattedData;
      var groupable = this.props.groupable === true || typeof this.props.groupable === "object" && this.props.groupable.enabled !== false;
      this.columnResize.resizable = this.props.resizable || false;
      this.dragLogic.reorderable = this.props.reorderable || false;
      this.dragLogic.groupable = groupable;
      var tableUserSelect = this.props.selectable && this.props.selectable.drag ? "none" : void 0;
      this.initializeVirtualization(total, groupable);
      var children = this.childrenToArray(this.props.children);
      this.initColumns(children, resolvedGroupsCount);
      var toolbar = children.filter(function(child) {
        return child && child.type && child.type.displayName === "KendoReactGridToolbar";
      });
      var noRecords2 = children.filter(function(child) {
        return child && child.type && child.type.displayName === "KendoReactGridNoRecords";
      });
      var leafColumns = this._columns.filter(function(c) {
        return c.children.length === 0;
      });
      var groupingPanel = groupable && React61.createElement(GroupPanel, { group: this.props.group || [], groupChange: this.groupChange, pressHandler: this.dragLogic.pressHandler, dragHandler: this.dragLogic.dragHandler, releaseHandler: this.dragLogic.releaseHandler, refCallback: this.dragLogic.refGroupPanelDiv, resolveTitle: this.resolveTitle });
      var _e = this.props, sort = _e.sort, sortable = _e.sortable, group = _e.group, filter = _e.filter, filterable = _e.filterable, _f = _e.filterOperators, filterOperators = _f === void 0 ? operators : _f, headerCellRender = _e.headerCellRender, columnMenu = _e.columnMenu;
      var header = React61.createElement(Header2, { size: this.props.size, columnResize: this.columnResize, staticHeaders: this.props.scrollable !== "none", scrollableDataElement: function() {
        return _this.vs.container;
      }, draggable: this.props.reorderable || groupable, ref: this.headerRef, elemRef: this.headerElementRef, headerRow: this.getHeaderRow(sort, sortable, group, groupable, filter, filterable, filterOperators, headerCellRender, columnMenu, this._columns, this._columnsMap), filterRow: this.props.filterable && React61.createElement(FilterRow, { size: this.props.size, columns: this._columns, filter: this.props.filter, filterOperators: this.props.filterOperators || operators, filterChange: this.filterChange, sort: this.props.sort, cellRender: this.props.filterCellRender, isRtl: this.isRtl, ariaRowIndex: this._columnsMap.length + 1 }) || void 0, cols: leafColumns.map(function(column, index) {
        return React61.createElement("col", { key: index.toString(), width: column.width !== void 0 ? Math.floor(parseFloat(column.width.toString())) + "px" : void 0 });
      }) });
      var scrollLeft = this.vs && this.vs.container && this.vs.container.scrollLeft || 0;
      var tableWidth = parseFloat(((this.props.style || {}).width || "").toString());
      var _g = tableColumnsVirtualization({
        enabled: this.props.columnVirtualization,
        columns: leafColumns,
        tableViewPortWidth: tableWidth,
        scrollLeft
      }), colSpans = _g.colSpans, isColHidden = _g.hiddenColumns;
      var dataRow = function(item2, rowId, rowDataIndex) {
        var isInEdit = false;
        var selectedValue = _this.props.selectedField ? getNestedValue(_this.props.selectedField, item2.dataItem) : void 0;
        return {
          row: leafColumns.map(function(column, columnIndex) {
            if (isColHidden[columnIndex]) {
              return null;
            }
            var columnKey = column.id ? column.id : columnIndex;
            var className = "".concat(column.className ? column.className + " " : "") + "".concat(column.locked ? "k-grid-content-sticky" : "");
            var style = column.left !== void 0 ? !_this.isRtl ? { left: column.left, right: column.right } : { left: column.right, right: column.left } : {};
            var currentColumnIsInEdit = false;
            if (column.editable && _this.props.editField) {
              var inEdit = getNestedValue(_this.props.editField, item2.dataItem);
              if (inEdit === true || inEdit === column.field) {
                isInEdit = true;
                currentColumnIsInEdit = true;
              }
            }
            var Cell = column.cell || currentColumnIsInEdit && GridEditCell || GridCell;
            return React61.createElement(Cell, { onContextMenu: _this.onContextMenu, key: columnKey, locked: _this.props.lockGroups, id: tableKeyboardNavigationTools.generateNavigatableId("".concat(rowId, "-").concat(String(columnIndex)), idPrefix), colSpan: colSpans[columnIndex], dataItem: item2.dataItem, field: column.field, editor: column.editor, format: column.format, className, render: _this.props.cellRender, onChange: _this.itemChange, selectionChange: _this.props.onSelectionChange ? function(e) {
              _this.selectionChange({ event: e, dataItem: item2.dataItem, dataIndex: rowDataIndex, columnIndex });
            } : void 0, columnIndex, columnsCount: leafColumns.length, rowType: item2.rowType, level: item2.level, expanded: item2.expanded, dataIndex: item2.dataIndex, style, ariaColumnIndex: column.ariaColumnIndex, isSelected: Array.isArray(selectedValue) && selectedValue.indexOf(columnIndex) > -1 });
          }),
          isInEdit,
          isSelected: typeof selectedValue === "boolean" && selectedValue
        };
      };
      var hiddenRows = 0;
      if (this.props.scrollable === "virtual") {
        for (var i = 0; i < this.vs.topCacheCount + this.vs.attendedSkip - (this.props.skip || 0); i++) {
          var item = this._data.shift();
          if (item) {
            this._data.push(item);
            hiddenRows++;
            if (item.rowType === "groupHeader") {
              i--;
            }
          } else {
            break;
          }
        }
      }
      var hidden = function(index) {
        return index >= _this._data.length - hiddenRows;
      };
      var absoluteDataIndex = this.vs.realSkip || 0;
      var body = [];
      var rowIndexStart = this._columnsMap.length + (this.props.filterable ? 1 : 0) + 1;
      var currentAriaRowIndex = 0;
      if (this._data.length) {
        var dataIndex_1 = -1;
        var detailRowCount_1 = 0;
        (this._slicedData || this._data).forEach(function(item2, rowIndex) {
          if (item2.rowType === "data") {
            absoluteDataIndex++;
            dataIndex_1++;
          }
          var isAlt = absoluteDataIndex % 2 === 0;
          var dataItemKey = _this.props.dataItemKey && getter(_this.props.dataItemKey)(item2.dataItem);
          var absoluteIndex = rowIndex + (_this.vs.realSkip || 0);
          var rowId = dataItemKey ? dataItemKey : "ai" + absoluteIndex;
          var detailRowId = rowId + "_1";
          var gridRow = dataRow(item2, rowId, dataIndex_1);
          currentAriaRowIndex = absoluteIndex + rowIndexStart + detailRowCount_1;
          body.push(React61.createElement(GridRow, { key: rowId, dataItem: item2.dataItem, isAltRow: isAlt, isInEdit: gridRow.isInEdit, rowType: item2.rowType, isHidden: hidden(rowIndex), onClick: function(e) {
            return _this.rowClick(e, item2);
          }, onDoubleClick: function(e) {
            return _this.rowDblClick(e, item2);
          }, selectedField: _this.props.selectedField, rowHeight: _this.props.rowHeight, render: _this.props.rowRender, ariaRowIndex: currentAriaRowIndex, absoluteRowIndex: absoluteIndex, dataIndex: dataIndex_1, isSelected: gridRow.isSelected }, gridRow.row));
          if (_this.props.detail && item2.rowType === "data" && item2.expanded) {
            var detailColspan = leafColumns.length - (_this.props.expandField ? 1 : 0) - (_this.props.group ? _this.props.group.length : 0) || 1;
            detailRowCount_1++;
            currentAriaRowIndex = absoluteIndex + rowIndexStart + detailRowCount_1;
            body.push(React61.createElement(
              "tr",
              { key: detailRowId, className: classNames("k-table-row", isAlt ? "k-detail-row k-table-alt-row k-alt" : "k-detail-row"), style: { visibility: hidden(rowIndex) ? "hidden" : "", height: _this.props.detailRowHeight }, role: "row", "aria-rowindex": currentAriaRowIndex },
              _this.props.group && _this.props.group.map(function(gr, idx) {
                var _a5;
                var columnStyle = (_a5 = gridRow === null || gridRow === void 0 ? void 0 : gridRow.row[idx]) === null || _a5 === void 0 ? void 0 : _a5.props.style;
                var style = columnStyle ? !_this.isRtl ? { left: columnStyle.left, right: columnStyle.right } : { left: columnStyle.right, right: columnStyle.left } : {};
                return React61.createElement(GridGroupCell, { id: "", dataIndex: item2.dataIndex, field: gr.field, dataItem: item2.dataItem, key: idx, style, ariaColumnIndex: 1 + idx, isSelected: false, locked: _this.props.lockGroups });
              }),
              _this.props.expandField && React61.createElement(GridDetailHierarchyCell, { id: tableKeyboardNavigationTools.generateNavigatableId("".concat(detailRowId, "-dhcell"), idPrefix) }),
              React61.createElement(GridDetailCell, { onContextMenu: _this.onContextMenu, dataItem: item2.dataItem, dataIndex: item2.dataIndex, colSpan: detailColspan, ariaColIndex: 2 + (_this.props.group ? _this.props.group.length : 0), detail: _this.props.detail, id: tableKeyboardNavigationTools.generateNavigatableId("".concat(detailRowId, "-dcell"), idPrefix) })
            ));
          }
        });
      } else {
        body.push(React61.createElement(
          "tr",
          { key: "no-records", className: "k-table-row k-grid-norecords", role: "row", "aria-rowindex": rowIndexStart },
          React61.createElement("td", { className: "k-table-td", colSpan: leafColumns.length, role: "gridcell" }, noRecords2.length ? noRecords2 : React61.createElement(GridNoRecords, null))
        ));
      }
      var pagerProps = __assign36({ size: this.props.size, onPageChange: this.pagerPageChange, total, skip: this.vs.propsSkip || 0, take: (this.props.take !== void 0 ? this.props.take : this.props.pageSize) || 10, messagesMap: pagerMessagesMap }, normalize3(this.props.pageable || {}));
      var pager = this.props.pager ? React61.createElement(this.props.pager, __assign36({}, pagerProps)) : React61.createElement(Pager, __assign36({ className: "k-grid-pager" }, pagerProps));
      var sorted = function(field) {
        return _this.props.sort && _this.props.sort.filter(function(descriptor) {
          return descriptor.field === field;
        }).length > 0;
      };
      var footer = this._columns.some(function(c) {
        return Boolean(c.footerCell);
      }) ? React61.createElement(Footer, { size: this.props.size, columnResize: this.columnResize, staticHeaders: this.props.scrollable !== "none", ref: function(f) {
        return _this._footer = f;
      }, row: React61.createElement(FooterRow, { columns: this._columns, isRtl: this.isRtl, ariaRowIndex: currentAriaRowIndex + 1 }), cols: leafColumns.map(function(column, index) {
        return React61.createElement("col", { key: index.toString(), width: column.width !== void 0 ? Math.floor(parseFloat(column.width.toString())) + "px" : void 0 });
      }) }) : null;
      var colGroups = React61.createElement("colgroup", { ref: function(c) {
        _this.columnResize.colGroupMain = c;
      } }, leafColumns.map(function(column, index) {
        return React61.createElement("col", { key: index.toString(), className: sorted(column.field) ? "k-sorted" : void 0, width: column.width !== void 0 ? Math.floor(parseFloat(column.width.toString())) + "px" : void 0 });
      }));
      var enableDragClues = this.dragLogic.reorderable || this.dragLogic.groupable;
      var tableDisplay = this.props.columnVirtualization ? "block" : "table";
      if (this.props.scrollable === "none") {
        return React61.createElement(
          TableKeyboardNavigationContext.Provider,
          { value: this.contextStateRef.current },
          React61.createElement(
            "div",
            __assign36({ id: this.props.id, style: this.props.style, className: classNames("k-grid", (_a4 = {
              "k-grid-md": !this.props.size
            }, _a4["k-grid-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _a4), this.props.className), ref: function(el) {
              _this._element = el;
            }, "aria-label": this.props.ariaLabel, onKeyDown: this.onKeyDown, onFocus: this.onFocus }, tableKeyboardNavigationScopeAttributes),
            toolbar,
            groupingPanel,
            React61.createElement(
              TableSelection,
              { selectable: this.props.selectable, onRelease: this.selectionRelease, childRef: function(table) {
                _this.tableElement = table;
              } },
              React61.createElement(
                "table",
                { style: { userSelect: tableUserSelect, display: tableDisplay } },
                colGroups,
                header,
                React61.createElement("tbody", __assign36({ role: "rowgroup", className: "k-table-tbody" }, tableKeyboardNavigationBodyAttributes), body),
                footer
              )
            ),
            this.props.pageable && pager,
            enableDragClues && React61.createElement(
              React61.Fragment,
              null,
              React61.createElement(DropClue, { ref: this.dragLogic.refDropElementClue }),
              React61.createElement(DragClue, { ref: this.dragLogic.refDragElementClue })
            )
          )
        );
      }
      var wrapperStyle = this.props.style || {};
      if (this.props.scrollable === "virtual") {
        if (!wrapperStyle.height) {
          wrapperStyle = Object.assign({}, wrapperStyle, { height: "450px" });
        }
      }
      return React61.createElement(
        TableKeyboardNavigationContext.Provider,
        { value: this.contextStateRef.current },
        React61.createElement(
          "div",
          __assign36({ id: this.props.id, style: wrapperStyle, className: classNames("k-grid", (_b2 = {
            "k-grid-md": !this.props.size
          }, _b2["k-grid-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _b2["k-grid-virtual"] = this.props.scrollable === "virtual", _b2), this.props.className), ref: function(el) {
            _this._element = el;
          }, role: "grid", "aria-colcount": leafColumns.length, "aria-rowcount": total, "aria-label": this.props.ariaLabel, onKeyDown: this.onKeyDown, onFocus: this.onFocus }, tableKeyboardNavigationScopeAttributes),
          toolbar,
          groupingPanel,
          header,
          React61.createElement(
            "div",
            { className: "k-grid-container", role: "presentation" },
            React61.createElement(
              "div",
              { ref: this.containerRef, className: "k-grid-content k-virtual-content", onScroll: this.scrollHandler, role: "presentation" },
              React61.createElement(
                "div",
                { className: "k-grid-table-wrap", role: "presentation" },
                React61.createElement(
                  TableSelection,
                  { selectable: this.props.selectable, onRelease: this.selectionRelease, childRef: function(table) {
                    _this.tableElement = table;
                  } },
                  React61.createElement(
                    "table",
                    { className: classNames("k-table k-grid-table", (_c2 = {
                      "k-table-md": !this.props.size
                    }, _c2["k-table-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _c2)), role: "presentation", style: { userSelect: tableUserSelect, display: tableDisplay } },
                    colGroups,
                    React61.createElement("tbody", __assign36({ className: "k-table-tbody", ref: this.tableBodyRef, role: "rowgroup" }, tableKeyboardNavigationBodyAttributes), body)
                  )
                )
              ),
              React61.createElement(
                "div",
                { className: "k-height-container", role: "presentation" },
                React61.createElement("div", { style: this.props.scrollable === "virtual" ? { "height": this.vs.containerHeight + "px" } : {} })
              )
            )
          ),
          footer,
          this.props.pageable && pager,
          enableDragClues && React61.createElement(
            React61.Fragment,
            null,
            React61.createElement(DropClue, { ref: this.dragLogic.refDropElementClue }),
            React61.createElement(DragClue, { ref: this.dragLogic.refDragElementClue })
          )
        )
      );
    };
    Grid2.prototype.selectionChange = function(options) {
      if (this.props.onSelectionChange) {
        var event_1 = options.event, dataItem = options.dataItem, dataIndex = options.dataIndex, columnIndex = options.columnIndex;
        var _a4 = getSelectionOptions(this.props.selectable), mode = _a4.mode, cell = _a4.cell;
        var selectionEvent = __assign36(__assign36({}, this.getArguments(event_1.syntheticEvent)), { dataItem, startColIndex: columnIndex, endColIndex: columnIndex, startRowIndex: dataIndex, endRowIndex: dataIndex, dataItems: this.getLeafDataItems(), altKey: false, ctrlKey: false, shiftKey: false, metaKey: false, mode, cell, isDrag: false, componentId: this._gridId, selectedField: this.props.selectedField || "" });
        this.props.onSelectionChange.call(void 0, selectionEvent);
      }
    };
    Grid2.prototype.raiseDataEvent = function(handler, data, moreData, syntheticEvent, targetEvent) {
      var dataStateChange = this.props.onDataStateChange;
      if (handler) {
        var ev = __assign36(__assign36(__assign36({}, this.getArguments(syntheticEvent)), data), { targetEvent });
        handler.call(void 0, ev);
      } else if (dataStateChange) {
        dataStateChange.call(void 0, __assign36(__assign36({}, this.getArguments(syntheticEvent)), { targetEvent: targetEvent || {}, dataState: __assign36(__assign36({}, this.getDataState()), moreData) }));
      }
    };
    Grid2.prototype.columnReorder = function(prev, next, nativeEvent) {
      var _a4;
      var _this = this;
      var column = this._columns[prev];
      var depth = column.depth;
      var end = function(index) {
        do {
          index++;
        } while (index < _this._columns.length && _this._columns[index].depth > depth);
        return index;
      };
      var spliced = this._columns.splice(prev, end(prev) - prev);
      (_a4 = this._columns).splice.apply(_a4, __spreadArray12([prev < next ? end(next - spliced.length) : next, 0], spliced, false));
      this._columns.filter(function(q) {
        return q.declarationIndex >= 0;
      }).forEach(function(c, i) {
        return c.orderIndex = i;
      });
      this._columnsMutations++;
      var eventColumnProps = this.columns;
      this.forceUpdate();
      if (this.props.onColumnReorder) {
        var reorderEvent = {
          target: this,
          columns: eventColumnProps,
          columnId: column.id,
          nativeEvent
        };
        this.props.onColumnReorder.call(void 0, reorderEvent);
      }
    };
    Grid2.prototype.groupReorder = function(prevIndex, nextIndex, nativeEvent) {
      if (this.props.group === void 0) {
        return;
      }
      var group = this.props.group.slice();
      group.splice.apply(group, __spreadArray12([nextIndex, 0], group.splice(prevIndex, 1), false));
      this.groupChange(group, nativeEvent);
    };
    Grid2.prototype.columnToGroup = function(columnIndex, groupIndex, nativeEvent) {
      var field = this._columns[columnIndex].field;
      if (!field) {
        return;
      }
      var group = (this.props.group || []).slice();
      group.splice(groupIndex, 0, { field });
      this.groupChange(group, nativeEvent);
    };
    Grid2.prototype.resetTableWidth = function() {
      var totalWidth = 0;
      if (!this.columnResize.colGroupMain) {
        return;
      }
      var colElements = this.columnResize.colGroupMain.children;
      for (var i = 0; i < colElements.length; i++) {
        var width = colElements[i].width;
        if (!width) {
          return;
        }
        totalWidth += parseFloat(width.toString());
      }
      totalWidth = Math.round(totalWidth);
      if (this._header) {
        this._header.setWidth(totalWidth);
      }
      if (this._footer) {
        this._footer.setWidth(totalWidth);
      }
      if (this.tableElement) {
        this.tableElement.style.width = totalWidth + "px";
      }
    };
    Grid2.prototype.initColumns = function(children, groupCount) {
      var idPrefix = tableKeyboardNavigationTools.getIdPrefix(this.navigationStateRef);
      this._columns = this.readColumns(children, idPrefix, this._columnsMutations);
      if (this._columns.length === 0) {
        this._columns = autoGenerateColumns(this.props.data, this.props.group, this.props.expandField, { prevId: 0, idPrefix });
      }
      this.configureColumns(this._columns, groupCount, idPrefix);
      this._columnsMap = this.mapColumns(this._columns);
      this.columnResize.columns = this._columns;
      this.dragLogic.columns = this._columns;
    };
    Grid2.prototype.getDataState = function() {
      return {
        filter: this.props.filter,
        sort: this.props.sort,
        skip: this.props.skip,
        take: this.props.take !== void 0 ? this.props.take : this.props.pageSize,
        group: this.props.group
      };
    };
    Grid2.prototype.getArguments = function(syntheticEvent) {
      return {
        nativeEvent: syntheticEvent && syntheticEvent.nativeEvent,
        syntheticEvent,
        target: this
      };
    };
    Grid2.displayName = "KendoReactGrid";
    Grid2.defaultProps = {
      // never use this
    };
    Grid2.propTypes = {
      data: PropTypes11.oneOfType([
        PropTypes11.array,
        PropTypes11.shape({
          data: PropTypes11.array,
          total: PropTypes11.number
        })
      ]),
      sortable: PropTypes11.oneOfType([
        PropTypes11.bool,
        PropTypes11.shape({
          mode: PropTypes11.oneOf(["single", "multiple"]),
          allowUnsort: PropTypes11.bool
        })
      ]),
      onSortChange: PropTypes11.func,
      sort: PropTypes11.array,
      filterable: PropTypes11.bool,
      filter: PropTypes11.any,
      onFilterChange: PropTypes11.func,
      pageable: PropTypes11.oneOfType([
        PropTypes11.bool,
        PropTypes11.shape({
          buttonCount: PropTypes11.number,
          info: PropTypes11.bool,
          type: PropTypes11.oneOf(["numeric", "input"]),
          pageSizes: PropTypes11.oneOfType([
            PropTypes11.bool,
            PropTypes11.arrayOf(PropTypes11.oneOfType([
              PropTypes11.string,
              PropTypes11.number
            ]))
          ]),
          previousNext: PropTypes11.bool
        })
      ]),
      pageSize: PropTypes11.number,
      onPageChange: PropTypes11.func,
      total: PropTypes11.number,
      skip: PropTypes11.number,
      take: PropTypes11.number,
      fixedScroll: PropTypes11.bool,
      onExpandChange: PropTypes11.func,
      expandField: PropTypes11.string,
      selectedField: PropTypes11.string,
      onSelectionChange: PropTypes11.func,
      onHeaderSelectionChange: PropTypes11.func,
      resizable: PropTypes11.bool,
      reorderable: PropTypes11.bool,
      group: PropTypes11.any,
      groupable: PropTypes11.oneOfType([
        PropTypes11.bool,
        PropTypes11.shape({
          enabled: PropTypes11.bool,
          footer: PropTypes11.oneOf(["always", "visible", "none"])
        })
      ]),
      onGroupChange: PropTypes11.func,
      onRowClick: PropTypes11.func,
      onRowDoubleClick: PropTypes11.func,
      onItemChange: PropTypes11.func,
      editField: PropTypes11.string,
      scrollable: PropTypes11.oneOf(["none", "scrollable", "virtual"]),
      rowHeight: PropTypes11.number,
      detailRowHeight: PropTypes11.number,
      detail: PropTypes11.any,
      style: PropTypes11.object,
      onDataStateChange: PropTypes11.func,
      onColumnResize: PropTypes11.func,
      onColumnReorder: PropTypes11.func,
      dataItemKey: PropTypes11.string,
      navigatable: PropTypes11.bool,
      size: PropTypes11.oneOf(["small", "medium"])
    };
    Grid2.contextType = TableKeyboardNavigationContext;
    return Grid2;
  }(React61.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuGroup.js
var React64 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuItemGroup.js
var React62 = __toESM(require_react());
var __extends40 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridColumnMenuItemGroup = (
  /** @class */
  function(_super) {
    __extends40(GridColumnMenuItemGroup2, _super);
    function GridColumnMenuItemGroup2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridColumnMenuItemGroup2.prototype.render = function() {
      var children = this.props.children;
      return React62.createElement("div", { id: this.props.id, className: classNames("k-columnmenu-item-wrapper", this.props.className), style: this.props.style }, children);
    };
    return GridColumnMenuItemGroup2;
  }(React62.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuItem.js
var React63 = __toESM(require_react());
var __extends41 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridColumnMenuItem = (
  /** @class */
  function(_super) {
    __extends41(GridColumnMenuItem2, _super);
    function GridColumnMenuItem2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onClick = function(e) {
        if (_this.props.onClick) {
          _this.props.onClick(e);
        }
      };
      return _this;
    }
    GridColumnMenuItem2.prototype.render = function() {
      var _a4 = this.props, title = _a4.title, iconClass = _a4.iconClass, svgIcon = _a4.svgIcon, selected = _a4.selected;
      return React63.createElement(
        "div",
        { onClick: this.onClick, className: "k-columnmenu-item ".concat(selected ? "k-selected" : "") },
        (iconClass || svgIcon) && React63.createElement(IconWrap, { name: iconClass ? toIconName(iconClass) : iconClass, icon: svgIcon }),
        title
      );
    };
    return GridColumnMenuItem2;
  }(React63.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuGroup.js
var __extends42 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridColumnMenuGroup = (
  /** @class */
  function(_super) {
    __extends42(GridColumnMenuGroup2, _super);
    function GridColumnMenuGroup2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onGroupClick = function(event) {
        event.preventDefault();
        if (_this.props.onGroupChange) {
          var column_1 = _this.props.column;
          if (!column_1.field) {
            return;
          }
          var newGroup = (_this.props.group || []).slice();
          var groupIndex = newGroup.findIndex(function(group) {
            return group.field === column_1.field;
          });
          if (groupIndex > -1) {
            newGroup.splice(groupIndex, 1);
          } else {
            newGroup.push({
              field: column_1.field
            });
          }
          _this.props.onGroupChange(newGroup, event);
        }
        if (_this.props.onCloseMenu) {
          _this.props.onCloseMenu();
        }
      };
      return _this;
    }
    GridColumnMenuGroup2.prototype.render = function() {
      var _a4 = this.props, group = _a4.group, column = _a4.column;
      var localizationService = provideLocalizationService(this);
      var isGrouped = Boolean(group && column.field && group.find(function(currentGroup) {
        return currentGroup.field === column.field;
      }));
      var message = !isGrouped ? groupColumn : ungroupColumn;
      return React64.createElement(
        GridColumnMenuItemGroup,
        null,
        React64.createElement(GridColumnMenuItem, { title: localizationService.toLanguageString(message, messages2[message]), iconClass: !isGrouped ? "k-i-group" : "k-i-ungroup", svgIcon: !isGrouped ? groupIcon : ungroupIcon, onClick: this.onGroupClick })
      );
    };
    return GridColumnMenuGroup2;
  }(React64.Component)
);
registerForIntl(GridColumnMenuGroup);
registerForLocalization(GridColumnMenuGroup);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuSort.js
var React65 = __toESM(require_react());
var __extends43 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var ASC_DIR = "asc";
var DESC_DIR = "desc";
var sortSeqMap3 = {
  true: {
    "asc": { "asc": "", "desc": "desc" },
    "desc": { "asc": "asc", "desc": "" },
    "": { "asc": "asc", "desc": "desc" }
  },
  false: {
    "asc": { "asc": "asc", "desc": "desc" },
    "desc": { "asc": "asc", "desc": "desc" },
    "": { "asc": "asc", "desc": "desc" }
  }
};
var sortIndex = function(field, sort) {
  if (!sort) {
    return -1;
  }
  return sort.findIndex(function(s) {
    return s.field === field;
  });
};
var sortedAsc = function(sortedIndex, sort) {
  return !!(sort && sortedIndex > -1 && sort[sortedIndex].dir === ASC_DIR);
};
var sortedDesc = function(sortedIndex, sort) {
  return !!(sort && sortedIndex > -1 && sort[sortedIndex].dir === DESC_DIR);
};
var GridColumnMenuSort = (
  /** @class */
  function(_super) {
    __extends43(GridColumnMenuSort2, _super);
    function GridColumnMenuSort2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.onAscClick = function(event) {
        _this.onSort(event, ASC_DIR);
        if (_this.props.onCloseMenu) {
          _this.props.onCloseMenu();
        }
      };
      _this.onDescClick = function(event) {
        _this.onSort(event, DESC_DIR);
        if (_this.props.onCloseMenu) {
          _this.props.onCloseMenu();
        }
      };
      _this.onSort = function(event, selectedDir) {
        event.preventDefault();
        if (!_this.props.onSortChange) {
          return;
        }
        var _a4 = _this.props, column = _a4.column, sortable = _a4.sortable, sort = _a4.sort;
        var _b2 = normalize2(sortable || false, false), allowUnsort = _b2.allowUnsort, mode = _b2.mode;
        var oldDescriptor = (sort || []).filter(function(d) {
          return d.field === column.field;
        })[0];
        var dir = sortSeqMap3[allowUnsort][oldDescriptor && oldDescriptor.dir || ""][selectedDir];
        var newDescriptor = mode === "single" ? [] : (_this.props.sort || []).filter(function(d) {
          return d.field !== column.field;
        });
        if (dir !== "" && column.field) {
          newDescriptor.push({ field: column.field, dir });
        }
        _this.props.onSortChange(newDescriptor, event);
      };
      return _this;
    }
    GridColumnMenuSort2.prototype.render = function() {
      var _a4 = this.props, sort = _a4.sort, column = _a4.column;
      var currentSortIndex = sortIndex(column.field, sort);
      var localizationService = provideLocalizationService(this);
      return React65.createElement(
        GridColumnMenuItemGroup,
        null,
        React65.createElement(GridColumnMenuItem, { title: localizationService.toLanguageString(sortAscending, messages2[sortAscending]), iconClass: "k-i-sort-asc-sm", svgIcon: sortAscSmallIcon, selected: sortedAsc(currentSortIndex, sort), onClick: this.onAscClick }),
        React65.createElement(GridColumnMenuItem, { title: localizationService.toLanguageString(sortDescending, messages2[sortDescending]), iconClass: "k-i-sort-desc-sm", svgIcon: sortDescSmallIcon, selected: sortedDesc(currentSortIndex, sort), onClick: this.onDescClick })
      );
    };
    GridColumnMenuSort2.active = function(field, sort) {
      var currentSortIndex = sortIndex(field, sort);
      return sortedDesc(currentSortIndex, sort) || sortedAsc(currentSortIndex, sort);
    };
    return GridColumnMenuSort2;
  }(React65.Component)
);
registerForIntl(GridColumnMenuSort);
registerForLocalization(GridColumnMenuSort);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuFilter.js
var React69 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuItemContent.js
var React66 = __toESM(require_react());
var __extends44 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridColumnMenuItemContent = (
  /** @class */
  function(_super) {
    __extends44(GridColumnMenuItemContent2, _super);
    function GridColumnMenuItemContent2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridColumnMenuItemContent2.prototype.render = function() {
      return React66.createElement(
        "div",
        { id: this.props.id, className: classNames("k-columnmenu-item-content", this.props.className), style: this.props.style },
        React66.createElement(Reveal, { style: { position: "relative", display: "block" } }, this.props.show ? this.props.children : null)
      );
    };
    return GridColumnMenuItemContent2;
  }(React66.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuFilterUI.js
var React68 = __toESM(require_react());

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuFilterCell.js
var React67 = __toESM(require_react());
var __extends45 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridColumnMenuFilterCell = (
  /** @class */
  function(_super) {
    __extends45(GridColumnMenuFilterCell2, _super);
    function GridColumnMenuFilterCell2(props) {
      var _this = _super.call(this, props) || this;
      _this.renderOperatorEditor = function() {
        if (_this.props.filterType === "boolean") {
          return;
        }
        var value = _this.props.operators.find(function(item) {
          return item.operator === _this.props.operator;
        }) || null;
        return React67.createElement(DropDownList, { value, onChange: _this.operatorChange, data: _this.props.operators, textField: "text" });
      };
      _this.inputChange = _this.inputChange.bind(_this);
      _this.operatorChange = _this.operatorChange.bind(_this);
      _this.boolDropdownChange = _this.boolDropdownChange.bind(_this);
      return _this;
    }
    GridColumnMenuFilterCell2.prototype.render = function() {
      return React67.createElement(
        React67.Fragment,
        null,
        this.renderOperatorEditor(),
        this.filterComponent(this.props.filterType, this.props.value, this.props.booleanValues)
      );
    };
    GridColumnMenuFilterCell2.prototype.inputChange = function(value, e) {
      cellInputChange(value, e, this.props);
    };
    GridColumnMenuFilterCell2.prototype.operatorChange = function(event) {
      cellOperatorChange(event, this.props.value, this.props.onChange);
    };
    GridColumnMenuFilterCell2.prototype.boolDropdownChange = function(event) {
      cellBoolDropdownChange(event, this.props.onChange);
    };
    GridColumnMenuFilterCell2.prototype.filterComponent = function(filterType, value, booleanValues) {
      var _this = this;
      switch (filterType) {
        case "numeric":
          return React67.createElement(NumericTextBox, { value, onChange: function(e) {
            _this.inputChange(e.value, e.syntheticEvent);
          }, rangeOnEnter: false });
        case "date":
          return React67.createElement(DatePicker, { value, onChange: function(e) {
            _this.inputChange(e.value, e.syntheticEvent);
          } });
        case "boolean":
          var noFilterSet_1 = function(filter) {
            return filter === null || filter === void 0;
          };
          return React67.createElement(DropDownList, { onChange: this.boolDropdownChange, value: booleanValues.find(function(item) {
            return item.operator === (noFilterSet_1(value) ? "" : value);
          }), data: booleanValues, textField: "text" });
        default:
          return React67.createElement(Input, { value: value || "", onChange: function(e) {
            _this.inputChange(e.target.value, e.syntheticEvent);
          } });
      }
    };
    return GridColumnMenuFilterCell2;
  }(React67.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuFilterUI.js
var __extends46 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign37 = function() {
  __assign37 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign37.apply(this, arguments);
};
var GridColumnMenuFilterUI = (
  /** @class */
  function(_super) {
    __extends46(GridColumnMenuFilterUI2, _super);
    function GridColumnMenuFilterUI2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridColumnMenuFilterUI2.prototype.render = function() {
      var _a4 = this.props, firstFilterProps = _a4.firstFilterProps, hideSecondFilter = _a4.hideSecondFilter, secondFilterProps = _a4.secondFilterProps, logicData = _a4.logicData, logicValue = _a4.logicValue, onLogicChange = _a4.onLogicChange;
      return React68.createElement(
        React68.Fragment,
        null,
        React68.createElement(GridColumnMenuFilterCell, __assign37({}, firstFilterProps)),
        !hideSecondFilter && React68.createElement(
          React68.Fragment,
          null,
          React68.createElement(DropDownList, { data: logicData, value: logicValue, onChange: onLogicChange, className: "k-filter-and", textField: "text" }),
          React68.createElement(GridColumnMenuFilterCell, __assign37({}, secondFilterProps))
        )
      );
    };
    return GridColumnMenuFilterUI2;
  }(React68.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuFilter.js
var __extends47 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign38 = function() {
  __assign38 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign38.apply(this, arguments);
};
var __spreadArray13 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var rootFilterOrDefault = function(rootFilter) {
  return rootFilter || { filters: [], logic: "and" };
};
var filterGroupByField = function(field, filter) {
  var rootFilter = rootFilterOrDefault(filter);
  var compositeFilters = rootFilter.filters.filter(function(f) {
    if (isCompositeFilterDescriptor(f)) {
      return f.filters && f.filters.length && f.filters.length <= 2 && !f.filters.find(function(nf) {
        return isCompositeFilterDescriptor(nf) || nf.field !== field;
      });
    }
    return false;
  });
  return compositeFilters[0] || null;
};
var GridColumnMenuFilter = (
  /** @class */
  function(_super) {
    __extends47(GridColumnMenuFilter2, _super);
    function GridColumnMenuFilter2(props) {
      var _this = _super.call(this, props) || this;
      _this.removeGroup = function(group, rootFilter) {
        var filters = __spreadArray13([], rootFilter.filters, true);
        var groupIndex = filters.findIndex(function(f) {
          return f === group;
        });
        if (groupIndex > -1) {
          filters.splice(groupIndex, 1);
        }
        return __assign38(__assign38({}, rootFilter), { filters });
      };
      _this.insertGroup = function(group, rootFilter) {
        return __assign38(__assign38({}, rootFilter), { filters: __spreadArray13([
          group
        ], rootFilter.filters, true) });
      };
      _this.isControlled = function() {
        return _this.props.expanded !== void 0;
      };
      _this.onFilterExpand = function() {
        var isControlled = _this.isControlled();
        var nextValue = !(isControlled ? _this.props.expanded : _this.state.expanded);
        if (_this.props.onExpandChange) {
          _this.props.onExpandChange(nextValue);
        }
        if (!isControlled) {
          _this.setState({
            expanded: nextValue
          });
        }
      };
      _this.firstFilterChange = function(event) {
        _this.filterChange(0, event);
      };
      _this.secondFilterChange = function(event) {
        _this.filterChange(1, event);
      };
      _this.filterChange = function(filterIndex, e) {
        var filters = _this.state.filterGroup.filters.map(function(f, i) {
          if (i === filterIndex) {
            return __assign38(__assign38({}, f), { value: e.value, operator: e.operator });
          }
          return f;
        });
        _this.setState({
          filterGroup: __assign38(__assign38({}, _this.state.filterGroup), { filters })
        });
      };
      _this.logicChange = function(event) {
        _this.setState({
          filterGroup: __assign38(__assign38({}, _this.state.filterGroup), { logic: event.target.value.operator })
        });
      };
      _this.clear = function(event) {
        event.preventDefault();
        if (!_this.props.onFilterChange) {
          return;
        }
        var field = _this.props.column.field;
        var rootFilter = rootFilterOrDefault(_this.props.filter);
        var filterGroup2 = filterGroupByField(field, _this.props.filter);
        var rootFilters = rootFilter.filters.filter(function(f) {
          return f !== filterGroup2;
        });
        if (!rootFilters.length) {
          _this.props.onFilterChange(null, event);
        } else {
          _this.props.onFilterChange(__assign38(__assign38({}, rootFilter), { filters: rootFilters }), event);
        }
        if (_this.props.onCloseMenu) {
          _this.props.onCloseMenu();
        }
      };
      _this.currentFilterGroup = function() {
        return __assign38(__assign38({}, _this.state.filterGroup), { filters: _this.state.filterGroup.filters.filter(function(nf) {
          return nf.value !== void 0 && nf.value !== null && nf.value !== "" || nf.value === null && nf.operator;
        }) });
      };
      _this.submit = function(event) {
        event.preventDefault();
        if (!_this.props.onFilterChange) {
          return;
        }
        var field = _this.props.column.field;
        var rootFilter = rootFilterOrDefault(_this.props.filter);
        var filterGroup2 = filterGroupByField(field, _this.props.filter);
        var currentFilterGroup = _this.currentFilterGroup();
        var updatedFilter = null;
        if (filterGroup2 && currentFilterGroup.filters.length > 0) {
          var rootFilters = rootFilter.filters.map(function(f) {
            if (f === filterGroup2) {
              return currentFilterGroup;
            }
            return f;
          });
          updatedFilter = __assign38(__assign38({}, rootFilter), { filters: rootFilters });
        } else if (currentFilterGroup.filters.length === 0) {
          var rootFilters = rootFilter.filters.filter(function(f) {
            return f !== filterGroup2;
          });
          if (rootFilters.length) {
            updatedFilter = __assign38(__assign38({}, rootFilter), { filters: rootFilters });
          }
        } else {
          updatedFilter = __assign38(__assign38({}, rootFilter), { filters: __spreadArray13(__spreadArray13([], rootFilter.filters, true), [currentFilterGroup], false) });
        }
        _this.props.onFilterChange(updatedFilter, event);
        if (_this.props.onCloseMenu) {
          _this.props.onCloseMenu();
        }
      };
      var filterGroup;
      if (props.column && props.column.field) {
        var filterType = getFilterType(props.column.filter);
        var defaultOperator = getDefaultOperator(props.filterOperators, filterType);
        filterGroup = filterGroupByField(props.column.field, props.filter);
        filterGroup = filterGroup ? __assign38(__assign38({}, filterGroup), { filters: filterGroup.filters.map(function(f) {
          return __assign38({}, f);
        }) }) : {
          logic: "and",
          filters: [
            { field: props.column.field, operator: defaultOperator, value: IsUnaryFilter(defaultOperator) ? null : void 0 },
            { field: props.column.field, operator: defaultOperator, value: IsUnaryFilter(defaultOperator) ? null : void 0 }
          ]
        };
        if (filterGroup.filters.filter(function(x) {
          return x.field === props.column.field;
        }).length === 1) {
          filterGroup.filters.splice(1, 0, { field: props.column.field, operator: defaultOperator });
        }
      }
      _this.state = {
        expanded: props.expanded || false,
        filterGroup
      };
      return _this;
    }
    GridColumnMenuFilter2.prototype.render = function() {
      var _a4 = this.props, column = _a4.column, filterUI = _a4.filterUI, hideSecondFilter = _a4.hideSecondFilter, filterOperators = _a4.filterOperators;
      if (!column || !column.field) {
        return React69.createElement("div", null);
      }
      var filterType = getFilterType(column.filter);
      var currentHideSecondFilter = hideSecondFilter !== void 0 ? hideSecondFilter : defaultHideSecondFilter[filterType];
      var localizationService = provideLocalizationService(this);
      var filters = this.state.filterGroup.filters;
      var currentOperators = operatorMap(filterOperators[filterType], localizationService);
      var booleanValues = operatorMap(booleanFilterValues, localizationService);
      var firstFilterCellProps = {
        field: column.field,
        value: filters[0].value,
        operator: filters[0].operator,
        operators: currentOperators,
        booleanValues,
        onChange: this.firstFilterChange,
        filterType
      };
      var secondFilterCellProps = {
        field: column.field,
        value: filters[1].value,
        operator: filters[1].operator,
        operators: currentOperators,
        booleanValues,
        onChange: this.secondFilterChange,
        filterType
      };
      var filterLogic = this.state.filterGroup.logic;
      var logicData = operatorMap(filterLogicList, localizationService);
      var logicProps = {
        value: logicData.find(function(item) {
          return item.operator === (filterLogic === null ? "" : filterLogic);
        }),
        onChange: this.logicChange,
        data: logicData
      };
      var FilterUI = filterUI;
      var filterUIProps = {
        firstFilterProps: firstFilterCellProps,
        secondFilterProps: secondFilterCellProps,
        logicData: logicProps.data,
        logicValue: logicProps.value,
        onLogicChange: logicProps.onChange,
        hideSecondFilter: currentHideSecondFilter
      };
      var isFilterValid = this.currentFilterGroup().filters.length !== 0 || IsUnaryFilter(filters[0].operator) || IsUnaryFilter(filters[1].operator);
      var expandState = this.isControlled() ? this.props.expanded : this.state.expanded;
      return React69.createElement(
        GridColumnMenuItemGroup,
        null,
        React69.createElement(GridColumnMenuItem, { title: localizationService.toLanguageString(filterTitle, messages2[filterTitle]), iconClass: "k-i-filter", svgIcon: filterIcon, onClick: this.onFilterExpand }),
        React69.createElement(
          GridColumnMenuItemContent,
          { show: !!expandState },
          React69.createElement(
            "div",
            { className: "kendo-grid-filter-menu-container" },
            React69.createElement(
              "form",
              { className: "k-filter-menu k-group k-reset k-state-border-up", onSubmit: this.submit, onReset: this.clear, onKeyDown: function(e) {
                return e.stopPropagation();
              } },
              React69.createElement(
                "div",
                { className: "k-filter-menu-container" },
                FilterUI ? React69.createElement(FilterUI, __assign38({}, filterUIProps)) : React69.createElement(GridColumnMenuFilterUI, __assign38({}, filterUIProps)),
                React69.createElement(
                  "div",
                  { className: "k-actions k-actions-stretched" },
                  React69.createElement(Button, { type: "reset" }, localizationService.toLanguageString(filterClearButton, messages2[filterClearButton])),
                  React69.createElement(Button, { themeColor: "primary", disabled: !isFilterValid }, localizationService.toLanguageString(filterSubmitButton, messages2[filterSubmitButton]))
                )
              )
            )
          )
        )
      );
    };
    GridColumnMenuFilter2.active = function(field, filter) {
      return !!filterGroupByField(field, filter);
    };
    return GridColumnMenuFilter2;
  }(React69.Component)
);
registerForLocalization(GridColumnMenuFilter);

// node_modules/@progress/kendo-react-grid/dist/es/columnMenu/GridColumnMenuCheckboxFilter.js
var React70 = __toESM(require_react());
var __extends48 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var __assign39 = function() {
  __assign39 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s)
        if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
    }
    return t;
  };
  return __assign39.apply(this, arguments);
};
var __spreadArray14 = function(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var isArrayEqual = function(firstArray, secondArray) {
  if (firstArray.length !== secondArray.length) {
    return false;
  }
  return firstArray.every(function(ex, i) {
    return ex === secondArray[i];
  });
};
var GridColumnMenuCheckboxFilter = (
  /** @class */
  function(_super) {
    __extends48(GridColumnMenuCheckboxFilter2, _super);
    function GridColumnMenuCheckboxFilter2(props) {
      var _this = _super.call(this, props) || this;
      _this.defaultFilter = function() {
        if (_this.props.filter) {
          return clone(_this.props.filter);
        }
        return { filters: [], logic: "and" };
      };
      _this.isControlled = function() {
        return _this.props.expanded !== void 0;
      };
      _this.parseData = function(originalData, isUnique) {
        var field = _this.props.column.field || "";
        var data = originalData.map(function(item) {
          return getNestedValue(field, item);
        });
        if (isUnique) {
          return data.filter(function(item, index) {
            return data.indexOf(item) === index;
          });
        }
        return data;
      };
      _this.getFilterIndex = function() {
        var field = _this.props.column.field;
        var currentFilter = _this.defaultFilter();
        var compositeFilterIndex = currentFilter.filters.findIndex(function(filter) {
          return filter.filters && filter.filters.length > 0 && filter.filters[0].field === field;
        });
        return compositeFilterIndex;
      };
      _this.onFilterExpand = function() {
        var isControlled = _this.isControlled();
        var nextValue = !(isControlled ? _this.props.expanded : _this.state.expanded);
        if (_this.props.onExpandChange) {
          _this.props.onExpandChange(nextValue);
        }
        if (!isControlled) {
          _this.setState({
            expanded: nextValue
          });
        }
      };
      _this.handleSearchChange = function(e) {
        var filterOperator = _this.props.searchBoxFilterOperator ? _this.props.searchBoxFilterOperator : "startswith";
        var filterExpression = {
          logic: "and",
          filters: [
            { field: _this.props.column.field, operator: filterOperator, value: e.target.value, ignoreCase: true }
          ]
        };
        _this.setState({
          value: e.target.value,
          data: _this.parseData(filterBy(_this.props.data || [], filterExpression), _this.props.uniqueData)
        });
      };
      _this.handleClear = function() {
        var filterOperator = _this.props.searchBoxFilterOperator ? _this.props.searchBoxFilterOperator : "startswith";
        var filterExpression = {
          logic: "and",
          filters: [
            { field: _this.props.column.field, operator: filterOperator, value: "", ignoreCase: true }
          ]
        };
        _this.setState({
          value: "",
          data: _this.parseData(filterBy(_this.props.data || [], filterExpression), _this.props.uniqueData)
        });
      };
      _this.clear = function(e) {
        e.preventDefault();
        if (!_this.props.onFilterChange) {
          return;
        }
        var updatedFilter = _this.state.currentFilter || null;
        if (updatedFilter !== null && updatedFilter.filters.length > 0) {
          if (_this.compositeFilterIndex >= 0) {
            updatedFilter.filters.splice(_this.compositeFilterIndex, 1);
          }
          _this.props.onFilterChange(updatedFilter, e);
        } else {
          _this.props.onFilterChange(null, e);
        }
        if (_this.props.onCloseMenu) {
          _this.props.onCloseMenu();
        }
      };
      _this.submit = function(e) {
        e.preventDefault();
        if (!_this.props.onFilterChange) {
          return;
        }
        var updatedFilter = _this.state.currentFilter || null;
        _this.props.onFilterChange(updatedFilter, e);
        if (_this.props.onCloseMenu) {
          _this.props.onCloseMenu();
        }
      };
      _this.handleCheckBoxChange = function(e, value) {
        var field = _this.props.column.field || "";
        var newFilter = __assign39({}, _this.state.currentFilter);
        var filters = __spreadArray14([], _this.state.currentFilter.filters, true) || [];
        var fieldFilters = [];
        if (_this.compositeFilterIndex !== -1 && newFilter.filters[_this.compositeFilterIndex].filters && value !== "all") {
          fieldFilters = newFilter.filters[_this.compositeFilterIndex].filters;
        }
        if (e.value && value === "all") {
          _this.state.data.forEach(function(item) {
            fieldFilters.push({ field, operator: "eq", value: item });
          });
        } else if (e.value) {
          fieldFilters.push({ field, operator: "eq", value });
        } else if (_this.state.currentFilter) {
          var index = fieldFilters.findIndex(function(filter) {
            return filter.value === value;
          });
          fieldFilters.splice(index, 1);
        }
        newFilter.logic = "and";
        if (_this.compositeFilterIndex !== -1) {
          filters[_this.compositeFilterIndex] = {
            logic: "or",
            filters: fieldFilters
          };
        } else {
          filters.push({
            logic: "or",
            filters: fieldFilters
          });
        }
        if (!e.value && value === "all" || fieldFilters.length === 0) {
          filters.splice(_this.compositeFilterIndex, 1);
        }
        newFilter.filters = filters;
        _this.setState({ currentFilter: newFilter });
      };
      _this.isAllSelected = function() {
        var isAllChecked = false;
        if (_this.state.currentFilter) {
          var filters_1 = __spreadArray14([], _this.state.currentFilter.filters, true);
          if (_this.compositeFilterIndex === -1) {
            return false;
          }
          isAllChecked = _this.state.data.every(function(item) {
            if (_this.compositeFilterIndex !== -1 && filters_1[_this.compositeFilterIndex].filters) {
              var index = filters_1[_this.compositeFilterIndex].filters.findIndex(function(filter) {
                return filter.value === item;
              });
              return index >= 0;
            }
            return false;
          });
          return isAllChecked;
        }
        return isAllChecked;
      };
      _this.compositeFilterIndex = _this.getFilterIndex();
      _this.state = {
        expanded: props.expanded || false,
        value: "",
        data: _this.parseData(_this.props.data, _this.props.uniqueData) || [],
        dataFromProps: _this.parseData(_this.props.data, false) || [],
        currentFilter: _this.defaultFilter()
      };
      return _this;
    }
    GridColumnMenuCheckboxFilter2.prototype.componentDidUpdate = function() {
      var field = this.props.column.field || "";
      var data = this.props.data.map(function(item) {
        return getNestedValue(field, item);
      });
      if (!isArrayEqual(data, this.state.dataFromProps)) {
        this.setState({
          data,
          dataFromProps: data
        });
      }
    };
    GridColumnMenuCheckboxFilter2.prototype.render = function() {
      var _this = this;
      var column = this.props.column;
      if (!column || !column.field) {
        return React70.createElement("div", null);
      }
      var localizationService = provideLocalizationService(this);
      var expandState = this.isControlled() ? this.props.expanded : this.state.expanded;
      var filterValues = [];
      if (this.state.currentFilter) {
        var currentFilter = __spreadArray14([], this.state.currentFilter.filters, true);
        this.compositeFilterIndex = currentFilter.findIndex(function(filter) {
          if (filter.filters && filter.filters.length > 0) {
            return filter.filters[0].field === column.field;
          }
          return false;
        });
        if (this.compositeFilterIndex !== -1 && currentFilter[this.compositeFilterIndex].filters.length > 0) {
          currentFilter[this.compositeFilterIndex].filters.forEach(function(filterItem) {
            if (filterItem.field === _this.props.column.field) {
              filterValues.push(filterItem.value);
            }
          });
        }
      }
      var searchBox = this.props.searchBox ? React70.createElement(this.props.searchBox, { value: this.state.value, onChange: this.handleSearchChange }) : React70.createElement(
        "div",
        { className: "k-searchbox k-textbox k-input k-input-md k-input-solid" },
        React70.createElement(IconWrap, { className: "k-input-icon", name: "search", icon: searchIcon }),
        React70.createElement(Input, { className: "k-input-inner", type: "text", placeholder: localizationService.toLanguageString(searchPlaceholder, messages2[searchPlaceholder]), value: this.state.value, onChange: function(e) {
          return _this.handleSearchChange(e.nativeEvent);
        } }),
        React70.createElement(Button, { type: "button", rounded: null, className: "k-input-button", onClick: this.handleClear, icon: "x", svgIcon: xIcon })
      );
      var uniqueFilterValues = filterValues.filter(function(item, index) {
        return filterValues.indexOf(item) === index;
      });
      return React70.createElement(
        GridColumnMenuItemGroup,
        null,
        React70.createElement(GridColumnMenuItem, { title: localizationService.toLanguageString(filterTitle, messages2[filterTitle]), iconClass: "k-i-filter", svgIcon: filterIcon, onClick: this.onFilterExpand }),
        React70.createElement(
          GridColumnMenuItemContent,
          { show: !!expandState },
          React70.createElement(
            "div",
            { className: "kendo-grid-filter-menu-container" },
            React70.createElement(
              "form",
              { className: "k-filter-menu k-group k-reset k-state-border-up", onSubmit: this.submit, onReset: this.clear },
              React70.createElement(
                "div",
                { className: "k-filter-menu-container" },
                searchBox,
                React70.createElement(
                  "ul",
                  { className: "k-reset k-multicheck-wrap" },
                  React70.createElement(
                    "li",
                    { className: "k-item k-check-all-wrap" },
                    React70.createElement(Checkbox, { label: localizationService.toLanguageString(filterCheckAll, messages2[filterCheckAll]), onChange: function(e) {
                      return _this.handleCheckBoxChange(e, "all");
                    }, checked: this.isAllSelected() })
                  ),
                  this.state.data.map(function(item, index) {
                    return React70.createElement(
                      "li",
                      { className: "k-item", key: index },
                      React70.createElement(Checkbox, { label: String(item), onChange: function(e) {
                        return _this.handleCheckBoxChange(e, item);
                      }, checked: uniqueFilterValues.includes(item) })
                    );
                  })
                ),
                React70.createElement("div", { className: "k-filter-selected-items" }, uniqueFilterValues.length + " " + localizationService.toLanguageString(filterSelectedItems, messages2[filterSelectedItems])),
                React70.createElement(
                  "div",
                  { className: "k-actions k-hstack k-justify-content-stretch" },
                  React70.createElement(Button, { className: "k-button", type: "reset" }, localizationService.toLanguageString(filterClearButton, messages2[filterClearButton])),
                  React70.createElement(Button, { themeColor: "primary", type: "submit" }, localizationService.toLanguageString(filterSubmitButton, messages2[filterSubmitButton]))
                )
              )
            )
          )
        )
      );
    };
    GridColumnMenuCheckboxFilter2.defaultProps = {
      uniqueData: true
    };
    return GridColumnMenuCheckboxFilter2;
  }(React70.Component)
);
registerForLocalization(GridColumnMenuCheckboxFilter);

// node_modules/@progress/kendo-react-grid/dist/es/rows/GridDetailRow.js
var React71 = __toESM(require_react());
var __extends49 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridDetailRow = (
  /** @class */
  function(_super) {
    __extends49(GridDetailRow2, _super);
    function GridDetailRow2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridDetailRow2.prototype.render = function() {
      return null;
    };
    return GridDetailRow2;
  }(React71.Component)
);

// node_modules/@progress/kendo-react-grid/dist/es/GridToolbar.js
var React72 = __toESM(require_react());
var __extends50 = function() {
  var extendStatics = function(d, b) {
    extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
      d2.__proto__ = b2;
    } || function(d2, b2) {
      for (var p in b2)
        if (Object.prototype.hasOwnProperty.call(b2, p))
          d2[p] = b2[p];
    };
    return extendStatics(d, b);
  };
  return function(d, b) {
    if (typeof b !== "function" && b !== null)
      throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
    extendStatics(d, b);
    function __() {
      this.constructor = d;
    }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
  };
}();
var GridToolbar = (
  /** @class */
  function(_super) {
    __extends50(GridToolbar2, _super);
    function GridToolbar2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridToolbar2.prototype.render = function() {
      var _a4;
      return React72.createElement("div", { className: classNames("k-toolbar k-grid-toolbar", (_a4 = {
        "k-toolbar-md": !this.props.size
      }, _a4["k-toolbar-".concat(kendoThemeMaps.sizeMap[this.props.size] || this.props.size)] = this.props.size, _a4), this.props.className) }, this.props.children);
    };
    GridToolbar2.displayName = "KendoReactGridToolbar";
    return GridToolbar2;
  }(React72.Component)
);
export {
  GRID_COL_INDEX_ATTRIBUTE,
  GRID_ROW_INDEX_ATTRIBUTE,
  Grid,
  GridCell,
  GridColumn,
  GridColumnMenuCheckboxFilter,
  GridColumnMenuFilter,
  GridColumnMenuFilterCell,
  GridColumnMenuFilterUI,
  GridColumnMenuGroup,
  GridColumnMenuItem,
  GridColumnMenuItemContent,
  GridColumnMenuItemGroup,
  GridColumnMenuSort,
  GridColumnMenuWrapper,
  GridDetailRow,
  GridEditCell,
  GridFilterCell,
  GridGroupCell,
  GridHeaderCell,
  GridHierarchyCell,
  GridNoRecords,
  GridRow,
  GridToolbar,
  getSelectedState,
  getSelectedStateFromKeyDown,
  setSelectedState
};
//# sourceMappingURL=@progress_kendo-react-grid.js.map
