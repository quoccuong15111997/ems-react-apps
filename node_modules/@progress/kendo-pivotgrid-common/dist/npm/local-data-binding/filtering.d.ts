import { CompositeFilterDescriptor, FilterDescriptor } from '@progress/kendo-data-query';
import { AxisSettings } from './interfaces';
/** @hidden */
export declare const initializeFiltering: (rows: AxisSettings[], columns: AxisSettings[], filter?: CompositeFilterDescriptor | FilterDescriptor[]) => {
    hasFilter: boolean;
    predicate: Function;
    filteringAxes: AxisSettings[];
};
