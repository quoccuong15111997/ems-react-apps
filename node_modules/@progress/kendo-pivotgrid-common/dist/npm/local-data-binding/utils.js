"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/** @hidden */
exports.subNode = function (node, field, initialNode) {
    var childNode = node.get(field);
    if (!childNode) {
        childNode = initialNode || new Map();
        node.set(field, childNode);
    }
    return childNode;
};
var separator = '&';
/** @hidden */
exports.createKey = function (key, value) { return key + separator + value; };
/** @hidden */
exports.splitKeyValue = function (keyValue) {
    var separatorIndex = keyValue.indexOf(separator);
    if (separatorIndex !== -1) {
        var key = keyValue.substring(0, separatorIndex);
        var value = keyValue.substring(separatorIndex + 1);
        return [key, value];
    }
    else {
        return [keyValue, undefined];
    }
};
