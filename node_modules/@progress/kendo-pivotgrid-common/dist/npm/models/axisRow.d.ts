import { AxisDataItem } from './dataItem';
/**
 * Represents a row in the column and row header.
 */
export declare type AxisRow = {
    name?: string;
    level?: number;
    index: number;
    cells: AxisDataItem[];
};
